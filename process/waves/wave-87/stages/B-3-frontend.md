# Wave 87 — B-3 Frontend (SKIPPED)

Backend-only wave (`design_gap_flag=false`). No UI surface — the fix changes only the server-side membership insert; a default-'Member' member has the identical (all-false) permission surface as the former NULL-role member, so nothing renders differently. Nothing to implement.

```yaml
skipped: true
reason: backend-only data-hygiene; no UI surface
```
