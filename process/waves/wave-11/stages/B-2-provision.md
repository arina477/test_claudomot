# Wave 11 — B-2 Provision (supertokens-integration, ab6ce69)
Provisioned: studyhall-e2e-fixture@example.com (user-id 21984eb2-8029-4c1b-9e73-bc586a0be4d2), email VERIFIED via SuperTokens core admin API (POST /recipe/user/email/verify/token → /recipe/user/email/verify; GET confirmed isVerified:true) — FIRST attempt, no fallback. Core reached via a temp Railway public domain (PORT=3567) created+DELETED (<60s, confirmed 404); API key in-memory only. PROOF: signin → POST /servers → 201 {id,ownerId=21984eb2} (verified claim st-ev.v:true passes AuthGuard on a privileged route — NOT /me which is EV-exempt). Creds in GITIGNORED command-center/testing/test-accounts.md (+ re-verify snippet). Committed (NON-SECRET): project.yaml test_users label+email; apps/api/scripts/re-verify-fixture.sh (reads key from Railway CLI at runtime, no hardcoded creds). NO secret committed.
