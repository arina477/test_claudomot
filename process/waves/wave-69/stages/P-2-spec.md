# P-2 Spec (pointer) — wave-69
Source of truth: seed tasks row 9f2bb017 description (YAML head, 3 self-contained blocks + prose). wave_type: multi-spec. design_gap_flag: true.
- **Spec A (9f2bb017):** reports table (target_type server|member|message, target_server_id routing, reason, status) + migration; POST /reports (session callerId, validate+resolve target_server_id); shared ReportSchema/CreateReportSchema; owner-initiated unlist (is_public=false → drops from discover, owner-gated); ReportsModule.
- **Spec B (d7250881):** GET /servers/:serverId/reports (moderate_members-gated) + POST .../resolve (timeout→ModerationService / delete_message→MessagesService soft-delete / dismiss; flip status+resolved_at/by); RANK GUARD via routing through setMemberTimeout (mod can't act on owner/manage_server); cross-server tamper guard (target_server_id===serverId); no re-implemented permission system.
- **Spec C (96d5ed58):** report affordance (server on /discover + member + message → reason dialog → POST /reports) + owner report inbox (GET reports + action buttons → resolve; gated on moderate_members via /me/permissions); success/error; minimal (filtering/appeals/block deferred); per D-3 canonicalized design.
CARRIES: report-action authz server-side (can(moderate_members)+rank-guard, hard AC); no-IDOR (session callerId); cross-server tamper guard; deleteMessage channelId from message row; report all 3 target types. design_gap TRUE → D-block (report dialog + owner inbox; prior art member-moderation.html + notifications-center.html). STRATEGIC: launch-gate incomplete after this bundle; public-launch-go founder-reserved.
