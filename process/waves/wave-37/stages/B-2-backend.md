# Wave 37 — B-2 Backend (node-specialist)
notifications.service.ts (@OnEvent('mention.created') persist ON CONFLICT DO NOTHING on mention partial-unique + try/catch-swallow decoupled from message flow; createForReminder ON CONFLICT; listForUser single-query LEFT JOIN enrichment [actor display_name??username, channelName, assignmentTitle+dueDate, messageExcerpt 150ch] + parallel unreadCount; markRead UPDATE WHERE id AND user_id → NotFoundException 404 if 0 rows [owner-404 convention], idempotent; markAllRead), notifications.controller.ts (GET /me/notifications?cursor, PATCH /:id/read, POST /read-all, session userId no param), notifications.module.ts (+service+controller), reminder-scan.service.ts (createForReminder in send-once guard). Typecheck+Biome clean. Deviation: rebuilt stale shared dist; updated reminder-scan.spec stub. ACCEPT.
