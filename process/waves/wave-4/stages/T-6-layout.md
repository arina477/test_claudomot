# Wave 4 — T-6-layout
active-partial: settings-profile built per design/settings-profile.html (username/avatar/accent controls); shell renders avatar (initials fallback) + accent CSS var; RTL render tests. Live pixel-diff deferred (browser limit c51589cd).
