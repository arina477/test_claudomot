# Wave 4 — P-0 Frame
## Discover
- wave_db_id 82387899 (wave 4); M1; no-prior-spec → full P-1..P-3. Seed 2a655960 (wave-3 RESCOPE-AUTO-SPLIT sibling, founder-approved). settings-profile.html mockup exists → design_gap_flag likely false.
## Reframe — disposition: PROCEED
- **problem-framer: PROCEED** — cause-layer (schema + API + storage subsystem; verified users table is id/email/display_name only). No antipatterns; declined RESCOPE-AUTO-SPLIT (coherent already-split slice; storage design pre-decided in _library.md sdks/databases §presign — integration not net-new). Flags→P-2/P-3: presign pattern LOCKED (don't relitigate presign-vs-multipart); FilesModule SHARED (purpose:'avatar'|'attachment' — don't hard-code avatar-only); accent_color vs databases.md avatar_color naming reconcile (use /profile-on-users, lower churn); Railway Bucket creds ACCOUNT-ISSUED → P-3 plan a founder-credential request/monitor so C/T don't block late.
- **ceo-reviewer: PROCEED (HOLD-SCOPE)** — right thing (completes M1 profile pillar; jumping to M2 would ship a 'coming soon'-stubbed page; hardening [rate-limit/branch-protection/domain] is the M1 tail, gated on real users). Right ambition (storage subsystem is NOT avatar-specific — shared presign pattern M3 attachments need; building under low-blast-radius avatar de-risks M3; deferring = re-churn). HOLD to the locked storage contract (NO resize/transcode/CDN). Username uniqueness = real correctness surface → ACs must cover collision/case-fold/concurrent-claim, not just happy path.
- mvp-thinner: SKIP (M1 platform-foundation).
- **Final framing for P-block:** full-stack profile completion = users +username(unique)/avatar_url/accent_color + profile GET/PATCH extension + shared FilesModule (presign, 2MB, MIME-allowlist, server-controlled key) + settings-profile wiring. Carries: storage credential founder-ask (B-0); presign locked; username collision ACs; accent_color naming.
