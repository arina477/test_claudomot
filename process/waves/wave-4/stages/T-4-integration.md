# Wave 4 — T-4-integration
active-live (C-2): username set→200 / dup→409 / bad→400, accent→200, GET /profile 4 fields, migration (users +3 cols + lower(username) unique idx) applied to prod Postgres. DB+API integration verified live.
