# D-2 Variants — timer-duration-config

- **Staging file:** `design/staging/timer-duration-config.html` (committed, 30.6 KB).
- **Generation approach:** single `/aidesigner` pass (initial generate). Prompt = D-1 brief + the adopted `design/study-timer.html` (to extend its chrome/.btn classes/emerald-amber phase language/surface layering) + `design/DESIGN-SYSTEM.md`. Directed to render all 5 affordance states (idle-editable, locked-running/paused with reset hint, validation-error with --danger, applying, applied/synced) + the <1024 slim-bar compact-collapse that doesn't crowd the countdown/phase-bar.
- **`/aidesigner` warnings:** none (HTTP 200). Credits available.
- **Checkpoint (Action 3):** SKIPPED — active mode `automatic` (per D-2 § Action 3 skip rule). Additionally a trivial extension of an existing adopted mockup (adds inputs+button to a shipped widget, reuses primitives), not a novel component class.
