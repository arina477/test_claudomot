# Wave 41 — D-2 Variants — member-moderation UI
- Staging file: `design/staging/member-moderation.html` (38.9KB, aidesigner Recipe 1 generate).
- Generation approach: brief + DESIGN-SYSTEM.md + server-channel-view.html prior-art (member roster rows) passed inline. One variant (the member panel + moderation kebab + Time out/Remove timeout menu + duration popover 5m/1h/1day + amber muted indicator).
- aidesigner warnings: none (HTTP 200).
