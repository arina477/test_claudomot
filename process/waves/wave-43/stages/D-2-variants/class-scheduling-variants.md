# D-2 Variants — class-scheduling
- **Staging:** design/staging/class-scheduling.html (aidesigner Recipe 1, 53KB).
- **Approach:** brief + DESIGN-SYSTEM inline → aidesigner. Renders date-grouped class calendar/agenda + session cards (title/time/recurrence chip/organizer), authoring modal (title/desc/start/end/None-or-Weekly recurrence + until), session detail w/ role-gated edit/delete, empty state. Reuses assignments-panel rows + role=dialog modal + emerald/amber chips.
- **aidesigner warnings:** none (HTTP 200).
- **Non-goal check:** no grade/score/RSVP/reminder/ICS affordance (the 1 keyword hit was "Physics" in a CSS comment). Agenda-list (not month-grid) per brief §10.
