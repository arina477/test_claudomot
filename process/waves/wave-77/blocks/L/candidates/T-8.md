4. Assert the body content of a denied or hidden response at T-8, not the status code alone, before closing the probe.
   Why: A matching status can mask the wrong reason, like an email-403 read as an authz-403.
