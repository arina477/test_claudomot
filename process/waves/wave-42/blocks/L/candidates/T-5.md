2. After the MCP-failure bypass to bundled chromium, write the browser flag into .mcp.json before exit.
   Why: An unpatched config makes each later session rediscover the bypass instead of inheriting it.
