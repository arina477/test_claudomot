# Wave 83 — P-0 Frame

## Discover
- **wave_db_id:** d15dad5d-d27f-4a46-a3de-494127e3cc9c (wave_number 83)
- **Prior-work:** SEED SWAPPED. Original seed 4a92327c (ParseUUIDPipe on uuid path params) DROPPED at P-0 discovery — premise EVAPORATED: the malformed-uuid→500 class was already closed app-wide by wave-33 (global SupertokensExceptionFilter 22P02→400, main.ts:121 + pg-error-utils.ts) and wave-40 (users.controller NUL/control-byte text-id guard), and is regression-locked by apps/api/test/integration/malformed-uuid-params.spec.ts. ParseUUIDPipe is a rejected convention (would 400 opaque text SuperTokens ids). Task cancelled with note.
- **Re-seeded with:** 875b97f4 (API security-headers hardening) — premise VERIFIED LIVE: `x-powered-by: Express` present + NO `Strict-Transport-Security` on api-production responses; no helmet wiring in main.ts.
- **Roadmap milestone:** unassigned (roadmap complete, 14/14 done; founder bug-fix phase). waves.milestone_id stays NULL.
- **Spec-contract short-circuit verdict:** no-prior-spec (prose seed → full P-1..P-3).
- **Product decisions:** none Tier-3 (security-headers config is a technical default, rule 17; LOW/no-exploit hardening).

## Reframe
- **Original framing (875b97f4):** add HSTS, disable x-powered-by, generic 429 body. 3 items, main.ts/app.module config, LOW/no-exploit defense-in-depth.
- **problem-framer:** PROCEED (0 antipatterns). Refinements: (1) bound helmet to SAFE headers only — helmet's default CSP/X-Frame/COEP/CORP could break the cross-origin SPA + SuperTokens credentialed CORS already wired in main.ts; (2) the 429 real target is the NestJS ThrottlerGuard (app.module) — the Express authRateLimiter in main.ts already emits a generic body, so don't touch that clean path; (3) choose HSTS max-age/includeSubDomains/preload deliberately (preload is effectively irreversible).
- **ceo-reviewer:** SELECTIVE-EXPANSION. Since helmet is added anyway for HSTS, also emit the FLAT on-by-default headers (X-Content-Type-Options nosniff, X-Frame-Options, Referrer-Policy) at zero marginal cost — HSTS-only is a 3/10 slice of a same-cost 9/10. CRITICAL guardrail: do NOT add a bespoke API CSP / Cross-Origin-Resource-Policy — web+api are different Railway origins; helmet's default CSP+CORP would silently break the cross-origin JSON fetch. T-8 must prove web→api credentialed request still succeeds after helmet lands. Soft note (non-blocking, N-1/founder's call): httpOnly session-cookie storage (9535895f) may be the higher-leverage security wave to sequence next.
- **mvp-thinner:** n/a (not a product-feature milestone — roadmap terminal).
- **Mediation:** the two reviewers are COMPLEMENTARY, not conflicting — ceo-reviewer's flat-header expansion is exactly the safe subset problem-framer's "don't enable helmet's dangerous suite" permits. Accept SELECTIVE-EXPANSION WITH problem-framer's CSP/CORP fence.
- **Disposition:** PROCEED (with selective expansion + guardrail).
- **Final framing the P-block will use:** Add helmet emitting SAFE flat security headers only — **HSTS** (deliberate max-age + includeSubDomains; hold preload unless justified), **X-Content-Type-Options: nosniff**, **X-Frame-Options**, **Referrer-Policy** — plus **disable x-powered-by**. EXPLICITLY disable/fence helmet's **CSP**, **Cross-Origin-Resource-Policy**, **Cross-Origin-Embedder-Policy** (cross-origin-breaking). Fix the **NestJS ThrottlerGuard** 429 body to drop the "ThrottlerException" class name (keep 429 status + Retry-After); leave the Express authRateLimiter path untouched (already clean). **T-8 (auth/security wave)** must: assert each new header on api responses, assert the 429 body no longer leaks the class name, and PROVE the deployed web→api credentialed fetch + SuperTokens CORS still succeed after helmet lands.
