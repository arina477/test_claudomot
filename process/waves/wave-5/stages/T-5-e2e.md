# Wave 5 — T-5-e2e
active — the NEW CI Playwright chromium job (c51589cd) ran + PASSED in PR#12 CI (2 tests: / + /login render). Closes the deferred browser-E2E gap from waves 1/3/4. Real browser coverage now in CI (non-required check, but green).
