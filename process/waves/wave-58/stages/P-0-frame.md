# P-0 — Frame (wave-58)
## Discover
wave_db_id 8dc68f98 (wave 58). Seed a1dda389 (wave-45 V-2 F2 test-honesty debt). M8 (in_progress), backfilled. Short-circuit no-prior-spec. No Tier-3. M9-Monetization ACUTELY flagged for founder (non-pausing, 4th surfacing at close).
## Reframe
**Framing:** delete-any-message.spec.ts:146-162 verifies cross-client message:deleted fan-out to client B as a pass-regardless SOFT-CHECK (`.waitFor(hidden,8s).then(true).catch(false)` → console.log, no expect gates it). Root cause (per the spec's own NOTE :148-152): B's socket channel-room join RACES A's delete, so the fan-out can be missed headless → the test masks it by passing regardless.
- **problem-framer — PROCEED.** Verified :153-162 is a genuine pass-regardless soft-check (boolean → console.log, no expect). Fix targets the correct layer (the test, not the feature — backend fan-out proven wave-41 T-4/T-8). **Load-bearing (P-2):** the robust shape = await B's channel-join ACK before A deletes (kills the documented race) + a BOUNDED RETRIED window (Playwright expect auto-retry, NOT a fixed one-shot that flakes). Fixes the single-client-realtime anti-pattern (T-5 rule 3). Clean scope (one soft-check; RBAC/IDOR steps 6/8 already hard-green, untouched).
- **ceo-reviewer — PROCEED / HOLD-SCOPE.** Floor-clearing worth (least-low-value tail item; residual risk = a FUTURE fan-out regression escaping this e2e layer). CONTRACT-CORRECT to continue: M9's success metric is founder-TBD → M9 CANNOT be decomposed/built, so it's not pickable work; this is legitimate small debt, not make-work. Keep M9 flag LOUD (4th surfacing at close). Minimal fix, no spec rewrite.
- **mvp-thinner — OK.** Single indivisible AC (soft-check → hard-assert conversion, one block); nothing to split; not over-cut. Sub-floor → floor override by rule.

**Disposition: PROCEED (minimal test-honesty fix).**
**Final framing:** wave-58 replaces the pass-regardless soft-check in delete-any-message.spec.ts with a DETERMINISTIC, race-free, bounded-retried hard assertion that client B receives the message:deleted fan-out after A deletes — (1) ensure B's channel-room subscription is established BEFORE A issues the delete (await a join-ack/ready signal to kill the race the NOTE documents), (2) hard-`expect` B's tombstone within a bounded retried window (Playwright auto-retry). The test now FAILS if B misses the fan-out. `claimed_task_ids=[a1dda389]`. design_gap_flag FALSE (e2e test-only, no production/UI change). NO spec rewrite (RBAC/IDOR steps unchanged).
