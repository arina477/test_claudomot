# Wave 76 — P-0 Frame

## Discover section
- **wave_db_id:** 7df4fb16-0999-4184-88c4-50f5e7d472d8 (wave_number 76; milestone_id backfilled → M13 b7400254)
- **Prior-work citation:** M9 substrate (wave-74/75 — subscriptions + EntitlementsService + educatorAdminTools flag + EntitlementGuard + boolean educator-tools stub). This wave builds the real access-controlled educator-tools API on top of that stub.
- **Roadmap milestone:** M13 — Institution partnerships & portable identity (in_progress; promoted 2026-07-07 after M9 good-enough). First autonomous slice = leg (1) educator admin console + analytics.
- **Spec-contract short-circuit verdict:** `no-prior-spec` (decomposer prose; full P-1..P-3).
- **Product-decision resolutions:** M13 promotion + fencing recorded in product-decisions 2026-07-07. B2B2C go-to-market + success metric (_TBD_) FENCED (founder-reserved); this wave is pure autonomous engineering over shipped data (consistent with the pricing-only standing delegation + M13 ## Approach).

## Reframe section
- **Original framing:** 4-task bundle — educator admin API foundation (seed 682e0912) + owner/member authz gate (ecf79f4a, reparented) + analytics aggregates API (80505bb1) + Educator Admin Console web UI (d81e266d).
- **problem-framer:** **PROCEED** — cause-level (attacks the real gap: the M9 substrate has no safe data-exposure boundary — the educator-tools stub gates on tier only + returns constant {enabled:true}, so any authed user can read any school-tier server's status; the EntitlementGuard comment itself says compose an owner/member check before exposing data). 7/8 seed claims verified in code. 2 notes → P-2:
  - **NOTE-1 (load-bearing):** the seed cites reusing "the shipped Educator/Facilitator RBAC role" — **no such named role exists**; the `roles` table (apps/api/src/db/schema/servers.ts) is capability-based (boolean flags like manage_assignments, moderate_members); "Educator/Facilitator" is only UI copy in ServerRolesPage.tsx. **P-2 MUST define the educator predicate concretely** as `owner OR member holding capability flag X` (pick the concrete capability), not a named role.
  - **NOTE-2 (minor):** the /status "preserve OR supersede" AC fork risks a silent breaking change to the wave-75 endpoint with passing tests → **P-2 decide explicitly** (preserve the boolean contract + add richer endpoints, or supersede with a documented migration).
- **ceo-reviewer:** **PROCEED / HOLD-SCOPE** — leg (1) is the correct FIRST M13 wedge (reuse-heavy over shipped data + the M9 entitlement; legs 2 portable-identity + 3 E2E need new from-scratch substrate — more expensive first steps). 4-task bundle is one coherent vertical slice, right-sized (backend-only would ship an invisible 3/10; the UI makes it demonstrable for small marginal cost; doesn't reach for identity/E2E). Sequencing engineering-core-before-metric is the sanctioned M9 pattern. No scope change.
- **mvp-thinner:** **OK** (metric-absence flag) — M13's success metric is _TBD_/fenced, so authoritative AC-level thinness can't be computed (can't improvise the founder's metric). On the best proxy (M13 ## Approach defines leg-1 as "console + analytics" verbatim), all 4 ACs are load-bearing: seed = irreducible substrate; ecf79f4a = the "access-controlled" clause; analytics = named in leg-1 scope + what the console renders (NOT a defer-able second slice); console UI = the "usable console" clause. Deferring analytics/UI would cut load-bearing scope, not thin it. `floor_constraint_active: false`. Recommends PROCEED on the engineering core + surface metric non-blocking.
- **Mediation outcome:** no expansion-vs-thin conflict (ceo HOLD-SCOPE, mvp-thinner OK). All PROCEED/OK.
- **Sibling task changes:** none (ecf79f4a already reparented into the bundle by the decomposer).
- **Disposition:** **PROCEED** (4 tasks, no change).
- **Final framing (rest of P-block):** Ship M13's first autonomous slice — a real access-controlled educator-tools admin API (AuthGuard + EntitlementGuard(educatorAdminTools) + a concrete owner/educator predicate) replacing the M9 boolean stub; a read-only server analytics aggregates API over shipped data; and an Educator Admin Console web UI gated on educatorAdminTools. **Carry to P-2:** NOTE-1 (define the educator predicate as owner OR member-with-concrete-capability, NOT a non-existent named role); NOTE-2 (decide the /status endpoint preserve-vs-supersede explicitly). **Carry to P-1:** UI wave (console) → likely design_gap_flag true → D-block. **Carry to P-4:** security-scope tightened gate (authz on a data-exposing endpoint) + T-8. **Fenced:** B2B2C go-to-market + success metric (_TBD_) — surface non-blocking, do not gate.

**claimed_task_ids:** [682e0912-30db-495c-984e-34dd046b1504, ecf79f4a-42db-4536-a7e8-a94ebb408bec, 80505bb1-3037-4863-aca7-ac95bbfe4e47, d81e266d-8e8c-43f4-9d3c-69a741fbbf9d]
