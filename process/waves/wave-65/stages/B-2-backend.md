# B-2 Backend — wave-65 (SKIP)
No server change. The wave reuses GET /servers + GET /servers/:id (api-production unchanged, NOT redeployed). All work is client-side (Dexie cache + ServerContext read-through).
```yaml
skipped: true
services_touched: []
deviations: []
```
