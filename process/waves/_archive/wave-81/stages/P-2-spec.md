# Wave 81 — P-2 Spec (pointer)
Source of truth: task 2340d2d3 `tasks.description`. single-spec. design_gap false.
## ACs: /settings/profile scrolls to bottom (LIVE, founder bug) + /settings/privacy + public /privacy /terms / (landing); global body overflow UNTOUCHED; shell routes /app /discover no double-scroll; 6px DS scrollbar preserved; no churn on short pages. Fix = shared FullPageScroll (h-dvh overflow-y-auto).
