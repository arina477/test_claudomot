# Wave 12 — B Backend (commit-per-spec, REST→gateway)
- B-0 schema (28839a5): messages (UNIQUE(channel_id,idempotency_key)+INDEX) + @nestjs/event-emitter + EventEmitterModule.forRoot(). migration 0005.
- B-1 shared (3d92252): messaging.ts (MessageResponse/SendMessage/MessageList).
- a0c322b4 REST (c68abea): canViewChannelById (resolve server_id from channels.server_id-notNull → canViewChannel; 404 missing) + ChannelMessageGuard (channelId-only @Param, IDOR-safe, default-DENY). messages.service createMessage (idempotent on-conflict-return + message.created event) + listMessages (cursor). POST/GET /channels/:channelId/messages (AuthGuard+ChannelMessageGuard; author session). 189 api tests.
- 723b5b6a gateway (f602b95): @WebSocketGateway /messaging. WS-UPGRADE auth via io.use() middleware: extract sAccessToken from handshake cookie (+ auth.accessToken fallback) → getSessionWithoutRequestResponse → assertClaims(isVerified) → reject unauth at connect (socket.data.userId). join_channel → canViewChannelById → join room else error. @OnEvent(message.created) → server.to('channel:id').emit('message:new') room-only. single-pod in-memory. IoAdapter in main.ts. 200 api tests (+10 gateway: 5 WS-auth, join, fan-out).
