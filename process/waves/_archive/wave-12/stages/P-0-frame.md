# Wave 12 — P-0 Frame
- wave_db_id 8f5fb910 (wave 12); M3 6198650e. claimed [a0c322b4 MessagingModule-REST(seed), 723b5b6a Socket.IO-gateway, d999d29c message-UI]. multi-spec (~3200 LOC). The conversational core — M3 success metric (2 students exchange messages real-time <1s). Highest-complexity M3 slice (real-time WebSockets, NEW infra).
- **problem-framer PROCEED** (no antipatterns) — right cause-layer (messages REST + Socket.IO fan-out + UI = the metric); coherent vertical slice, DON'T split (REST-first-Socket.IO-later ships an unobservable wave, fails V-1; the dependency chain message.created→message:new→render is the tie-breaker vs LOC). Arch-conformance VERIFIED (schema/indexes/cursor-pagination [doubles as offline catch-up #11]/idempotency/two-namespaces match _library). **T-8 HARD flags:** (1) channel-gating — send+list both @UseGuards(AuthGuard, ChannelPermissionGuard)→can(); guard channelId from route-params (IDOR). (2) WS-upgrade auth — io.use() validates SuperTokens session on UPGRADE (not first message); unauth socket rejected. (3) no cross-channel leak — room-per-channel; join_channel re-derives can('read') server-side; fan-out server.to('channel:id') only. (4) no author-spoof — author_id session-derived, never body. INFRA: single-pod in-memory adapter correct (NO Redis — H2/single-pod pattern; P-3 must NOT add Redis); verify Railway proxy passes WS-Upgrade at C-2 (not assumed); deploy-verify must NOT false-green on a dead WS namespace (HTTP health ≠ socket). design_gap_flag TRUE-delta (server-channel-view.html exists; component-level message-row[pending/sent/failed]+composer gap).
- **ceo-reviewer PROCEED (HOLD-SCOPE)** — highest-leverage build (messaging is the content; servers are empty rooms without it; named in the bet's H1). real-time-from-start CORRECT (the <1s metric can't be met by REST-poll → not deferrable; right-sized). Correctly thinned (reactions/threads/mentions/attachments/presence/typing/member-list deferred). Sequencing right (RBAC was prerequisite [reused]; on critical path to M4 offline wedge [M3 Required-by M4]). HOLD-SCOPE contingent on T-8 rigor + two-client <1s verification.
- mvp-thinner: SKIP (platform-foundation/the success metric is the floor).
- design_gap_flag: TRUE (component-level message UI → D-block scoped to primitives).
- **Framing:** messages table (channel_id, author_id[session], content, created_at, idempotency-key UNIQUE(channel_id,idempotency_key)) + POST send + GET list (cursor) [both ChannelPermissionGuard-gated] + /messaging Socket.IO gateway (WS-upgrade session-auth, room-per-channel, can('read')-on-join, server.to fan-out) + message UI (composer + virtualized list, pending/sent/failed). Single-pod in-memory. CARRY: T-8 live-probe authed message paths via wave-11 verified fixture (T-8 rule 1); two-client real-time <1s proof; verify Railway WS at C-2.
