# Wave 12 — B-3 Frontend (d999d29c, 495e799)
messagingSocket (singleton /messaging, withCredentials, join/leave/onMessageNew). useMessagesWithRetry (fetch on channel-select, socket subscribe, optimistic sendMessage, retryMessage). MessageList (role=log aria-live; SentRow/PendingRow[aria-busy]/FailedRow[role=alert+retry]; empty-state in-place; load-older). MessageComposer (auto-grow, Enter-send/Shift+Enter). MainColumn wires them; ChannelSidebar channels clickable→selectChannel; ServerContext selectedChannelId. Optimistic: client-gen idempotencyKey→pending→POST→confirmed(dedup by id)/failed→retry(same key). Socket dedup (message:new skips own id); channel-switch leave/join re-subscribe; connect-error graceful. socket.io-client dep. 24 web tests.
