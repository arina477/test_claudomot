# P-0 — Frame (wave-53)

## Discover section

- **wave_db_id:** `cefd7597-6775-400f-9492-9c479481c363` (wave_number 53; FS-dir ↔ DB-row traceability only)
- **Prior-work citation:** wave-52 T-8 (penetration-tester) verified this finding live (F-1, V-2 non-blocking, LOW). Same error-handling class as wave-23 inherited non-UUID `:serverId → 500`. No prior wave has fixed this leak; this is the first drain of the M8 security-hardening tail.
- **Roadmap milestone:** M8 `84e17739-af5e-4396-beb9-b6f3d6836fc4` — "Educator tools & deeper academics" (in_progress). Wave-53 `milestone_id` backfilled. Seed already carried `milestone_id=M8`. M8 substantive scope shipped (per wave-52 N-1); draining the hardening tail security-first.
- **Spec-contract short-circuit verdict:** `no-prior-spec` (prose seed, no fenced YAML head → full P-1..P-3).
- **Product-decision resolutions:** none. LOW-severity security hardening; the fix approach (shared UUID-format guard at the parse layer vs. generic unknown-error → generic-client-message mapping with server-side detail logging) is a technical default (rule 17) — decided at P-3, not founder-facing. Security-scope → T-8 Security stage + P-4 security-scope-tightened gate apply.

## Reframe section

**Original task framing:** Fix the study-room.gateway.ts catch (~line 372) that forwards the raw Drizzle error verbatim to the client when a non-UUID serverId fails the Postgres UUID cast in assertMember (leaks query text + `server_members` table/column names + echoes the caller's own userId). Request is still DENIED → info-disclosure only, not auth bypass. Seed flags an APP-WIDE error-handling pattern (controllers/gateways casting client ids to uuid columns without format validation).

**problem-framer — PROCEED.** Symptom-vs-cause PASS: verified in code that all four parsers (`parseServerPayload`/`parseRoomPayload`/`parseCreatePayload`/`parseConfigPayload`, gateway lines 522-561) check type + non-empty length but NOT UUID format, so a malformed id slips through to the cast in `assertMember` (service:170-174) and the raw error leaks at the catch (gateway:371-373). The seed names the CAUSE layer (parse-layer guard / generic-error mapping), not a symptom-layer one-off patch → no antipattern (#1/#2) matched. Real (pen-tester-verified) schema/table/column disclosure on a shipped realtime surface — legitimate hygiene, not polish. Not RESCOPE-AUTO-SPLIT (one coherent concern = uuid-cast error handling). Scope note: recommends option (b) — reusable guard on study-room now, app-wide sweep as a follow-up seed (split is P-1's/merge's call). Process note: `PRODUCT-PRINCIPLES.md § Antipatterns` is empty (rules 1-5 only); applied embedded catalog; a project-specific "raw-DB-error-to-client" antipattern is an L-2 candidate on 2nd-wave confirmation.

**ceo-reviewer — SELECTIVE-EXPANSION.** Draining a hardening straggler IS the forward path (M8 state machine blocks M9 promotion until open tasks drain; BOARD rejected M9-M13 horizon-jumps 4×). A pen-tester-verified privacy leak is a defensible first drain (privacy posture is a named bet differentiator vs Discord — bet `ad1a3685`). But fixing ONLY the study-room instance is a 3/10; implement instead as the shared parse-layer UUID guard + generic-error mapper (an 8/10 that closes the class), same author/PR/~cost. Guardrail: LOW severity — no gold-plating into a validation framework; if closing the class cleanly exceeds one PR, ship the study-room leak-close now + seed a follow-up sweep. No strategic risk to the offline-first / academic wedge (hardens the substrate).

**mvp-thinner — THIN.** No mvp-critical AC to protect (seed traces to no `## Scope`/metric item; every metric-advancing M8 slice is already done). The thinness call is the intra-seed separability the seed itself names. KEEP in wave-53: (1) fix the verified-leaking study-room site, (2) build the reusable UUID-format guard / generic-error-mapping mechanism proven against that real leak. SPLIT to a sibling: the unbounded app-wide sweep of every OTHER uuid-cast-without-validation site (unknown site count until audit; no other site verified-leaking — wave-23 was a 500, not a confirmed disclosure; consumes the guard rather than blocking on it). Not OVER-CUT (study-room fix + guard is independently valuable).

**Mediation outcome (ceo-reviewer × mvp-thinner):** Both propose expansion-to-reusable-guard + defer-the-sweep — they converge, not conflict. Per P-0 Mediation precedence, ceo-reviewer wins the tie because every M8 mvp-critical `## Scope` item is covered by a `status='done'` child task (per wave-52 N-1 judgment). ceo-reviewer's own guardrail ("seed a follow-up if the sweep exceeds one clean PR") lands on the same disposition as mvp-thinner's THIN — the app-wide sweep is unbounded (unknown site count, needs an audit) and clearly exceeds a single clean PR. **Accepted mvp-thinner's split.**

**Sibling task created:** `c52a7a52` — "App-wide sweep: apply UUID-format guard to all remaining client-serverId/roomId uuid-cast sites (info-disclosure hardening)". INSERTed with `milestone_id=M8`, `status='todo'`, `wave_id=NULL`, and **`parent_task_id=NULL`** (top-level seed) — a deliberate deviation from Action 6's literal "parent_task_id=<seed>": the sweep is a DEFERRED follow-up that does NOT ride wave-53's bundle, so parenting it to fb1c367a (consumed + closed this wave) would strand it (N-2 seed-pick requires `parent_task_id IS NULL`; sibling-load requires a live parent). Matches the established M8 hardening-tail pattern — every existing F-1 follow-up (incl. fb1c367a) is a top-level seed. It becomes a future N-2 seed candidate.

**Disposition:** PROCEED (selective expansion to a reusable guard; app-wide sweep split to deferred seed `c52a7a52`).

**Final framing the rest of P-block will use:** wave-53 ships (1) the study-room.gateway.ts info-disclosure leak-close, and (2) a reusable UUID-format-validation guard at the parse layer AND/OR a generic unknown-error → generic-client-message mapper (with server-side detail logging), applied to the study-room gateway as first consumer — proven against the pen-tester-verified leak. `claimed_task_ids = [fb1c367a]` (single-seed; the sweep sibling `c52a7a52` is deferred, not claimed). Security-scope wave → T-8 + P-4 security-tightened gate. Likely `design_gap_flag: false` (backend/error-handling only, no new UI surface — confirmed at P-1).
