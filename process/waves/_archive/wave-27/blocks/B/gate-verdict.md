# Wave 27 — B-6 Verdict

**Reviewer:** head-builder (fresh spawn, Phase 1 independent gate)
**Reviewed against:** process/waves/wave-27/blocks/B/review-artifacts.md
**Attempt:** 1  (first gate)

## Verdict
APPROVED

## Rationale
Both specs are honest, behavior-preserving, and correctly scoped for a self-use MVP with no user-visible change. **Spec A (server index)** is a genuine index-only optimization: `servers.ts` adds `index('server_members_user_id_idx').on(table.user_id)` alongside the existing unique constraint; migration `0012_flashy_spacker_dave.sql` is a real committed `CREATE INDEX "server_members_user_id_idx" ON "server_members" USING btree ("user_id")`, journaled at idx 12, with no startup auto-migrate. Critically, `presence.service.ts` has an **empty diff** — `getServerIdsForUser`/`getCoMemberUserIds` logic is untouched (index-only, no rewrite), so the optimization is purely a planner win with identical semantics. The EXPLAIN proof test is NOT decorative: it runs `EXPLAIN (FORMAT TEXT) SELECT server_id FROM server_members WHERE user_id = $1` against **real Postgres** (CF-2 `./pg-harness` imported first to bind DATABASE_URL before the SUT), seeds a real fixture row so the planner sees data, and asserts three things — `Index Scan` present, `server_members_user_id_idx` named, and `Seq Scan on server_members` absent — plus an AC3 behavior-preserving test that the returned server-ID set is correct across a 3-user topology. `skipIf(!DATABASE_URL_TEST)` fails loud with an explicit `it.skip` message (not a silent pass) and the proof is CI-gated where PG is provided. **Spec B (client subscription lift)** is correct and preserves CARRY-B: `MessageList` now holds exactly ONE list-level `subscribePresence` in a single `useEffect` driving a `presenceTick`; each `SentRow` derives its author's tri-state (`boolean|null`) from the store and passes the scalar to `AuthorPresenceDot`, which is `React.memo`'d on that scalar `status` prop. The subscriber budget drops from O(N) (wave-26 per-row) to O(1) — asserted by the `subscribePresenceCallCount === 1` test for a 2-message list. CARRY-B render-scoping is preserved and asserted: when author-B flips offline, author-A's dot output is unchanged (memo bails on the identical scalar), verified by the dedicated CARRY-B test. Tri-state (online/offline/unknown→no-dot via `hasPresence` gate), AC4 single socket, and the T-5 self-seed regression are all intact. **Commit-per-spec discipline (Action 6) passes cleanly:** ff4126b touches only `apps/api/*` and cites `Refs: 6a546c7b`; bd18a08 touches only `apps/web/*` and cites `Refs: 07361daf`; no commit crosses both apps; both claimed_task_ids have a citing commit. **CARRY-A honored** — Spec A was authored by postgres-pro, not database-administrator. No scale gold-plating, no unguarded door touched (read-path index + presence-render refactor only).

## Top-2 scrutinized

### 1. Spec A honesty — "decorative test" heuristic (PASS)
The EXPLAIN test is the load-bearing proof and it is real, not a mock. It imports the pg-harness side-effect module FIRST (CF-2) so the lazy `db` singleton resolves to the test DB, then issues a genuine `EXPLAIN (FORMAT TEXT)` against live Postgres and asserts Index Scan on the named index AND the absence of `Seq Scan on server_members`. A fixture row is seeded so the planner does not short-circuit to a Seq Scan on an empty table (a common false-green in index-proof tests). The query text mirrors exactly what `getServerIdsForUser` executes. The `presence.service.ts` diff is empty, confirming index-only with zero query-logic change — the strongest possible behavior-preservation signal. `skipIf` is fail-loud with an explicit skip reason; CI provides DATABASE_URL_TEST so the proof actually runs in the pipeline.

### 2. Spec B CARRY-B — does the memo actually bail? (PASS)
The concern was a whole-list re-render silently dropping per-author scoping (a partial perf regression). Mechanism verified sound: `presenceTick` is list-level, so all SentRows re-run on any event — but the presence-DOT render is scoped by `React.memo` on `AuthorPresenceDot`'s scalar `status: boolean|null` prop. For an author whose status is unchanged, the derived scalar is referentially identical across renders, so the default shallow comparison bails and neither `AuthorPresenceDot` nor `PresenceDot` re-renders. The CARRY-B test proves the observable contract (B flips → A's dot output unchanged) and the AC1 test proves the O(1) subscription budget. Net: wave-26's O(N) socket-listener budget becomes O(1) with per-author dot-render scoping preserved. (Minor honesty note: the test comment references a "custom areEqual", but the code uses default memo on a scalar prop, which is functionally equivalent and correct — no rework warranted; flagged for /review as a cosmetic comment nit at most.)

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
