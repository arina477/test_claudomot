# Wave 27 — T-6-layout

No visual delta. Spec B is behavior-preserving (the PresenceDot component + dot styling unchanged; only the subscription topology changed). T-5 live confirmed dots render identically (emerald online / grey offline, same DOM). No new UI surface.

```yaml
test_pattern: ci-verified-or-active
skipped: false
findings: []
```
