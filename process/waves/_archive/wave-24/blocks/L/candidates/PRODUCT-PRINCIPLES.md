2. Override-ship via BOARD when the mandatory expansion returns incomplete-scope on a named external block; do not pad.
   Why: Padding an externally-blocked milestone distorts wave focus without resolving the block.
