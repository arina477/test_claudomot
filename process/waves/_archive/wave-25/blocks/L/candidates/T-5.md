1. On Playwright MCP launch failure, drive the bundled chromium directly rather than marking the layer blocked.
   Why: Bundled chromium shares the render path, so a launch failure loses no real coverage.
