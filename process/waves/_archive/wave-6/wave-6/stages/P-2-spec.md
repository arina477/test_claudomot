# Wave 6 — P-2 Spec (pointer)
Source: task da242f6b .description. single-spec; CI boot-probe job (boot compiled `node dist/src/main.js` + throwaway PG + minimal env → poll /health 200 bounded → fail on crash; make a required check). design_gap_flag=false. No code change (CI-only).
