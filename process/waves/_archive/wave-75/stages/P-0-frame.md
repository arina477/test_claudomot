# Wave 75 — P-0 Frame

## Discover section
- **wave_db_id:** e977f3b9-b7e4-42f7-a2aa-828988946c39 (wave_number 75; milestone_id backfilled → M9 3e507bc0)
- **Prior-work citation:** wave-74 (M9 entitlements substrate — subscriptions table + EntitlementsService + read-only createServer gate, shipped d79dd18). This wave builds directly on it (swaps placeholder caps → real values; makes tiers changeable; enforces one limit). P-4 review scope = the delta (mock billing + enforcement + UI), not the substrate.
- **Roadmap milestone:** M9 — Monetization: freemium tiers (in_progress). Fits `## Scope` (billing module + entitlements, freemium tiers).
- **Spec-contract short-circuit verdict:** `no-prior-spec` (decomposer authored prose descriptions; P-2 authors the spec contract). Full P-1..P-3 run.
- **Product-decision resolutions:** MONEY/PRICING Tier-3 signals are **pre-resolved** by the founder's 2026-07-07 standing delegation + explicit directive — mock-payment approach, brain-set tier prices/limits/success-metric (recorded in M9 prose + product-decisions), real Stripe fenced pending founder keys (rule 6). No founder re-routing needed this wave.

## Reframe section
- **Original framing:** 4-task bundle (BillingProvider seam + mock upgrade endpoint / real TIER_CAPS + educator-tools enforcement / "Your plan" upgrade UI / createServer TOCTOU hardening).
- **problem-framer:** **PROCEED** — no antipatterns; symptom-vs-cause PASS (cause-level). Verified all seed premises against shipped code. Notes → P-1/P-2: (1) pin the M9 brain-set caps as canonical (current code drifted: free voice 50→10, server_pro 20GB→50GB, school 100GB→500GB); (2) make "maxServersPerOwner stays non-restrictive" an explicit AC (wave-74 regression guard) + ensure any atomicity work doesn't silently tighten it; (3) verify the seam mutates subscriptions.tier by serverId (per-server key), distinct from the create-gate's owner-as-free deferral.
- **ceo-reviewer:** **PROCEED / HOLD-SCOPE** — traces to the live "academic-tools + offline-first, displace Discord" bet; mock proves the expensive mechanism now so real Stripe collapses to a provider swap. 4 tasks right-sized; one enforced entitlement (educator-tools) is 9/10 on the success metric (storage/voice enforcement need quota-metering + LiveKit wiring — genuine later slices). Binding note → P-3/head-builder: **shape the BillingProvider interface for a real Stripe provider's async redirect + webhook reality, not the mock's synchronous shape** — else the "Stripe is a drop-in later" thesis silently breaks.
- **mvp-thinner:** **THIN — defer db90252a (createServer TOCTOU) only.** The metric ("self-upgrade free→server_pro, higher entitlements take effect immediately, verified live") never traverses the createServer count path; this wave keeps server-count caps non-restrictive (69765cee hard AC), so the TOCTOU stays provably unreachable — fixing it now is untestable premature hardening. It becomes load-bearing at the first slice that ships a restrictive server-count cap. KEEP the other 3 (downgrade shares the same seam — zero net split; the "Your plan" panel IS the verified-live surface). Floor NOT triggered (residual ~2,000–3,200 LOC / 3 specs stays a coherent charging slice).

- **Mediation outcome:** No expansion-vs-thin tie (ceo-reviewer said HOLD-SCOPE, not EXPAND). Orchestrator arbitrated ceo-reviewer "keep db90252a" vs mvp-thinner "defer db90252a": **accepted mvp-thinner's defer.** ceo-reviewer's inclusion rationale ("correctness bug on the load-bearing path") rests on a misread — the path THIS wave makes load-bearing is tier-change→educator-tools, not createServer count; the count path stays unreachable at non-restrictive caps. Deferring also makes the fix *testable* (against an actually-reachable cap) instead of untestable-now. head-product validates this merge at P-4.
- **Sibling task changes:** db90252a **de-parented** (parent_task_id → NULL, wave_id NULL, status todo) — removed from the wave-75 bundle, retained as a standalone M9 todo, load-bearing prerequisite for the future restrictive-caps slice. No new task INSERTed (it already existed).
- **Disposition:** **PROCEED** (narrowed to 3 tasks).
- **Final framing (rest of P-block):** Ship the M9 freemium upgrade path behind a MOCK/TEST billing provider — `BillingProvider` seam + `MockBillingProvider` + owner-only tier-change endpoint (seed 4bc40741); swap TIER_CAPS to the canonical brain-set values + enforce the educator-admin-tools entitlement for real (69765cee); "Your plan" panel + mock upgrade affordance + refresh-on-success (77665ee5). Real Stripe fenced. **Carry to P-2/P-3:** canonical caps pinned; explicit non-regression AC on maxServersPerOwner; per-serverId seam key; BillingProvider interface shaped for real Stripe async/webhook reality. **Carry to P-4:** security-scope tightened gate (payments/owner-only endpoint, no-IDOR) + T-8.

**claimed_task_ids:** [4bc40741-146a-4f05-8970-1614eb6b2b43, 69765cee-9764-48b1-bdad-2c45ef05f25a, 77665ee5-f484-464c-b4ee-3b86cae65480]
