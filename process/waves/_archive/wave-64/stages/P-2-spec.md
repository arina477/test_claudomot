# P-2 Spec — wave-64 (pointer, multi-spec, 2 blocks)
Authoritative: tasks.description of SEED a1b9b06b (YAML head with `specs:` [substrate + message-attachment] + --- + prose).
wave_type: multi-spec; claimed_task_ids: [a1b9b06b (substrate), 83aa28e4 (message image-attachment)]; design_gap_flag: false.
10e7543f (assignment media) DESCOPED at P-0 → deferred M12 candidate (needs online assignment-attachment-open surface first).
Key: Dexie v4 blob substrate (cachedAttachmentBlobs + get/put + per-item 10MiB cap; RULE 11 restate all 7 prior tables) → wire MESSAGE image-attachment (thumbnail :439 + lightbox :467) cache-on-view (presigned 1h TTL → fetch at view time) + offline object-URL render with REVOKE discipline. CORS-OPEN verified. Image-only; non-image chips unchanged.
