# Wave 17 — P-4 Phase-2 spec-vs-roadmap drift check (jenny)

**Verdict: APPROVE** — spec is a 1:1 expression of the original wave-7 V-3 carry intent, and the roadmap framing (M3 tech-debt, floor-exempt, single-test scope, BOARD-ordering carry) is coherently recorded. No drift. CF-1 judged an intentional engineering choice WITHIN the spec's own contract, not spec-drift.

Task under check: `25523fb0-edef-46e4-928b-55e78495d181` (real-PG create-server rollback test, test-infra).

---

## Per-item

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | Spec matches original intent (real-PG test, force mid-txn failure, assert no orphan rows) | **MATCHES** | Original (wave-7 V-3): "create-server transaction atomicity test uses a db.transaction stub that always invokes its callback, so the rollback path is empirically unproven. Add a real-Postgres integration test that forces a mid-transaction failure and asserts no orphan rows persist." Spec AC-1 (real txn engine, NOT the always-invoke stub) + AC-2 (force failure PART-WAY THROUGH → assert ROLLED BACK, zero orphans in servers/roles/server_members/categories/channels) restate this verbatim. 1:1. |
| 2 | No creep | **MATCHES** | The only additions over the bare original are the companion POSITIVE commit assertion (AC-3) and apply-real-migrations (AC-4). Both are intrinsic proofs OF the same claim — the positive case proves the harness runs real txns (not no-ops, the original's whole worry), and migrate-first is what makes "real Postgres" real. Not new scope; they close the proof. |
| 3 | No cut | **MATCHES** | Every clause of the original (real PG, mid-txn failure, no orphan rows) is present and strengthened with explicit per-table assertions + failure-position coverage (first-insert AND last-insert) in edge-cases. Nothing dropped. |
| 4 | **CF-1: PGlite "PREFERRED" → P-3 picks existing CI Postgres** | **MATCHES (intentional, in-contract)** | Spec `contracts.sdk` reads: "PGlite ... (PREFERRED) OR testcontainers OR **a dedicated test Postgres**. P-3 picks." The "PREFERRED" word attaches to PGlite as a *default suggestion*, but the contract explicitly delegates the pick to P-3 and explicitly enumerates "a dedicated test Postgres" as an allowed option. P-3 (lines 5–8) chose the existing CI Postgres-16 service (DATABASE_URL_TEST), giving an engineering rationale: zero new deps, exercises the SAME node-postgres/drizzle driver path prod uses (PGlite is a *different* driver), and matches the `db/index.spec.ts` real-DB precedent. This is a reasoned in-contract deviation, NOT drift — the spec's own delegation clause sanctions it, and the acceptance criteria (AC-1/AC-4/AC-5: real transactional engine, real migrations applied, CI-green + deterministic + isolated) are all satisfiable on CI PG. **Judged: intentional engineering choice within the contract.** Minor note (non-blocking): P-3's choice trades away the "stands up its OWN ephemeral PG / no dependence on external PG" property in AC edge-case ("harness must NOT depend on the prod DB or the live app; stands up its own ephemeral Postgres") — P-3 mitigates via skip-with-clear-reason locally when DATABASE_URL_TEST is unset + truncate-isolation, which preserves determinism/isolation but relies on a provided test PG rather than an in-process one. This is a deliberate, documented trade-off (P-3 line 8), aligned with the contract's "dedicated test Postgres" branch; flagging for head-product awareness, not as a DRIFT. |
| 5 | Floor-exemption (test-infra) consistent with wave-16 product-decision | **MATCHES** | wave-16 PD ("Test-coverage waves are exempt from the feature-LOC floor", product-decisions.md L215–218) establishes the precedent for test-infra/tech-debt seeds below the single-spec LOC floor, kept single-task to respect the N-2 pick. Spec prose ("Backend test-infra; floor-exempt per wave-16 P-1 precedent") + P-0 mvp-thinner ("doesn't trace to M3 success metric — carried tech-debt") apply it consistently. Same shape, same rationale, not re-litigated. |
| 6 | Scope stays a SINGLE create-server rollback test | **MATCHES** | Spec `claimed_task_ids = [25523fb0]` only. P-0 reframe (line 5, 11) explicitly scopes to create-server and DEFERS the owner-lockout/rbac call-site ("Only 2 db.transaction call-sites ... this wave scopes to create-server"). Does NOT balloon into testing all db.transaction call-sites. |
| 7 | Does NOT build the full 02fa8011 tier | **MATCHES** | 02fa8011 stays a separate parked follow-on; spec explicitly frames the harness as the REUSABLE enabler that 02fa8011 will consume as "a future thin consumer" — but P-0 line 11 ("do NOT bundle 02fa8011") and P-3 line 31 (helper is what "the reusable real-PG tier 02fa8011 will consume") keep 02fa8011 OUT of this wave. The harness is the irreducible enabler, not the tier itself. Correct separation. |
| 8 | Does NOT advance/claim an M3 feature | **MATCHES** | This is carried M2→M3-reassigned tech-debt (P-0 line 6; product-decisions M2→M3 pivot L174) hardening an already-LIVE create-server flow — NOT an M3 messaging feature. M3 success-metric features (threads + attachments) remain unbuilt and unclaimed by this wave. No false-advance. |
| 9 | ceo BINDING ordering note (2/3 tech-debt → N-1 BOARD) coherently recorded as a sequencing guardrail | **MATCHES** | P-0 line 12 records it as a roadmap-SEQUENCING guardrail carried to wave-17 N-1: if wave-18 would AGAIN be parked tech-debt out-prioritizing threads/attachments, route a tech-debt-vs-feature ORDERING decision to BOARD before the decomposer fires. It is a delivery-ordering carry, NOT a change to THIS spec's scope or ACs — it governs what comes NEXT, not what this wave builds. Coherent and correctly scoped to N-1. |

---

## Clarification needed
None blocking. CF-1's local-ephemeral trade-off (item 4 note) is worth a one-line acknowledgement at the B-6 head-builder gate so the "skip cleanly when DATABASE_URL_TEST unset locally, RUN in CI" wiring (P-3 line 22) actually lands — but it is in-contract and does not change the verdict.

## Bottom line
**APPROVE.** Spec = original intent, no creep, no cut. Roadmap framing (single test-infra task, floor-exempt per wave-16 precedent, 02fa8011 deferred, no M3 feature claimed, ceo ordering note as N-1 guardrail) all consistent. CF-1 is an intentional, documented, in-contract engineering choice (the spec's `sdk` line delegates the pick to P-3 and lists "dedicated test Postgres" as allowed) — not drift.
