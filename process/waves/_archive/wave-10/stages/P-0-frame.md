# Wave 10 — P-0 Frame
- wave_db_id abe06365 (wave 10); M2 41e61975. claimed [35f191f4 RbacModule(seed), 2c927c44 channel-perm-overrides+guard, 7a10f13d owner-lockout, 0b9bcf35 role-mgmt-UI]. multi-spec (~3000-3800 LOC, biggest M2 wave). The access-control CAPSTONE — closes M2 success metric "see the right channels PER ROLE". After this M2 is closeable → M3 messaging.
- **problem-framer PROCEED** (no antipatterns) — cause-layer (role_id is a nullable scaffold; no roles table/RbacService/ChannelPermissionGuard yet; channel access = membership-only today); coherent dependency chain (roles+can()+assignment → channel-overrides+guard → owner-lockout → UI). **T-8 HARD flags:** server-side can() everywhere (gate API not just UI; IDOR test); ChannelPermissionGuard reads ROUTE-PARAMS only (body-spoof test); explicit DEFAULT-DENY for no-override; TRANSACTIONAL owner-lockout (concurrent-demote race); role-assignment authz (member-cannot-self-promote); server-side channel-LIST filtering (no hidden-channel enumeration). Split: NOT auto-split (required chain); P-1 sizing call — if forced, A=foundation+owner-lockout, B=channel-overrides+UI (don't split owner-lockout from role-mutation, or UI from its API). Arch: respects #6 single-role-per-member. Recon: table name channel_permission_overrides (M2 scope) vs permissions (_library L144) → P-2.
- **ceo-reviewer PROCEED (HOLD-SCOPE)** — closes M2's last success-metric clause; NOT too ambitious — the arch was ALREADY thinned at v6b (#3: single-role-per-member, NO join-tables/custom-role-builder/permission-matrix/hierarchy) → the planned scope IS the simpler model; cutting any piece = lesser-not-faster (unenforced/unsafe/unusable). Sequencing correct (RBAC before M3). HOLD-SCOPE contingent on T-8 rigor.
- mvp-thinner: SKIP (platform-foundation).
- design_gap_flag: TRUE (server-settings.html exists → roles tab = new content/interactions, not net-new page).
- **Framing:** RbacModule (roles table, RbacService.can() server-side, role CRUD+assignment owner/admin-gated) + channel_permission_overrides + ChannelPermissionGuard (gates API channel read/write, default-deny, server-side list-filter) + owner-lockout (transactional last-owner invariant) + role-mgmt UI. Single-role-per-member. CARRY: verified-prod-fixture 4a2ad286 ESCALATION-CRITICAL for B (live-verify RBAC authed paths).
