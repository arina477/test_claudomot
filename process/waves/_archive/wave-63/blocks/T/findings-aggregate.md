# Wave-63 T-block findings aggregate
T-5 LIVE offline probe = PASS (deployed prod 699a619): assignments (2) + schedule (22) rendered from Dexie v3 cache while transport-offline (navigator.onLine=false, /api fetch TypeError); falsification contrast confirmed (fresh /api fetch fails + timer-sync live feature degrades offline). T-9 gate = APPROVED. Code-level: Dexie v3 (5 v1+v2 tables restated verbatim, byte-compared by head-builder), academic-cache 16/16 incl v1→v2→v3 preservation (NAMED exit criterion, value-asserted) + window-isolation, offline-fallback panel tests 45/45 local re-run, full web 520/520 + api 731/731.
FINDING (non-blocking, pre-existing, NOT caused by wave-63): study-timer.test.tsx "400 renders inline error" is FLAKY (async race — failed once on CI, passed on re-run + 2× local). Feeds the existing unassigned "flaky-test stabilize" item. Flaky-retry-masking test-honesty debt; a future test-stabilization wave should fix the async assertion.
findings: []
