# Wave 5 — T-4-integration
active-live: rate-limit middleware + version live-verified against prod; avatar server-2MB code shipped (live upload pending bucket creds). Migration n/a (no schema this wave).
