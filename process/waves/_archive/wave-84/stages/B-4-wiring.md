# Wave 84 — B-4 Wire
Self-wired: api Session.init (DI via AuthModule), web Session.init (SuperTokens.init at bootstrap), CSP via the Vite build-time plugin (in dist/index.html). No new routes. Repo-wide typecheck (api + web) exit 0.
