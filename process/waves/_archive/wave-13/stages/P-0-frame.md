# Wave 13 — P-0 Frame
- wave_db_id 9e3cf3bc (wave 13); M3 6198650e. claimed [e12886d7 edit/delete(seed), d78df376 reactions, f323a71f UI]. multi-spec. Completes the M3 message lifecycle, reusing wave-12 MessagingModule + /messaging gateway + ChannelMessageGuard (no new namespace/auth surface). design_gap_flag TRUE.
- **problem-framer PROCEED** (no antipatterns) — M3 scope's literal next clause (send/receive shipped → edit/delete, reactions); coherent bundle (same messages/message_reactions tables, same guard chain, same gateway, same shared Zod). Data model PRE-DESIGNED in _library: messages.is_edited/edited_at/is_deleted/deleted_at/content_snapshot (L153); message_reactions UNIQUE(message_id,user_id,emoji) (L142). **T-8 flags:** edit=author-only; delete=author OR moderator role-perm (manage_channels/manage_messages) — SERVER-SIDE via can()+authorship (never UI-only); reaction toggle idempotent via UNIQUE; fan-out (message.updated/deleted, reaction.added/removed) room-scoped (no-leak), TWO-CLIENT verify. Flags→P-2: soft-delete ROW-LEVEL only (no thread machinery — threads deferred); committed migration (no auto-migrate); aggregated reaction counts on list/get; cursor/idempotency unchanged.
- **ceo-reviewer PROCEED (HOLD-SCOPE)** — table-stakes lifecycle (reactions in M3 metric; edit/delete = can't-fix-typo reads-broken); right-sized (extends built infra; threads/mentions/attachments/presence deferred). M3-FIRST CORRECT: M4-offline has a HARD dependency on the message-lifecycle contract (M3 Required-by M4: outbox/idempotent-replay/reconcile sit on send/edit/delete) → shipping M4 against a churning contract = the most expensive surface to redo; completing M3 basics first DE-RISKS the wedge.
- mvp-thinner: SKIP (platform-foundation/success-metric floor).
- design_gap_flag: TRUE (inline edit, delete tombstone, reaction pills → D-block).
- **Framing:** messages +is_edited/edited_at/is_deleted/deleted_at(/content_snapshot) (soft-delete) + message_reactions(message_id,user_id,emoji UNIQUE) + edit (author-only) / delete (author||moderator) endpoints + reaction-toggle (idempotent) + realtime fan-out (message.updated/deleted, reaction.added/removed, room-only) via the EXISTING gateway + UI (inline-edit, tombstone, reaction-pills). Reuses ChannelMessageGuard. T-8: authz + two-client + wave-11 fixture.
