# Wave 38 — B-3 Frontend
**SKIPPED — backend-only wave.** Avatar upload/render UI shipped wave-4; `<img src={avatarUrl}>` follows the new 302 redirect transparently (avatar_url stays an opaque stable string). Zero frontend change.
