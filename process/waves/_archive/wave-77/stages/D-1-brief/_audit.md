# Wave 77 — D-1 Audit
1 gap: **cross-server member profile card** (new component from MemberListPanel rendering PublicProfile). The academic-identity EDITOR extends the shipped ProfilePage form (reuses existing settings-form DS → NOT a gap). Grep of design/ confirms no member-profile-card mockup. No prior bug-design tasks. Brief authored: member-profile-card-brief.md.
