# W36 T-8 Security (active) — PASS
This wave changes NO auth/session/authz surface — instrument.ts scrubPii is a behavior-identical extraction; the rest is test files. The privacy-enforcement boundary (profile-visibility roster-hiding + data-export IDOR + PII scrub) is UNCHANGED since wave-35 (where it was T-8-reproduced live) and remains live (C-2: /profile/privacy 401). What this wave ADDS is durable regression coverage of that boundary, and B-6 (head-builder + code-reviewer) verified the tests are honest (real SUT, real PG, session-scoping IDOR test at the controller, real scrubPii SUT not a replica). No new attack surface. `security_scope` flagged but no new auth code → no findings. PASS.
