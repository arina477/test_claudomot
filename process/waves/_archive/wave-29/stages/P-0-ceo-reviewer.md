verdict: RECONSIDER
verdict_source: ceo-reviewer
mode_applied: SCOPE-REDUCTION
mode_rationale: |
  SCOPE-REDUCTION, not HOLD-SCOPE: the task is real wave-14 correctness debt, but it is a
  displayName empty-guard + a *latent, currently-unused* schema-wrapper alignment — a real bug
  that does not matter yet (the ServerMembers wrapper is dead until the wire shape changes; the
  empty-displayName fallback is a narrow edge). Fix-cost is small but fix-value is near-zero at a
  0-user pre-launch product, so it fails the "worth doing NOW" bar. Not SCOPE-EXPANSION/SELECTIVE-EXPANSION
  because the wave is not timid on a valuable axis — it is on a low-value axis entirely; expanding a
  low-value cleanup only spends more on the wrong thing. Not a bare PROCEED because picking this row is
  the 8th consecutive instance of the exact failure mode the wave-27 escalation named. Not ESCALATE:
  the strategic conflict (park-or-key) is ALREADY with the founder (digest 2026-07-01, unanswered) —
  re-opening it would duplicate a live ask, which I am explicitly barred from doing.
bet_traced_to: "Academic tools + offline-first win students from Discord (live)"
milestone_traced_to: "M5 (a5232e16) — Academic tooling: assignments — ACTIVE (in_progress)"
proposed_scope_change: |
  DO NOT seed wave-29 with d23a0740 (displayName empty-guard + unused ServerMembers wrapper schema).
  It is the lowest-strategic-value row in the M5 debt queue: a latent (not active) contract trap plus a
  cosmetic empty-string edge, tracing to no user-visible outcome and no bet-load-bearing scope. Ship-cost
  > ship-value at self-use-mvp scale.

  Recommended NEXT-wave direction (does NOT front-run the founder's M5-park decision, which stays founder
  territory — this only re-orders WHICH credential-free work the loop does while that fork is pending):
  Prefer a founder-credential-FREE slice that actually advances the live bet over draining latent code-debt:
    1. HIGHEST LEVERAGE — M6 voice/video groundwork (LiveKit Cloud already decided, product-decisions
       2026-Q2; VoiceModule token-mint is server-side, needs NO founder credential). "Drop-in voice/video
       study rooms" is one of the three park-target milestones the founder was OFFERED in the digest, is
       directly on the displace-Discord bet, and clears the >1500-LOC feature floor cleanly (ending the
       floor-merge streak). Doing groundwork here is strictly better than a displayName guard whether the
       founder ultimately picks A (key) or B (park) — under B it becomes the active path; under A it is
       reusable H1 scope.
    2. If the loop must stay strictly inside re-homed debt, prefer a HIGHER-user-value M5/adjacency row
       over d23a0740 (e.g. a presence/messaging item a student would actually perceive). The
       displayName-empty-guard + unused-schema pair is the weakest available pick on user-perceivable value.

  Note the 8th-wave recurrence explicitly: waves 16/23/24/25/26/27/28 all floor-merged under-value M5-adjacent
  debt while M5's headline (reminders, cron+Resend) sat Resend-blocked. Wave-29 picking d23a0740 makes it the
  8th. The wave-27 escalation named "draining verifiable debt while the real milestone is blocked" as the
  de-facto strategy and the failure mode for a 0-user pre-launch product. The studio is now unambiguously
  inside that failure mode — this is a factual recurrence flag for head-product to carry into P-4, NOT a new
  founder ask (the park-or-key fork is already pending with the founder).
drop_rationale: |
  Not a full DROP — d23a0740 is genuine correctness debt worth doing EVENTUALLY (before external users hit
  the empty-displayName edge or the ServerMembers wire shape is consumed). It should stay tracked in the M5/M4
  debt queue, deferred, not cancelled. What is wrong is doing it NOW as the wave-29 seed — hence RECONSIDER
  (re-pick the seed), not DROP (discard the work).
escalation_reason: |
  n/a — NOT escalating. The only strategic conflict here (continue-draining-debt vs park-or-pivot M5) is the
  park-or-key fork already surfaced to the founder in digest 2026-07-01 and still unanswered. Re-opening it
  would duplicate a live founder ask, which the directive explicitly forbids. My verdict stays within
  next-wave-direction authority and flags the recurrence without duplicating the ask.
sibling_visible: false
