# Wave 68 — B-6 Verdict

**Reviewer:** head-builder (fresh spawn, B-6 Phase 1 gate)
**Reviewed against:** process/waves/wave-68/blocks/B/review-artifacts.md
**Attempt:** 1  (first gate)

## Verdict
APPROVED

## Rationale

All 8 acceptance criteria for task 2bd37c4c are satisfied in the diff, and the two load-bearing surfaces (the owner-authz gate and the memberCount fix) hold under direct code review plus the mocked unit tier (119/119 green, verified by re-run in this gate). The single caveat is an environment condition, not a code defect: the mandatory live-DB integration spec exists and is correct, but could not execute in this gate sandbox because the harness Postgres (localhost:5433) is not running here — a driver-level `ECONNREFUSED`, identical across all 19 integration files. That is an infra-readiness condition (always-on rule 13), not a rework trigger against the diff. It IS a hard handoff obligation to the C-block: CI must run `test:ci` against the provisioned harness DB and prove the wave-68 live-DB tests execute GREEN (not skip) — that is the exact guard the spec's memberCount-live-DB hard AC demands.

Per-judge findings:

1. **SECURITY / owner-authz (AC1, AC2) — PASS (load-bearing).** `updateServer` (servers.service.ts:451-490) loads the server (404 if missing, line 458-460), then gates `server.owner_id !== userId → ForbiddenException` (line 462-464) BEFORE building any SET or issuing the UPDATE. A non-owner is rejected 403 and the row is never touched. Two tests assert the "update never applied" property: the mocked unit test (servers.service.spec.ts:1650-1659) asserts `mockUpdate` is never called for a non-owner (verified green in this gate), and the real-Postgres integration test (test/integration/update-server-member-count.spec.ts:138-162) re-reads the row after the rejected PATCH and asserts is_public/description/topic are all unchanged. Server-side gate is authoritative. UI defense-in-depth confirmed: `ServerOverviewSettings.tsx:129` gates `isOwner = currentUserId === ownerId`, and the publish control renders only inside `{isOwner && (...)}` (line 420-472) — non-owners never see it; `handleSave` (line 199-201) omits `is_public` from the patch entirely when not owner.

2. **memberCount fix + live-DB test (AC8, AC9) — PASS.** `discoverServers` (servers.service.ts:598-664) rewrites the wave-67 always-0 correlated subquery to `count(server_members.user_id)::int` over a LEFT JOIN + GROUP BY. LEFT JOIN preserves 0-member public servers (0, not NULL/missing); GROUP BY keys on servers.id and all selected/ordered server columns. `is_public=true` filter (line 615), optional ILIKE across name/description/topic (line 618-624), stable ORDER BY memberCount DESC, name ASC, id ASC (line 651), and limit/offset with a defensive cap of 50 (line 603-604) are all preserved. The live-DB spec (integration/update-server-member-count.spec.ts:178-257) seeds three public servers with 0/1/2 real memberships via pg-harness (real Postgres, NOT a mock) and asserts memberCount == real count, the 0-member honest zero (typeof number, not null/NaN), presence of all three, and the DESC ordering. This is precisely the guard the mocked unit test lacked. (Non-execution here = ECONNREFUSED only; the spec is well-formed and runs under `test:ci`.)

3. **opt-in + unpublish (AC3) — PASS.** No schema change, no backfill (spec + review-artifacts confirm columns exist from wave-67 migration 0024; B-0 made no schema change). `is_public` is only written when `patch.is_public !== undefined` (service line 471); default false is untouched for servers nobody patches. Unpublish (is_public=false) writes false → row drops out of the `eq(servers.is_public, true)` discover filter. Partial PATCH is honoured: `'description' in patch` / `'topic' in patch` presence checks (line 472-473) mean a description-only patch leaves is_public alone (unit test 1671-1683 asserts set() has description but NOT is_public), and explicit null clears via `?? null` (unit test 1699+). Idempotent re-publish confirmed by the Zod-partial + straight UPDATE path.

4. **pre-populate / no-clobber (contract read-extension) — PASS.** The read contract was extended correctly: `ServerSummaryWithInviteSchema` (packages/shared/src/servers.ts:23-28) adds is_public/description/topic (non-optional but nullable), and the SOLE producer of `ServerDetail` — `findServerDetail` (servers.service.ts:193-202) — populates all three from the loaded server row, so no producer emits an off-contract shape. `ChannelSidebar` passes `selectedDetail.server.{is_public,description,topic}` into the settings surface as initial values (diff hunk at ChannelSidebar.tsx), and `ServerOverviewSettings` seeds field state from those props (line 139-141, reset scoped to serverId only at 149-156 so a background detail refresh does not clobber in-progress edits). Save sends only owner-derived fields; untouched fields are preserved by the partial-PATCH server logic. No other `ServerSummaryWithInvite` consumer breaks: the only other readers (sync/types.ts, sync/cache.ts, ServerContext.tsx, api.ts getServerDetail) consume the wider shape and are additive-compatible. Dexie: `cachedServerDetails` stores the whole ServerDetail as a blob keyed by `'id'` with no field indexes on the new columns (db.ts:193), so added object fields require NO Dexie version bump — no migration gap, no silent data loss.

5. **Reuse / scope discipline — PASS.** ServerOverviewSettings reuses the design/server-settings.html Overview shell, the ServerRolesPage settings-overlay chrome + isOwner pattern, and DS §8 toggle / form primitives; it does NOT touch the Roles permission matrix (ChannelSidebar adds a distinct `server-roles-btn` alongside the new Overview gear, preserving the Roles entry). No scope creep — no moderation, no ranking, no directory-side changes beyond the memberCount aggregation the spec folds in. Single-spec wave, claimed_task_ids [2bd37c4c]; commits are cleanly separated (backend / frontend / read-contract / pre-populate) and all cite the wave-68 task. B-5 local gate (typecheck 4/4, biome clean, web 596/596, api 764/764, both builds) is consistent with the servers unit re-run here (119/119).

## Handoff obligations to C-block (CI/CD)

- **HARD:** C-1 CI MUST run `pnpm --filter @studyhall/api run test:ci` (or `test:integration`) against the provisioned harness Postgres and confirm the wave-68 live-DB tests in `test/integration/update-server-member-count.spec.ts` execute and pass — NOT skip. The spec's memberCount-live-DB and non-owner-reject-row-unmodified assertions are hard ACs; a green run in CI is what closes AC9. (In this gate sandbox they were skipped only because localhost:5433 is unreachable — `ECONNREFUSED`, an infra condition.)
- Standard: Railway deploy is CLI-push per changed service at C-2 (`railway up`), no schema migration to sequence this wave (columns pre-exist).

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
