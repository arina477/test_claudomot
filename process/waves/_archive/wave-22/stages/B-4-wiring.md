# Wave 22 — B-4 Wiring
```yaml
typecheck_passed: true     # repo turbo
build_passed: true         # repo turbo
routes_registered: ["POST/GET /servers/:serverId/assignments", "GET/PATCH/DELETE /assignments/:id", "PUT /assignments/:id/status", "assignment attachment presign (AssignmentsModule in app.module)"]
design_token: "--danger-text promoted to DESIGN-SYSTEM §1 (D-block carry done)"
panel_entry: "ChannelSidebar Workspace category → Assignments → ServerContext openAssignments → MainColumn early-return"
drift_defects: []
```
