# BOARD vote — user-advocate — descope-who-can-dm-w35

## Vote
APPROVE Path A

## Rationale (≤150 words)
From P1's keyboard: gating who-can-DM against a non-existent feature (feature #21, H2-deferred) serves no student today. Path B delays the core study loop — messaging, voice (F3/F4), assignments (F6), and the offline wedge (F5) — multi-wave to satisfy one setting nobody can exercise yet. That is launch-delay inflation with zero user-perceivable value; it fails return-tomorrow.

Path A ENFORCES profile-visibility now (a real control with a real effect) and persists who-can-DM as a forward preference. Per the privacy-predictability dimension, a saved-and-honestly-labeled future setting is NOT privacy-theater — the failure mode is a control that silently no-ops. So the vote is conditional: the who-can-DM control must read unambiguously as not-yet-active (helper text "takes effect when direct messages arrive," muted/inactive styling per DESIGN-SYSTEM.md), never a live-looking toggle. Persisting the choice forward is trust-preserving (Google Docs offline-badge honesty). Amend the M7 metric to match.

## Hard-stop?
none

## Dissent note (only if APPROVE with concerns)
Labeling is load-bearing: if who-can-DM ships as a functional-looking toggle without a clear "not active yet" affordance, it becomes privacy-theater — that build would flip my vote to REJECT.
