# BOARD vote — counter-thinker — descope-who-can-dm-w35

## Vote
REJECT (the A-vs-B binary as framed; adopt the missing third path A′)

## Rationale (≤150 words)
The frame anchored the BOARD (strategist + industry-expert both APPROVE A, identical dissent) on persist-vs-build and skipped **A′: enforce profile-visibility, DROP who-can-DM from shipped UI AND the metric — no forward-persisted dead toggle — re-add as an AC on feature #21.** Inversion / pre-mortem: 12 months out, a labeled-but-inert "who can DM you" toggle sits on the product's *named privacy wedge* doing nothing while DMs slipped further right — privacy-theater precisely where the differentiation lives, worse than a clean descope. Path A's only gain over A′ is pre-building persistence for a consumer a full horizon away (feature #21, H2) — speculative-generality / YAGNI, sunk-cost-flavored. A′ dominates A on leanness and theater-avoidance, and B on scope. If founder intent is contact-governance broadly, the enforceable-today narrowing is "who can @mention me" (mentions are LIVE, fire realtime pings) — a real surface, not a dead one.

## Hard-stop?
none — reversible; metric amendment is spec-correction (metric references a verified-nonexistent feature), not silent bet-retirement. Both peers already require logging it. NOTE: amending a metric on the founder's named privacy wedge warrants a founder-visible line, not just a decisions-log entry.

## Dissent note (if A adopted over A′)
If the BOARD keeps A, enforce the honest UI label AND kill the toggle from *shipped* settings (persist server-side only) — a visible inert control is the theater; invisible forward-persistence is not.
