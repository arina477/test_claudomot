# P-0 — Frame (wave-57)
## Discover
wave_db_id b79d3365 (wave 57). Seed ff09c4c9 (wave-51 T-5 F-1, pre-existing UX papercut). Milestone M8 (in_progress), backfilled. Short-circuit no-prior-spec. No Tier-3. M9-Monetization strongly flagged for founder (non-pausing).
## Reframe
**Framing:** on the DM surface, ServerRail `selectServer` (ServerRail.tsx:237) + the Home button navigate but don't clear `dmHomeActive` (AppShell local useState, cleared only by the DM-rail `onDmHome` toggle at :55-58), so AppShell's ternary (~:118) keeps rendering `<DmHome/>` — first click swallowed, double-click needed to return to server/channel.
- **problem-framer — PROCEED.** Root cause VERIFIED in code (dmHomeActive local useState :37; only clearing path is onDmHome toggle; selectServer + Home change selectedId via ServerContext but never reset dmHomeActive). Cause-layer fix = add `setDmHomeActive(false)` to those handlers (or lift the reset into AppShell's select handler). Minimal targeted fix is right for a papercut. Deeper "derive dmHomeActive from route/selected-server" refactor considered but disproportionate (no route source-of-truth; DM-home ≠ selectedId==null since a last-selected server persists). Flagged the desync-prone boolean as an L-2 foot-gun candidate IF a 2nd desync bug appears. No antipattern.
- **ceo-reviewer — PROCEED / HOLD-SCOPE.** Genuine deterministic UX-correctness bug on a shipped LIVE high-frequency nav path; fix-value > fix-cost. Minimal targeted fix; NO larger nav-state refactor (gold-plating, no live-bet backing). M9-Monetization remains the founder-reserved high-value pending item; no new P-0 escalation.
- **mvp-thinner — OK.** Single-root-cause fix; both handlers (selectServer + Home) fail the same missing-reset → one coherent indivisible unit + one test. Nothing to split (fixing one path = half-fix). Not over-cut. Sub-floor → P-1 override by rule.

**Disposition: PROCEED (minimal targeted fix).** No reframe, no split.
**Final framing:** wave-57 adds `setDmHomeActive(false)` to BOTH the ServerRail `selectServer` and Home-button paths (cleanest: lift the reset into AppShell's server-select handler so both go through it), so navigating from the DM surface to a server/channel exits DmHome on the FIRST click. `claimed_task_ids=[ff09c4c9]`. design_gap_flag FALSE (state-transition correctness fix; no new UI surface/mockup). Frontend wave (B-3). + a component test asserting single-click DM→server exit. NO nav-state-model refactor.
