# Wave 9 — P-0 Frame
- wave_db_id 88aff17b (wave 9); M2 41e61975. claimed [863c10ef invite-revoke(seed), 5331b7d5 share-modal-permanent(8b), 08ff762f invite_code-backfill(8a)]. multi-spec (3, completion). Finishes the invites feature (wave-8 deferrals/drifts). RBAC = wave-10 (BOARD-bound).
- **problem-framer PROCEED** (no antipatterns) — coherent invite-lifecycle completion; revoke = missing WRITE verb (read path already 404s on revoked: servers.service.ts:38/244/374); 8a/8b real wave-8 V-1 drift fixes. Flags→P-1/P-2/T-8: (1) **revoke authz TODAY = servers.owner_id + invites.created_by** (RBAC "admin" deferred wave-10); server-side authorized. (2) revoked-link honesty — preview AND join both 404/410 (T-8 confirm). (3) 8a backfill idempotent + collision-safe (only WHERE NULL, CSPRNG, mirror the 23505 retry at servers.service.ts:203-233), committed migration not auto-migrate. (4) 8a→8b ORDERING — backfill before default-to-permanent (or 8b graceful fallback for NULL code). (5) design_gap_flag TRUE-delta (invite-share.html exists; revoke affordance + modal change = deltas). (6) scope guard: no RBAC/kick-ban/settings (watch "admin" not pulling RBAC early).
- **ceo-reviewer PROCEED (HOLD-SCOPE)** — BOARD already decided 5-1-1 (completion-before-expansion); right-sized (revoke endpoint+UI + 2 drift fixes, same subsystem); not gold-plated (no analytics/audit/expiry-UI); RBAC correctly wave-10-bound. Traces to the live bet (invites = Discord-parity acquisition surface).
- mvp-thinner: SKIP (platform-foundation).
- design_gap_flag: TRUE-delta (revoke affordance in the share/invite UI + share-modal-default change).
- **Framing:** invite-revoke endpoint (owner/creator-gated, server-side) + UI affordance + revoked→404 honesty; share modal defaults to servers.invite_code (8b); backfill servers.invite_code idempotent/collision-safe (8a, before 8b). NO RBAC/kick-ban/settings.
