# Wave 8 — P-0 Frame
- wave_db_id 44af7dae (wave 8); M2 41e61975. claimed [c7443638 invite-backend(seed), 77e2041a preview+join-API, 72fc08ea invite-join-page, 54407e1d invite-create/share-UI]. multi-spec (4, ~2800 LOC). Builds on wave-7 (servers/channels live). The JOIN half of M2's success metric.
- **problem-framer PROCEED** (no antipatterns) — milestone success-metric core; coherent vertical slice; framing matches code (servers has no invite_code yet; server_members UNIQUE(server_id,user_id) → idempotent re-join; role_id stays nullable, no RBAC leak; two-tier invite = _library #4 + SameSite=Lax #10/#17 enables link→login→redeem). **LOAD-BEARING flags → P-2/T-8:** (1) **invite-code entropy CRITICAL** — must be CSPRNG/unguessable/non-enumerable (invites are an access-control surface; spec only said "URL-safe+unique"). (2) public preview GET /invites/:code leaks server name+member-count → compounds entropy need; minimum-summary-only. (3) join requires a VERIFIED session (EmailVerification REQUIRED), not merely authed. (4) max_uses check-and-increment must be ATOMIC (concurrency). Sizing: ~2800 on larger side, not split (coherent); P-1's call (seam = backend c7443638+77e2041a vs frontend 72fc08ea+54407e1d).
- **ceo-reviewer PROCEED (HOLD-SCOPE)** — multi-user unlock; correct sequencing (invites BEFORE M3 messaging — messaging is meaningless single-user); two-tier invite right-sized (KEEP both — standard primitive, locked arch, revocability); RBAC/kick-ban/settings correctly deferred. NOTE: "see channels PER ROLE" completes with the deferred RBAC bundle — don't mark M2 metric fully-closed at this wave.
- mvp-thinner: SKIP (platform-foundation).
- **design_gap_flag: TRUE (DELTA)** — design/invite-join.html already exists (validate/compose); invite-create/share modal reuses Modal/Button/Toast primitives. D-block = compose-and-validate, not net-new.
- **Framing:** invite backend (two-tier: ad-hoc invites table + servers.invite_code, CSPRNG) + preview(public,minimal)+join(verified-session,atomic max_uses) API + invite-join page + invite-create/share UI. Owner/member only; NO RBAC/realtime.
