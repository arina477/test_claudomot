# P-4 Karen — wave-16 load-bearing-claim verification (create-server authed E2E)

**Verdict: APPROVE** (1 non-blocking note to fix before B-block: test-automator not in AGENTS.md; nearest catalog match = ui-comprehensive-tester).

The harness, UI-flow, and fixture claims the spec+plan rest on are grounded in real files. The key claimed-vs-real check — *can the authed create-server E2E even be written against the live app* — passes: the entire flow (sign-in → create-server modal → server rail → #general sidebar) exists and is server-seeded.

## Per-claim

| # | Claim | Status | Evidence |
|---|---|---|---|
| 1 | playwright.config.ts + smoke.spec.ts exist; setup-project/storageState pattern is addable (config has 1 chromium project, no setup) | **VERIFIED** | `apps/web/playwright.config.ts:19-24` — single `chromium` project, no `setup`, `retries: process.env.CI ? 1 : 0` (:11), `baseURL` from `E2E_BASE_URL` prod default (:14). `apps/web/e2e/smoke.spec.ts:1-35` — unauthenticated public-route pattern, `getByRole` selectors (:16,:32). Adding a `setup` project + `dependencies` + `storageState` is a standard config delta; nothing blocks it. |
| 2 | Verified fixtures exist on disk (gitignored) with email+password the E2E signs in as | **VERIFIED** | `command-center/testing/test-accounts.md` present (6429 bytes). `e2e-fixture`: email `studyhall-e2e-fixture@example.com` + password + **email-verified: Yes** + "Can create servers (EmailVerification REQUIRED claim = true)". Second fixture `-b` also present. Verified = can pass the AuthGuard+EmailVerification door the create-server endpoint requires. |
| 3 | **The create-server UI flow the test asserts EXISTS in the live app** (modal → rail → #general sidebar) | **VERIFIED (key check)** | All components real under `apps/web/src/shell/`: `CreateServerModal.tsx`, `ServerRail.tsx`, `ChannelSidebar.tsx`, composed in `AppShell.tsx:49,61` (rail + sidebar columns mounted; modal mounted when `createModalOpen`). Selectors are labelled+stable: ServerRail `aria-label="Server rail"` + `data-testid="server-rail"` (:101-102), each server icon `aria-label={server name}` (:9,:58) → rail-by-name assertion works. Modal `data-testid="create-server-modal"`, titled "Create a Server", labelled close/submit. **#general is server-seeded**, not UI-faked: `apps/api/src/servers/servers.service.ts:65,96` creates `'General'` category + `#general` text channel on every server create; ChannelSidebar renders channels from ServerContext. Flow is end-to-end real → the E2E is writable as specified. |
| 4 | CI `e2e` job exists + can receive fixture secrets via env (plan adds E2E_FIXTURE_* GitHub secrets + ci.yml env wiring) | **VERIFIED** | `.github/workflows/ci.yml:119-133` — `e2e` job exists: playwright install chromium (:131) + `playwright test` (:133), `env.E2E_BASE_URL` already set (:123). It does NOT yet pass `E2E_FIXTURE_EMAIL/PASSWORD` — but the plan owns that as an explicit step (P-3 step rows for `gh secret set` + ci.yml env). Claim is "addable", not "already present" — correct. |
| 5 | test-automator in command-center/AGENTS.md | **WRONG (non-blocking)** | `grep -ni "test-automator\|automator" command-center/AGENTS.md` → **no match.** AGENTS.md test agents are `head-tester` (:46, T-block gate, not a test author) and `ui-comprehensive-tester` (:68, "Playwright tester for live-site verification", T-5 e2e swarm). Per rule 11, the spawned author must exist in the capability sheet AND AGENTS.md, else substitute the closest catalog match + note the swap. **Closest match = `ui-comprehensive-tester`** (Playwright, live-site, exactly this job). Plan's "test-automator ... present" assertion is unverified against AGENTS.md. Non-blocking for the spec/plan APPROVE, but the B-block orchestrator MUST route to `ui-comprehensive-tester` (or install test-automator via agent-creator) and note the swap — do not spawn a non-catalog name. |

## Antipattern sweep (claim 6)

- **ACs falsifiable?** Yes. AC1 (test exists + asserts rail + #general), AC4 (unique name per run) are concrete and checkable. AC5 (green in CI + smoke untouched) is binary.
- **Anti-flake AC concrete?** Yes, and load-bearing. AC3 names web-first `expect(locator)`, **no arbitrary sleeps**, deterministic fixture state, **no retry-masking**. P-3 reinforces: `retries:1` is a "pre-existing safety net, not a flake licence — must pass first-attempt"; selectors prefer role/text. This is the right bar given the documented server-roles flake.
- **Gold-plating?** No — happy-path only; empty-name validation explicitly deferred (edge-cases). Cleanup-on-prod is best-effort/tolerated, not built. Scope is one E2E + the reusable authed harness. Floor-exempt tech-debt per P-1. Appropriately thin.
- **Fixture-password-via-CI-secret sound?** Yes. Password lives ONLY in gitignored test-accounts.md → it is NOT readable from the repo in CI → it MUST come from a GitHub Actions secret. Harness reads `E2E_FIXTURE_EMAIL/PASSWORD` from env; orchestrator sets the repo secret via `gh secret set` (rule-6 ops action, test-account creds we provisioned). Correct and the only sound approach. Also correctly gitignores `e2e/.auth/` (storageState holds a live session).

## Minor flags (non-blocking, for B-block)

- **Target is live prod** (`E2E_BASE_URL` default, real verified prod fixture). Each run creates a uniquely-named server that persists (DELETE not implemented on servers, per test-accounts.md note "DELETE not implemented ... server persists"). Unique-suffix naming keeps runs independent; accumulation is accepted. Sound, but the B-block should confirm the rail-by-name assertion targets the *just-created* unique name (not a substring colliding with prior runs' servers).
- **storageState staleness:** the setup project re-signs-in per run, so a stale session can't accumulate — good. Just ensure the smoke spec keeps its own no-storageState project so it stays genuinely unauthenticated (P-3 already calls this out).

## Bottom line

Spec ACs are verifiable and non-happy-state is handled; the plan's harness/fixture/CI claims map to real files; the create-server flow is real end-to-end so the E2E is writable exactly as specified. **APPROVE.** One correction the B-block must apply: the implementer agent is `ui-comprehensive-tester` (catalog), not `test-automator` (absent from AGENTS.md) — substitute + note the swap, or install per agent-creator before spawning.
