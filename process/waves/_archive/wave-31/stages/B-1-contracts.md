# Wave 31 — B-1 Contracts (SKIPPED)
No shared contract surface: the voice-token response is an inline `{token,url}` DTO on the voice controller; no `@studyhall/shared` type / Zod / OpenAPI. No fast-path (B-3 depends on B-2's endpoint + the adopted design → serialize).
```yaml
skipped: true
fast_path_approved: false
deviations: []
```
