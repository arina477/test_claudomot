# Wave 31 — B-6 Verdict

**Reviewer:** head-builder (fresh spawn, Phase 1)
**Reviewed against:** process/waves/wave-31/blocks/B/review-artifacts.md
**Attempt:** 1  (first gate)

## Verdict
APPROVED

## Rationale

The credential-issuing surface is sound and the client is faithful to the adopted design — the two things this gate exists to protect. **Token-mint security (the core):** `voice-token.service.ts` gates strictly in the spec-mandated order — channel-load → 404 (line 83), `type !== 'voice'` → 400 (line 88), `canViewChannelById` → 403 (line 93), creds-unset → 503 (line 103), then mint (line 108+). It uses the channel-id-only `canViewChannelById` (NOT the 2-param guard) as required; that method resolves `server_id` from the channel row and delegates to `canViewChannel(userId, server_id, channelId)` (rbac.service.ts:428-437), so a user can only mint for a voice channel in a server they belong to — cross-server voice is structurally impossible. Missing channel returns `false` (default-deny) and is distinguished from 403 by the explicit channel-load-first ordering, with unit tests proving RBAC is NOT reached on the 404/400 short-circuits (spec test lines 206-218). **Secret server-side:** grep confirms `livekit-server-sdk`/`AccessToken` appears in `apps/web` only inside comments and the anti-pattern test — never as an import; no `VITE_LIVEKIT_API*` secret exists; the secret never leaves the service and never enters the response `{ token, url }`. **Token correctness:** identity=userId (JWT `sub`), room=channelId, grants roomJoin/canPublish/canSubscribe, `ttl:'1h'`, `await at.toJwt()` (async, correctly awaited); the unit test decodes the real signed JWT and asserts `exp` is bounded (`> now` AND `<= now + 2h`), not a fixed timestamp. **ESM bridge:** tsconfig is `module: CommonJS`; the lazy cached `import('livekit-server-sdk')` is the correct bridge for the ESM-only SDK — api typecheck exits 0 and the happy-path test exercises the live dynamic import (not a mock), so it is verified-sound, not a fragile hack. **Client:** connect-on-demand (Join click; no auto-connect on mount — proven by test 113), `video={false}` (audio-first), disconnect on Leave (`handleLeave`) AND on unmount (teardown `useEffect` → `roomRef.current.disconnect()`), error state with classified human-readable copy that never leaks raw SDK errors, all 5 states from `design/voice-study-room.html`. The participant-count badge + alone/populated split are IN the adopted design (html lines 278/362/383), driven by LiveKit's own `useParticipants` — this is design fidelity, not occupancy scope creep (the keep-OUT "occupancy" is the persistent channel-list indicator). **Scope:** screen-share, presence rings, reconnection UI, grid, server-side occupancy all correctly absent. **Commit discipline (multi-spec):** 747b15c→Refs 1dd1f2ca (web only), 448d5fa→Refs d8a85de0 (api/voice only), dcbb778→B-0 shared dep-manifest install citing both IDs but touching only the two package.json files (permitted shared-B-0 carve-out); both claimed task_ids have implementation commits; no cross-spec logic commit. **Test honesty:** api tests decode a real JWT signed with a real secret (verified 14/14 pass locally); web tests prove states + wiring with LiveKit legitimately mocked (media plane excluded by design — verified 13/13). Live voice-connect is correctly deferred to T-5/C-2 (LIVEKIT creds unset; placeholder build).

Accepted-debt (non-blocking, does not affect verdict): the web anti-pattern test (voice-study-room.test.tsx:268-275) asserts `.toBeDefined()` on an always-defined query — a weak assertion. Its load-bearing guarantee is nonetheless real: had `VoiceStudyRoom` imported `livekit-server-sdk`, the module-graph import at the top of the test file would fail (package absent from web deps), erroring the whole suite. The grep independently confirms no such import. Optional hardening (e.g., a static import-scan assertion) is a candidate for a follow-up but is not required for this gate.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 2
