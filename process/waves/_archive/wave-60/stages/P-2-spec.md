# P-2 Spec — wave-60 (pointer)
Authoritative spec: tasks.description of 5bcbd27f (YAML head + --- + prose).
wave_type: single-spec; claimed_task_ids: [5bcbd27f]; design_gap_flag: false.
ACs: server rail → surface-900; picker modal card → surface-900; disabled-send → emerald@40%; all via var() token
consumption (cause-fix); dark palette + AA intact. Contracts: design tokens (DESIGN-SYSTEM.md / globals.css); no API/data.
