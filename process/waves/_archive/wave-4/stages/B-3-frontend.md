# Wave 4 — B-3 Frontend (react-specialist, d551154)
ProfilePage: username input (client-validate ^[a-z0-9_]{3,20}$; PATCH → 409 "username taken"/400 format/200); avatar (accept png/jpeg/webp, ≤2MB pre-check; presignAvatar → if 503 "not available yet" graceful → PUT to storage [no credentials] → confirmAvatar → refresh); accent-color picker (PATCH {accentColor}). ProfileContext (fetch GET /profile once, refresh). Shell: ChannelSidebar renders avatarUrl (initials fallback) + accentColor on presence/text; --user-accent CSS var set on load+select. api.ts: getProfile/patchProfile/presignAvatar/putAvatarToStorage/confirmAvatar. 37 web tests (26+11 new) + 26 api green. typecheck/build/lint clean.
