# Wave 41 — P-0 Frame

## Discover section
- **wave_db_id:** 6a583dad (wave_number 41)
- **Prior-work citation:** M8's first bundle (authored by milestone-decomposer this session, commit 05c4e88). Builds on shipped RBAC (`can(userId, serverId, permission)`, per-server `roles` rows with boolean permission columns), the assignments feature (already `can()`-gated on `manage_assignments`), and Socket.IO messaging (message-delete already fans out).
- **Roadmap milestone:** M8 (84e17739), in_progress, class=`product-feature`, H2. First slice. Founder-directed (Path-B resume → start educator tools).
- **Spec-contract short-circuit verdict:** `no-prior-spec` (decomposer wrote prose descriptions, no fenced YAML head). Full P-1..P-3.
- **Product-decision resolutions:** none Tier-3. **Carry:** M8 `## Success metric` is `_TBD by founder_` (ceo + mvp-thinner both flagged) — safe for THIS foundation slice; surface to founder for finalization before the next (discretionary) M8 bundle (scheduling / study-group / DMs / search compete on it).

## Reframe section
- **Original framing:** seed = "distinct educator ROLE TYPE + educator permission + RE-GATE assignments"; sibling = "light moderation (delete-any + timeout)".
- **problem-framer verdict:** REFRAME (file P-0-problem-framer.md; antipatterns #1/#2/#4). Verified vs shipped code: (1) **No role-TYPE concept in the RBAC** — a role is a per-server `roles` row with independent boolean permission columns (`packages/shared/src/rbac.ts`, `apps/api/src/db/schema/servers.ts`), single-role-per-member. "Educator" is NOT a new kind of role — it's a role whose flags include moderation. A "distinct educator role type" invents a taxonomy the model lacks (#4). (2) **Assignments are ALREADY `can()`-gated on `manage_assignments`** (assignments.service.ts assertOrganizer ~L56) — not an owner-hardcode; the "re-gate" delta is overstated (any role with the flag manages assignments today). Don't rewrite the working authz (#2). (3) The genuinely-absent capability is **moderation, not the role**: delete-any-message already exists gated on `manage_channels` with `message:deleted` socket fan-out shipped; **member timeout has ZERO substrate** — no `muted_until` on `server_members`, and the send path (channel-message.guard.ts) only checks channel visibility, never mute-state. Reframe: "add a moderation permission flag to the existing RBAC; educator = a role bundling it," NOT "new role type + re-gate assignments." Flag for P-1: **timeout is a new stateful mute mechanism (schema column + a new send-time gate) — much heavier than delete-any (reuses shipped can()+socket). P-1 owns the timeout-split decision.**
- **ceo-reviewer verdict:** PROCEED / HOLD-SCOPE (file P-0-ceo-review.md). Right first slice — it's the dependency ROOT of M8 (everything gates on the educator identity); right ambition (the role DOES something on ship via moderation, not an inert flag); scope held. Carry: `_TBD` metric safe for this slice, load-bearing before the mid-milestone discretionary slices → founder-checkpoint item.
- **mvp-thinner verdict:** OK / `flag_metric_undefined: true` (file P-0-mvp-thinner.md). Can't run thinness analysis without a metric (contract-barred from improvising) → no split. Recommends proceed un-thinned (foundation slice, under-thinning risk low); re-run mvp-thinner against a concretized metric before the next larger M8 bundle.
- **Mediation outcome (head-product):** ADOPT the problem-framer REFRAME (corrective — the WHAT is right, the model/nouns are corrected). No full re-spawn: ceo + mvp-thinner already weighed the corrected-scope substance; P-4's fresh Karen/jenny/head-product re-validate. ceo HOLD-SCOPE + mvp OK → scope held, un-thinned, BUT the timeout-heaviness is carried to P-1 as an explicit split candidate.
- **Sibling task IDs created:** none new (bundle already authored by decomposer).
- **Disposition:** REFRAMED (model corrected; scope held; timeout-split deferred to P-1).

### Final framing (rest of P-block uses this)
**Wave-41 = M8 foundation: a moderation permission on the existing roles RBAC + the educator role that bundles it + light moderation as its first power. (multi-spec: seed 6cf06f99 + sibling 6ddddc2d)**
1. **Seed — educator role via a moderation permission flag (NOT a new role type):** add a moderation permission (e.g. `moderate_members`) to the existing per-server `roles` boolean-permission model + `can()`; the "Educator/Facilitator" role = a role granting moderation (+ the existing `manage_assignments`, which ALREADY gates assignments — so educators managing assignments is achieved by granting that existing flag, NOT by rewriting assignment authz). Grant/revoke via the existing role-management UI (name/surface the educator-friendly grant). Backend authz tests (real PG): grantee passes moderation checks, non-grantee 403, grant/revoke round-trips.
2. **Sibling — light moderation:** delete-any-message (gate on the new moderation perm via `can()`, reuse the shipped `message:deleted` socket fan-out — cheap) + member timeout (NEW: `muted_until` on `server_members` + send-time mute enforcement, server-side expiry). Gated on the moderation perm; can't moderate an owner/admin above them. Minimal UI on message hover-actions + member context menu (visible only with the perm). Backend authz+behavior tests (real PG).
3. **P-1 timeout-split candidate:** if the multi-spec bundle exceeds the max rubric OR timeout's substrate (schema + new send-gate) makes it materially heavier, P-1 may split member-timeout into a later M8 bundle, shipping delete-any-message as the role's first power (still a coherent, non-inert slice). P-1 decides on the size rubric.
4. **Carry:** M8 `_TBD` success metric → founder-checkpoint before the next M8 bundle (non-blocking here).
