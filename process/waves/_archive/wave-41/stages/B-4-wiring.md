# Wave 41 — B-4 Wiring
- RbacModule registers ModerationController + ModerationService. Moderation routes live. Frontend wired to POST/DELETE timeout + getMyPermissions + listServerMembers(mutedUntil). End-to-end typecheck clean (web 0, api 0, shared build 0).
