verdict: PROCEED
verdict_source: ceo-reviewer
mode_applied: HOLD-SCOPE
mode_rationale: |
  This is the exactly-right foundational slice for the first M8 wave, so no scope
  change is warranted (rules out SCOPE-EXPANSION / SELECTIVE-EXPANSION / SCOPE-REDUCTION).
  Not expansion: the deferred M8 items (assignment collect/return, scheduling, study-group
  tools, DMs, search) are correctly held back, and the strongest expansion candidate —
  assignment collect/return, the closest-to-the-wedge alternative — actually DEPENDS on
  this seed (it needs an educator identity to own it), so pulling it forward would both
  bloat the bundle and incur rework. Not reduction: dropping the moderation sibling would
  ship the inert-flag 3/10 this slice explicitly avoids; the sibling makes the role DO
  something on ship and proves the RBAC wiring end-to-end. The bar here is execution
  quality on a clean, minimal, dependency-root slice.
bet_traced_to: "Academic tools + offline-first win students from Discord" (status='live')
milestone_traced_to: 84e17739-af5e-4396-beb9-b6f3d6836fc4 — M8 Educator tools & deeper academics (H2, product-feature, T5)
proposed_scope_change: |
  None. Scope held as authored.
escalation_reason: |
  n/a
carry_flag: |
  M8's `## Success metric` is `_TBD by founder_`. SAFE to build this slice regardless —
  educator role + RBAC foundation + light moderation is required under any plausible M8
  metric (no metric definition makes "name an educator + moderate" wasted work). BUT the
  metric becomes load-bearing before M8's mid-milestone DISCRETIONARY slices, where scope
  choices diverge and compete for priority (class scheduling vs. study-group tools vs.
  DMs vs. search — the metric is what arbitrates that ordering). Flag the metric as a
  founder-checkpoint item to resolve before those slices, not before this one.
sibling_visible: false
