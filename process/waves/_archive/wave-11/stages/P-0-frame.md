# Wave 11 — P-0 Frame
- wave_db_id 7605792a (wave 11); M3 6198650e (just promoted). claimed [4a2ad286 verified-prod-fixture]. single-task test-infra. design_gap_flag FALSE. De-risks M3 (entirely authed/session-gated) live-verification.
- **problem-framer PROCEED** (no antipatterns) — right cause-layer (no-persistent-fixture is the cause of the 4-wave ad-hoc-verify pain). BINDING: password ONLY in gitignored command-center/testing/test-accounts.md (verified gitignored); project.yaml test_users = labels+emails ONLY (doctor fails on secrets). Persistence: record SuperTokens user-id + DB row-id (re-verify not re-create). Lean-minimal (one fixture + record + tiny re-verify snippet; NO fixture framework). No founder dep (admin-API verify autonomous; Resend not needed).
- **ceo-reviewer PROCEED (HOLD-SCOPE)** — worth it now (de-risk concentrated + large; 4 waves already failed verification); right-sized (matches v13 test-account precedent; no persona-matrix/auto-rotation); fixture-before-messaging correct ("build when needed" = never-built 4x). Watch: gitignored creds, CSPRNG password, prod-target, idempotent. T-8 flagged (user creation + sessions).
- mvp-thinner: no-op (test-infra, no product ACs).
- **Framing:** provision a persistent verified prod test user (signup via live api → verify email via SuperTokens core admin-API → record email + CSPRNG password + user-id in gitignored test-accounts.md) + a tiny re-verify snippet. Enables repeatable authed C-2/T-8 verification for all M3+ waves.
