# Wave 3 — T-5-e2e
active-partial: live core auth flow curl-verified end-to-end at C-2 (signup/login/session/me/profile). Full browser click-through (verify-link, form interactions) deferred — Playwright MCP lacks the 'chrome' channel binary (CI chromium job c51589cd is the path); RTL 17 page tests cover render+form presence.
