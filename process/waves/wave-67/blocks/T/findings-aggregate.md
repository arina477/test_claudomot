# Wave-67 T findings aggregate
CI: web 583 + api 752 (discover + join-public private-reject security assertion). Deploy: both services SUCCESS @43d20b2, migration 0024 live. T-5/T-6/T-8 live PASS (browse+join E2E via published-then-torn-down fixture; §8 Join 7.4:1; private join-public→403). Journey regen dfe35a1.
findings:
  - {id: F67-T5-1, severity: significant, source: T-5-e2e, file: apps/api/src/servers/servers.service.ts, summary: "GET /servers/discover returns memberCount:0 for EVERY server (observed 0 with 1 AND 2 members; raw DB count correct). The memberCount correlated subquery in discoverServers is broken — always 0. Mocked unit tests (752) passed because they mock the DB; the live probe caught the real SQL returning 0. User-visible: every discovery card shows '0 members'. Fix the correlated subquery (likely a Drizzle sql-binding / correlation issue) so memberCount reflects the real server_members count."}
  - {id: F67-T5-2, severity: low, source: T-5-e2e, file: apps/api/src/servers/servers.service.ts (joinPublicServer), summary: "join-public inserts the new member's server_members row with role_id=NULL (owner has a default role). Confirm role-less-member RBAC intent — a public joiner may need a default member role assigned, else RBAC checks keyed on role_id may misbehave. Investigate."}
