# Wave 13 — B Backend (edit/delete + reactions)
- B-0 schema (7c8590d): messages +is_edited/edited_at/is_deleted/deleted_at; message_reactions (UNIQUE(message_id,user_id,emoji)+INDEX). migration 0006 (postgres-pro; name-override comment).
- B-1 shared (44a29e6): MessageResponse +isEdited/editedAt/isDeleted/reactions[{emoji,count,reactedByMe}]; EditMessage; ReactionToggle.
- e12886d7+d78df376 (d1dd407, both task ids cited — tightly-coupled MessagingModule): editMessage (author-only 403; can't-edit-deleted 409); deleteMessage (author OR can(serverId,manage_channels) — serverId RESOLVED from channels.server_id before can(); soft-delete is_deleted+content cleared→tombstone null; idempotent). toggleReaction (idempotent INSERT/DELETE on UNIQUE; emit reaction.added/removed). listMessages aggregates reactions + reactedByMe (viewerUserId). controller PATCH/DELETE/POST-reactions (AuthGuard+ChannelMessageGuard, session userId). gateway @OnEvent message.updated/deleted + reaction.added/removed → server.to('channel:id') ROOM-ONLY. 219 api tests (28 service + 4 gateway).
