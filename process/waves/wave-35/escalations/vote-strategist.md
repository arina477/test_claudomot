# BOARD vote — strategist — descope-who-can-dm-w35

## Vote
APPROVE Path A

## Rationale (≤150 words)
Live bet (`founder_bets`): H1 = "desktop MVP (group servers, text channels, real-time messaging) usable by one class cohort"; privacy is a named wedge ("privacy-first vs Discord's ad-data posture"). DMs are deliberately feature #21, H2-deferred (`feature-list.md:43`). Path A holds the wedge: profile-visibility enforcement delivers real privacy-wedge value now against a real target, and M7 stays last-H1 launch-polish — advancing the deploy-verified-cohort trajectory that finally confronts the falsifier ("students keep preferring Discord"). Path B pulls an H2 comms subsystem into the last H1 milestone: a premature horizon-jump that (a) chases Discord-parity comms rather than the academic/offline/privacy wedge, (b) turns "launch polish" into a multi-wave build, delaying the switch-behavior signal. Path A's honest-labeled forward-persisted preference avoids strategy-by-demo (no fake enforcement). Sequencing discipline (differentiator-first, retention-before-parity) favors A decisively.

## Hard-stop?
none

## Dissent note
Amend the M7 metric in the milestone row AND log to `product-decisions.md` so the who-can-DM→feature-#21 dependency is recorded, not silently dropped.
