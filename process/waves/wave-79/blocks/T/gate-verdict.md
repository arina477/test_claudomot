# Wave 79 — T-9 Verdict

**Reviewer:** head-tester (fresh spawn)
**Reviewed against:** process/waves/wave-79/blocks/T/review-artifacts.md + findings-aggregate.md (+ source spot-check of the crown-jewel integration spec, the shipped indicator, C-1 CI evidence, and the canonical DB spec for tasks 60bda5be / 491cb85d / 3fb88f44)
**Attempt:** 1  (first gate)

## Verdict
APPROVED

## Rationale
The suite is honest and its load-bearing security claims survive source-level scrutiny — this is not coverage theater, mock-the-SUT, or a fabricated server-blind PASS. (1) SERVER-BLIND is genuinely proven, not asserted: the crown-jewel `dm-encryption.integration.spec.ts` imports `pg-harness` first (CF-2), runs on real postgres:16 (CI run 28912467863 confirmed EXECUTED by C-1's coverage-theater guard, merge 0fa0f5f), and reads the persisted row over a SEPARATE connection — `SELECT content, ciphertext FROM dm_messages` with `expect(rows[0]?.content).toBeNull()` plus a full-table scan `count(*) WHERE content IS NOT NULL` — proving server-side truth, not just a DTO. This is corroborated by a live prod round-trip (GET messages returns `content:""` + ciphertext; the plaintext probe `E2E-PROBE-SECRET-K7Qx9M` never appears on the wire or in any server response). The app-DB direct-SELECT limitation (brain host cannot reach the StudyHall app DB) is honestly disclosed and compensated by these two independent evidence lines — adequate. (2) The no-oracle 404 is real in source: cases A/B/C/D `toStrictEqual` against the REAL `ProfileController.getEncryptionKey`, byte-identical across who_can_dm=nobody / not-shared / nonexistent / no-key, plus live malformed-non-uuid → 404 (not 500) and `not.toHaveProperty('email')`. This is stronger than a 200-vs-403 IDOR pair — it removes the distinguishing oracle entirely. (3) Indicator honesty fails closed structurally, not just in prose: `data-testid="e2e-lock-affordance"` lives inside `case 'shield-check'`, and `shield-check` is the icon ONLY for the `encrypted` state (`isLock:true`); every other state (`not-encrypted-plaintext`, `not-encrypted-group`, `cannot-decrypt`, `loading`, `error`) is `isLock:false` with a non-lock glyph — verified live (lock count=1 encrypted / 0 on 29 plaintext / 0 on server channel). (4) No-private-key-leak is architecturally enforced: non-extractable CryptoKey, `exportKey('pkcs8')` throws InvalidAccessError, 0 wire markers, PUT body public-only. (5) T-5 realtime is genuine two-client (A sends, B decrypts a distinct identity — not a self-echo). Mutation sanity holds on every load-bearing test (drop the NULL-content write → `toBeNull` fails; render a lock on plaintext → testid-absent assertion fails; drift the 404 body → `toStrictEqual` fails). The three carried findings — F-T5-1 (medium, auth-guard race on DM route), F-T8-1 (info, group/plaintext-fallback indicator honesty not live-constructible this wave, verified structurally + CI + DOM + server-channel negative control, deferred per P-4 correction 4), F-T8-2 (low, server does not validate senderKeyRef vs registered key, low-risk under the server-blind model) — are correctly SURFACED to V-2, not silenced; none is a critical journey regression and none blocks. The group-indicator gap is an acceptable, documented boundary (P-4 correction 4 puts group E2E out of leg-3a scope), not a REWORK: the honesty rule is proven correct on every state the wave can construct plus structurally on the state it cannot. T-7 SKIP is legitimately justified (client-side native Web-Crypto, bounded, single-row endpoints, server never decrypts). Every applicable T-9 stage-exit check ticks.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
