# Wave 18 — P-0 Frame

## Discover
- **wave_db_id:** 1da5ff68-d3bb-41aa-b414-abc205248433 (wave_number 18)
- **Prior-work:** built on the shipped send/list REST data plane (a0c322b4) + /messaging WS gateway (723b5b6a) + the full M3 core (edit/delete/reactions/presence/mentions, all LIVE). thread_parent_id self-FK already declared in the messages schema (migration additive).
- **Roadmap milestone:** M3 (6198650e) in_progress; wave-18 backfilled. BOARD-endorsed (wave-17 N-1 7/7 → feature-first, threads-first).
- **Spec-contract short-circuit:** no-prior-spec (decomposer prose) → full P-1..P-3.
- **Product decisions:** none new Tier-3. Threads reuses /messaging (no new namespace/auth surface). Additive schema 0008. The BOARD ordering decision (7/7 B) is the governing scope call.

## Reframe
- **problem-framer: PROCEED** — net-new primitive at right layer; one-level threads = correct Slack/Discord MVP (nesting = gold-plating); reply_count/last_reply_at denormalized-on-parent (transactional) = correct read-optimized choice vs N+1 count-on-read; edge cases enumerated (reject reply-of-reply, cross-channel parent, idempotency); coherent slice (no split); thread-following/notifications correctly OUT.
- **ceo-reviewer: PROCEED / HOLD-SCOPE** — literal M3 ## Scope + success-metric requirement; BOARD-authored; coherent 8-9/10 slice (data plane + affordance + panel + outbox parity = whole); nested/notifications/unread-in-thread correctly OUT (unread-in-thread needs its own read-state plane = separate slice); outbox parity = the explicit M3→M4 handoff.
- **mvp-thinner: THIN** — proposed splitting 0b728319 (outbox parity) on the grounds its rationale is FORWARD-looking (M4 consistency) not threads-usability (threads "work" via the seed's idempotency+realtime + the panel's optimistic append). Flagged floor tension: cutting ~500-700 LOC → ~2100-2300 LOC / 2 specs = BELOW the 2500 multi-spec floor; preferred resolution pull an adjacent sibling, fallback keep under §2b override.
- **Mediation (mvp-thinner THIN vs problem-framer+ceo-reviewer keep):** REJECT the split — keep all 3 tasks. Rationale: (1) problem-framer + ceo-reviewer both frame the 3-task slice as COHERENT — outbox-parity is the deliberate M3→M4 send-path handoff (the seed's own decomposition placed it in this slice), not while-we're-in-there bundling; (2) M4 (offline-first) explicitly builds on this send-path — deferring outbox-parity plants a known gap M4 would immediately hit; (3) keeping all 3 keeps the wave cleanly ABOVE the multi-spec floor (~2800 LOC) with no RESCOPE gymnastics, vs the THIN-split's below-floor outcome. mvp-thinner is technically right that outbox-parity isn't strictly needed for "threads work THIS wave," but the coherent-slice + M4-handoff + floor considerations win. (Precedence: ceo-reviewer/problem-framer coherence over mvp-thinner thinness here — M3 mvp-critical scope is best served by shipping the coherent slice the BOARD authored.)
- **Disposition: PROCEED (all 3 tasks; no split).** design_gap likely TRUE (thread-view panel + affordance = new UI surface; server-channel-view.html has no thread markup) → D-block expected.
- **Final framing:** Wave-18 ships M3 threads: thread-reply data plane (thread_parent_id one-level, transactional reply_count/last_reply_at, thread-scoped realtime, idempotency) + thread-view panel & in-list affordance + thread-reply outbox parity. claimed_task_ids = [497c2ae6, 6c008dd6, 0b728319].
