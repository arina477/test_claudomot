# P-0 — Frame (wave-52)

## Discover section
- **wave_db_id:** 1638611a-87ac-41e9-936a-cdf6642d3605 (wave_number 52, running, milestone M8)
- **Prior-work:** wave-49 shared study timer + wave-50 custom durations (the substrate this reuses); the focus-room bundle was authored by milestone-decomposition at wave-51 N-1 (commit d2bd9d0) as the founder-directed study-group headline pivot (ceo-reviewer 2x-recommended, wave-50 + wave-51 P-0).
- **Roadmap milestone:** M8 (in_progress). Bundle pre-assigned M8; waves.milestone_id set at open.
- **Spec-contract short-circuit:** no-prior-spec (prose seeds) → full P-1..P-3.
- **Product-decision resolutions:** none Tier-3 (presence-only, reuses shipped substrate; voice/LiveKit deferred = the one money/infra decision, correctly out of slice-1).

## Reframe section
**Original framing:** M8 study-group slice — joinable focus room (body-doubling). 3-task bundle: d123d9e0 (backend join-presence) + aad849ac (UI) + ef84b378 (room-scoped timer). ~2200 LOC.

**problem-framer (a6bf7eea) — PROCEED (no antipatterns):** symptom-vs-cause PASSES — the explicit-JOIN + per-room roster + room-owned timer is a genuine user-value delta over wave-49's ambient "who's viewing" roster ("I'm focusing NOW, with THESE people, on OUR Pomodoro"), addressed at the right layer (reuse Socket.IO server-room substrate + a DISTINCT study-room:presence namespace). Voice/persistence correctly deferred (not gold-plating); no "rooms framework" premature abstraction; reuses assertMember/IDOR + the wave-49 anchors/compute-on-read/one-shot-setTimeout model (no per-room loop). **3 spec MUST-LOCKS (sharpen framing, not defects):** (1) **ephemeral room identity** — "create/name a room" = in-memory descriptor, NOT a persisted focus_rooms table; P-2 must state this so B doesn't add a table. (2) **room-vs-server presence boundary** — the new study-room:presence Map/namespace must NOT share a Map/room-name/wire-event with wave-49's timerPresence (the one wrong-layer risk, guard at B-6). (3) **room-timer state-location (the real trap)** — ephemeral rooms have no persisted row, so room-timer anchors live in-memory keyed by roomId (distinct from the service's serverId-keyed map); do NOT reuse the server_study_timer table; reuse ONLY the pure compute-on-read formulas. ef84b378 split = P-1 sizing call (RESCOPE-AUTO-SPLIT deferred to P-1). Rule-4 spirit: a user can arrive at a focus room already in the server-timer roster from another surface → keep the two presences independent.

**ceo-reviewer (adbfeafa) — PROCEED / HOLD-SCOPE:** this IS the headline it recommended in wave-50 + wave-51 (pivot done). Slice-1 is NOT presence-only — it's presence + a per-room synchronized timer (ef84b378), the load-bearing Discord-differentiator ("join a room where everyone runs the same Pomodoro + see who's focusing" = synchronized body-doubling Discord lacks; that's the 7+ vs the 3/10 of a Discord-parity voiceless room). Deferring voice/LiveKit deliberate + correct (voice is Discord's turf + XL/costly + the bet is pre-validation → ship the presence+timer loop first as the cheap test; voice = slice-2 gated on evidence). 3-task bundle right-sized (not bigger — voice/whiteboard/persistence each standalone; not smaller). Traces: live bet (academic-tools + offline-first displace Discord) → M8 ## Scope (study sessions).

**mvp-thinner (abf325e9) — OK (floor_constraint_active):** 3-task bundle intact, no split — the decomposer's ef84b378 split was conditional on breaching the CEILING, but the wave runs UNDER the multi-spec floor (~2200 LOC / 3 tasks < 2500 / 6); splitting ef84b378 (~700-900 LOC) leaves residual (~1300-1500 / 2 tasks) BELOW both multi- and single-spec floors → refuse. join+roster alone is passive/weaker (the timer makes it an active co-working session — coherent, not thinner-but-complete). Scope-fence clean (voice/persisted-attendance/scheduled-rooms/multi-room-moderation/whiteboard all deferred, none leaked); no intra-task peel. **Flags:** (a) success-metric caveat — M8 ## Success metric (teacher + DMs) is done; the bundle traces to ## Scope "study sessions", so it's scope-legitimate but success-metric-OPTIONAL (kept per founder ## Scope authorization + "when in doubt keep"); (b) cosmetic slice-numbering (seeds say "slice 2", manifest "slice 3" — reconcile at P-1).

**Mediation:** none (no expansion proposed; mvp OK).

**Disposition: PROCEED.**

**Final framing (rest of P-block uses this):** the **joinable focus room** (body-doubling co-working) — within a server, a member creates/names an EPHEMERAL (in-memory, NO persisted table) focus room and explicitly JOINS it; the server exposes open focus rooms + a per-room ephemeral roster (who's focusing together) over a DISTINCT `study-room:presence` Socket.IO namespace (separate Map/events from wave-49 `timerPresence`); a focus room carries its own room-scoped synchronized Pomodoro (in-memory anchors keyed by roomId, reuse ONLY the wave-49/50 pure compute-on-read formulas + one-shot idempotent auto-advance, NOT the server_study_timer table). UI: open-rooms list + "N focusing" + create + live roster + leave; dark-theme/design-system, responsive incl. <1024 slim; NO voice/video. Membership-gated (assertMember/IDOR, serverId+roomId from client, userId from session). **3 MUST-LOCKS** (ephemeral identity / room-vs-server presence separation / room-timer in-memory state) carried to P-2/P-3/B. **Deferred:** voice/LiveKit (slice-2), persisted attendance/history, scheduled rooms, multi-room moderation, whiteboard. claimed_task_ids = [d123d9e0, aad849ac, ef84b378]. **Slice numbering:** call it the study-group "joinable focus room" slice (reconcile the seed "slice 2" vs manifest "slice 3" at P-1 — cosmetic; it's the 3rd study-group BUILD after timer + durations).
