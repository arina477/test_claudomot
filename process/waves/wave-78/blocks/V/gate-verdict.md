# Wave 78 — V-3 Verdict

**Reviewer:** head-verifier (fresh spawn, agentId head-verifier-wave78-v3-phase1)
**Reviewed against:** process/waves/wave-78/blocks/V/review-artifacts.md
**Attempt:** 1  (1 = first gate)

## Verdict
APPROVED

## Rationale

Both V-1 reviewers are sound and evidence-backed, not acceptance-by-assertion, and I independently re-proved the load-bearing claims rather than trust their summaries. **Karen (APPROVE, 6/6):** her squash-merge stale-hash conclusion is correct — I confirmed `git merge-base --is-ancestor 1fca71a 855e811` returns FALSE (expected: PR #97 squash-merged, so per-commit hashes don't survive as ancestors), while the fix CONTENT is verifiably in the merge tree. I re-read `855e811:MemberProfileCard.tsx` lines 205-225 directly: the branch is the fail-closed allowlist form (`!(err instanceof HttpError) || err.status >= 500 → error`, else → `hidden`), and `grep '!== 404'` returns NONE — the old fail-open form is genuinely gone. I re-verified the service gate (`if (fields.academicRole !== undefined) { patch.academic_role = ... }` with `academic_role: string | null`) and the contract (`z.preprocess('' → null, z.enum().nullable().optional())` on the WRITE schema only; both read schemas remain plain `.nullable()`, untouched) — all match Karen's quotes exactly. **jenny (APPROVE, 0 drift, 3 LOW):** her spec-conformance axis is independent and her 8 ACs are backed by live API probes + live-bundle string inspection, not assertion. The load-bearing anti-oracle IS proven, not assumed: jenny probed the 404 path from an authenticated fixture-A session and got a 68-byte body byte-identical across nonexistent-UUID / malformed-non-UUID / second-nonexistent (diff-clean), plus the client branch collapses every non-5xx/non-404 status to `hidden`. My own anonymous probe returns a uniform 401 at the auth boundary (the 404 path is auth-gated, so anonymous can't reach it — jenny's authenticated context was the correct one), corroborating uniform-response discipline; and the true anti-oracle is client-side, which I confirmed in-tree. The transport→retryable half is proven via the live minified bundle branch (`status>=500 → error`). **V-2 triage** is correct: 6 findings → 4 distinct after honest dedup, 0 blocking, 1 non-blocking (server-delete gap, task d075cfed, wave_id NULL → seedable per the N-2 memory rule), 4 legitimately noise. None of the four noise suppressions is load-bearing: (1) displayName residue is test-fixture self-declared data, not app behavior; (2) playwright shared-profile is already-filed infra (wave-77 cda38633), correctly deduped not re-filed; (4) cross-viewer 404 not re-probed is mitigated by proven server byte-identity + wave-77/78 T-8 cross-viewer proof; (5) J-3 spec-under-spec is correctly non-blocking — the code is a SAFE SUPERSET (routes 401/403/410/429 → hidden, fail-closed) of the spec's narrower "404→hidden, 5xx→retryable" wording, i.e. safer than spec, a hardening not a drift. A spec-gap where the implementation is stricter than the contract is never blocking (nothing shipped violates the acceptance criteria; the ACs are a subset of shipped safe behavior), and jenny correctly classified it GAP-not-DRIFT. No reviewer false-negative, no green-by-suppression, no suppressed-but-blocking finding detected. Fast-fix queue is empty; Phase 2 skips.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
