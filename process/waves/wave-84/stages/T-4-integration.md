# Wave 84 — T-4 Integration
```yaml
test_pattern: active
evidence: ["csp.test.ts (21) — CSP policy shape incl. all origins, build-fail guard, DSN parse", "supertokens config specs — getTokenTransferMethod header (api) + tokenTransferMethod header (web)", "C-2 live: web CSP meta includes api+storage+livekit; api at 5cb5e789"]
findings: []
```
