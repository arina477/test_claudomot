# Wave 46 — T-9 Verdict

**Reviewer:** head-tester (fresh spawn, agentId head-tester-t9-w46)
**Reviewed against:** process/waves/wave-46/blocks/T/review-artifacts.md + findings-aggregate.md (+ independent source spot-check of the load-bearing F4/F6 root causes and the T-8 socket-auth correction)
**Attempt:** 1  (first gate)

## Verdict
APPROVED

## Rationale
The T-block honestly proves the DM slice-1 feature works and its suite is not falsely green — it caught the real bugs rather than hiding them. Coverage is solid where it matters: T-1's bypass grep is sound (the only `as any`/`as unknown as` casts live in `dm.service.spec.ts` at the mock/DI boundary; production `dm.service.ts`/`dm.controller.ts`/`dm-participant.guard.ts`/`shared/dm.ts`/`useDm.ts` are bypass-clean), both static CI jobs green on merge SHA 2a738f7. T-2 unit coverage is transition-table-shaped with user-observable assertions — who_can_dm across all three policies asserts the typed `ForbiddenException` AND no-partial-write, IDOR asserts `NotFoundException` + no-insert, idempotency asserts same-row + no-re-fan-out, find-or-create asserts the tx-vs-dedup branch by call count — no mock-call-count-only theater, and it correctly does NOT claim two-client realtime at the unit level (service asserts the internal `dm.message` emit; the gateway spec asserts the wire `dm:message` fan-out). T-3 gives every Zod branch a valid + typed-400 invalid case with the exact field error, and honestly defers `dm:message` to T-5 (curl cannot drive Socket.IO) rather than faking it. T-4 runs against real Postgres (deployed api fronting prod PG with migration 0021) and proves find-or-create/idempotency-UNIQUE/who_can_dm/IDOR at the DB boundary; its HIGH F4 cursor defect is genuine and correctly root-caused — I independently confirmed at source that `encodeCursor` truncates to `.toISOString()` millisecond precision (dm.service.ts:58-59) while the exclusive predicate compares `created_at > $ms` (L597) against a µs `timestamptz`, so the boundary row re-appears; the operator is indeed strictly `>`, and the mock structurally could not catch it — exactly the class the deferred real-PG integration existed to find. T-5's headline is a genuine two-client test: a separate-process/separate-profile second client captured a real on-wire `dm:message` Socket.IO frame at the receiver plus the DOM update with no reload on both runs — not a single client's own echo; the one honest limitation (the B→A reply direction has DOM-only evidence because A negotiated HTTP long-polling) is disclosed and does not weaken the fully-framed A→B headline. F6 (self double-render) is source-confirmed by me at useDm.ts:205 — the dedup checks only `m.kind==='real' && m.id===message.id`, so the M1 sender-fan-out echo appends a second row while the optimistic row is still `kind:'optimistic'` (the code comment's "confirmation arrives via REST, not socket" assumption was invalidated by the M1 fix). T-6's dark-theme token audit uses getComputedStyle with real diffs and names off-token surfaces as adjacent (on-palette) substitutions, no invented hex. T-7's SKIP is evidence-backed (zero package.json delta, fully-indexed + limit-bounded query layer, query shapes mirror already-perf-verified channel messaging), not a skip-on-assertion. T-8, the load-bearing private-messaging layer, holds under ACTIVE exploitation with the unauthorized actor asserted to 404 (A2/A3 IDOR), body-param smuggling and caller-spoofing actively attempted and rejected, injection parameterized-safe, and secret-grep clean; the pen-tester's socket MEDIUM was legitimately source-corrected to LOW — I verified the `/messaging` namespace (messaging.gateway.ts:63) installs `installWsAuthMiddleware` (L90) which rejects unauth WS upgrades via `next(new Error('Unauthorized'))` (ws-auth.ts:66/91) before handleConnection, and the residual is idle anon connects on the traffic-free default `/` namespace with zero DM data. Nothing is thin or false-green. The HIGH/MAJOR findings (F4, F6, F7) are user-visible defects on a live feature but each is a bounded correctness/render bug with a confirmed root cause and a routed fix — none exceeds the wave's risk budget or requires ESCALATE. The T-block's job is to prove suite honesty and surface findings with evidence; it did both. V-2 owns the blocking classification.

## Cascade
Not applicable (APPROVED — no rework).

- **Stages that must re-run after the above:** none
- **Stages that stay untouched:** T-1, T-2, T-3, T-4, T-5, T-6, T-7, T-8

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 2
