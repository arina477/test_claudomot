
# Sentry error tracking (wave-35). Provider-issued DSN, set on Railway at deploy. Unset -> SDK no-ops.
SENTRY_DSN=

# Public base URL of the api service — used to build stable avatar redirect URLs (wave-38)
PUBLIC_API_URL=https://api.example.com
