---
name: User Journey Map
description: Canonical inventory of every user flow, screen, route, API endpoint. Regenerated at T-9 from production state.
last_updated: 2026-06-30 (T-9 wave-15 — M3 @MENTIONS LIVE; data plane (parse word-boundaried + resolve member-only + persist message_mentions table, migration 0007) + realtime fan-out + GET /me/mentions + composer @autocomplete member-picker + mention pills (self/other) + unread-mention badge. NEW REST: GET /me/mentions (session-derived authz, cursor-paginated). NEW realtime: per-user room `user:<userId>` + `mention` event (the B-6 H-1 fix — decoupled from channel rooms so a mentioned user not viewing the channel still gets notified). EXTENDED: MessageResponse.mentions[], ServerMember.username. LOAD-BEARING-PROVEN live with TWO DISTINCT VERIFIED users (A+B co-members): two-client mention realtime ALIVE (B not in channel room received `mention` event, correct channel+recipient, 0 message:new leak); my-mentions authz IDOR-closed (B sees only B, A sees only A, ?userId= ignored, 401 unauthed); membership-scoped resolution (non-member → plain text, no row); author NOT self-badged; edit-diff add/remove round-trips; pills WCAG AA (viewer pill 10.08:1). No new page route — extends page-9. KNOWN coverage gaps → V-2: message_mentions real-PG integration tier still absent (2-wave carry 02fa8011, MEDIUM); Playwright MCP swarm blocked by chrome-channel-absent (MEDIUM, worked around via bundled chromium). Wave-14 F-4 typing defect remains carried. Verified-prod fixtures: studyhall-e2e-fixture (username studyhallfixturea) + studyhall-e2e-fixture-b (username studyhallfixtureb))
last_updated_wave16: 2026-06-30 (T-9 wave-16 — TEST-INFRA, no UI/schema/product change. Annotation-only regen: the authed create-server flow (page 11 / F7) is NOW E2E-covered. NEW authed Playwright E2E `apps/web/e2e/create-server.spec.ts` + storageState harness `auth.setup.ts` (signs in as verified prod fixture via real /login → creates uniquely-named server → asserts rail by exact accessible name → selects → asserts auto-seeded #general). Ran 4/4 in CI `e2e` job vs live prod (PR#28, merge 6982ffe). Closes the long-carried wave-7 V-3 / T-9-significant "no authed-browser e2e on create-server" coverage gap. NO new route/screen/endpoint — flow is structurally unchanged, so no crawl-regen; coverage annotation updated only. Verified-prod fixture: studyhall-e2e-fixture. Known carries (NOT regressions): prod test-server accumulation w/o teardown until DELETE /servers/:id ships [M-3 = P-4 NOT-MATERIAL]; broad chromium-authed testMatch [M-1]; 9 pre-existing wave-14 biome warnings.)
last_updated_wave17: 2026-06-30 (T-9 wave-17 — TEST-INFRA, no UI/schema/product change. Annotation-only regen: the create-server flow's atomic server-side seeding (page 11 / F7) is NOW real-Postgres-rollback-integration-tested. NEW integration spec `apps/api/test/integration/create-server-rollback.spec.ts` + REUSABLE real-PG harness `apps/api/test/integration/pg-harness.ts` + parallel-safe `vitest.integration.config.ts`. Three cases against a real Postgres 16: positive commit (all 5 row-kinds present), mid-txn failure on the channels insert → real ROLLBACK → 0 orphan rows across servers/roles/server_members/categories/channels, first-insert failure → 0 rows. RATIFIED REAL: real `db.transaction` (not a stub), fault injected INSIDE the open txn via `wrapPoolConnect` patching the real SUT pool's `connect()`, real Postgres ROLLBACK, no-orphan asserted via a SEPARATE cross-connection harness pool (true committed-row visibility check). Ran 3/3 in CI `test` job vs real Postgres 16 service (PR#29, merge dfb65ca). NOTE a false-green was caught at C-1 — Turbo strict-env stripped DATABASE_URL_TEST → integration SKIPPED despite green check; routed to devops-engineer, fixed via turbo.json env passthrough, re-verified the 3 cases actually executed + passed. PARTIALLY MITIGATES the 2-wave-recurring real-PG-tier gap (02fa8011): the reusable harness now exists, so 02fa8011 becomes a thin consumer. NO new route/screen/endpoint — flow structurally unchanged, no crawl-regen; coverage annotation updated only. Known carries (NOT regressions): B-6 accepted M1 (CF-2 DATABASE_URL override no NODE_ENV/VITEST guard, safe — test/ excluded from dist), M2 (integration config no reflect-metadata, safe — spec bypasses Nest DI), L1-L3; 9 pre-existing wave-14 biome warnings; prod test-server accumulation w/o teardown until DELETE /servers/:id ships [M-3].)
last_updated_wave18: 2026-07-01
last_updated_wave19: 2026-07-01
last_updated_wave20: 2026-07-01
last_updated_wave22: 2026-07-02
last_updated_wave21: 2026-06-30
last_updated_wave23: 2026-07-02 (T-9 wave-23 — M5 bundle 2: delegated assignment-organizer authz LIVE. NO new route/screen — annotation-only regen. Structural change: assignment-posting (page 14, F6/F9) is now gated on a dedicated `manage_assignments` RBAC permission (split from `manage_channels`), so a server owner can DELEGATE assignment-posting to a non-owner member/TA without granting channel-management. NEW REST `GET /servers/:serverId/me/permissions` (session-derived EffectivePermissions {owner + 5 flags}, IDOR-safe, non-member 403). Page-14 assignments CTA now visible for owner OR manage_assignments (was owner-only). Page-13 Roles Management now exposes a 5th permission toggle "Manage Assignments" → the permission is grantable in-product (end-to-end). Migration 0011 (roles.manage_assignments boolean, additive + backfill manage_channels→manage_assignments, no silent privilege loss) applied prod-first; api 0ebf493d + web 31fca925 SUCCESS. LOAD-BEARING-PROVEN LIVE at T-8: penetration-tester mutated a fixture role on prod + re-probed the full truth-table — write door (no-perm 403, manage_channels-only 403, granted 201, owner 201), read door IDOR-safe both directions (401 unauthed, 403 non-member, ?userId ignored), escalation door all self-grant paths 403. B-6 /review 0 crit/high/med. Known coverage gaps → V-2: visual E2E/layout BLOCKED by Playwright chrome-absent (task 67881a58, recurring w16/w22/w23 — HTTP-level live-verified as substitute; deployed-bundle string-match confirms gate code live); new authz surface has no dedicated real-DB integration test (F23-T-4, reinforces 02fa8011); non-UUID :serverId → 500 needs ParseUUIDPipe (F23-T-8a, non-blocking, likely app-wide); stale manage_channels code comments (F23-T-8b, docs). Reminders arc (cron+Resend) still DEFERRED (founder key pending). Verified-prod fixtures: studyhallfixturea (owner) + studyhallfixtureb (member).)
last_updated_wave25: 2026-07-01 (T-9 wave-25 — M5 debt: mention token-parser PARITY (client MessageList ↔ server) + editMessage atomicity. NO new route/screen — annotation-only regen, structurally unchanged (extends page-9 messaging surface). USER-VISIBLE FIX: the client tokenizer now derives its mention slug from the SAME grammar as the server. A dot/punctuation-suffixed handle like `@studyhallfixturea.done` now renders the resolved PILL + `.done` as plain trailing text (before: the whole run mis-keyed → rendered as plain text client-side despite server resolution). Shared `MENTION_TOKEN_SLUG_SRC`/`extractMentionSlug` in packages/shared drives the server regex directly; the web client uses a self-contained mirror (`apps/web/src/shell/mentionSlug.ts`) because the CJS-only shared pkg is un-bundleable as a runtime value by vite (documented CJS-avoidance convention) — a shared-vs-local PARITY CONTRACT TEST (`mention-slug-parity.test.ts`) makes any drift a RED test, so single-source-of-truth is enforced. ATOMICITY: editMessage mention-diff (UPDATE messages + DELETE + INSERT message_mentions) now wrapped in one `db.transaction` — a partial failure can no longer leave inconsistent message_mentions. NEW real-PG rollback integration spec `edit-message-mentions-rollback.spec.ts` (fault-injected mid-txn INSERT → real ROLLBACK asserted via a SEPARATE harness connection, 0 partial rows) — CONSUMES the wave-17 harness, materially advancing the long-carried message_mentions real-PG integration gap (02fa8011, flagged wave-15, partially-mitigated wave-17). LOAD-BEARING-PROVEN LIVE at T-5 (bundled-chromium, MCP chrome-absent): 4 scenarios PASS ×2, 0 flakes on prod — AC2 resolved→emerald pill (WCAG-token), AC2 dot-suffix→pill+trailing, AC3 unresolved→plain (no false pill), AC3 mixed→one-pill-one-plain. B-6 /review 0 crit/high (2 Medium tightened: rollback-proof honesty + derive-regex-from-SRC). CI-rule-5 caught a real first-run RED (rollback spec 5000ms timeout → test-harness fault-injection fix, ZERO prod change → 53ms pass) — atomicity itself was always correct. Deployed dbe55a2 via Railway CLI up: api b0251962 + web 25a010b0 SUCCESS (services deploy via `railway up` image-push, NOT git-trigger — a stale-revision false-green was caught + corrected at C-2). PR#37. Known carries → V-2: Playwright MCP chrome-channel-absent (67881a58, recurring w16/w22/w23/w25 — bundled-chromium substitute; LOW infra, not product); mid-word `@` split boundary still divergent from server `(?:^|\s)@` (pre-existing, low blast radius — server mentions[] gate; deferred). Verified-prod fixtures: studyhallfixturea (self-mention resolves).)
last_updated_wave26: 2026-07-01 (T-9 wave-26 — presence dots on message-row AUTHOR avatars. NO new route/screen — annotation-only regen (extends page-9 server-channel-view + the wave-14 presence surface). USER-VISIBLE: each message-row author avatar now shows a live presence dot (online emerald / offline muted) driven by the existing single /presence store — presence extended beyond the member panel. Delivered via a NEW shared `PresenceDot` component (apps/web/src/shell/PresenceDot.tsx) — extracted from the member-panel inline hard-coded-hex dot and bound to the `--color-accent-emerald` token; the member panel was refactored onto it so there is ONE dot-styling source (removes the wave-14 hex duplication). Unknown-presence author (absent from store) → NO dot (graceful degrade, tri-state hasPresence gate). Single /presence socket preserved (no second subscription); per-author memoized re-render. T-5 LIVE E2E CAUGHT a critical prod bug that unit tests masked: author dots rendered for NO author because the /presence co-member snapshot EXCLUDES the connecting user own userId (getCoMemberUserIds filters self; socket.to excludes emitter) → hasPresence(self)=false. FIXED (T-5 fix-up cycle 1, PR #39 12b5ec2): ProfileResponse now returns `userId`; ProfileContext seeds the viewer own presence ONLINE via seedSelfPresence on session load. RE-VERIFIED LIVE x2: self-authored message → emerald online dot (presence-dot-inner + sr-only Online), member panel + a11y unregressed. All 5 ACs live. B-6 also fixed an a11y regression (aria-hidden was over the sr-only label → moved to the decorative dot only). Deployed: api 539c476d + web 4a703d92 (index-BAcJ6YNx.js) SUCCESS (railway up; frontend feature also touched api for the userId field). PRs #38 (feature, also repaired main CI) + #39 (self-presence fix). Known carries -> V-2: per-row presence subscription is O(rows x events) callback work (member panel lifts a single subscription) = future perf lift (B-6 P2, non-blocking); Playwright MCP chrome-absent (67881a58) — bundled-chromium substitute per T-5 rule 1. DM/member-mention/hover affordance dots deferred to sibling fdb444fc. Verified-prod fixture: studyhallfixturea (self-presence seed → online).)
last_updated_wave28: 2026-07-01 (T-9 wave-28 — invite-code ROTATION: owner-ONLY `POST /servers/:id/invite-code/rotate` closes the long-carried wave-9 "permanent invite_code is irrevocable if leaked" gap (d058283d). NO new page route/screen — annotation-only regen (extends the existing wave-8 invites surface, page-12/share-modal; regenerate-link UI is keep-OUT/demand-gated). Backend-only, NO schema/migration — CSPRNG-regenerates the existing `servers.invite_code` UNIQUE column in place; old link invalidated implicitly. Owner-ONLY authz (`server.owner_id !== callerId → 403`; NOT owner-or-creator like revoke — a permanent code has no creator concept; conscious owner-ONLY bypass of the reserved-but-unwired `manage_server` RBAC flag, product-decisions L331-335). callerId session-derived (no IDOR). CSPRNG `randomBytes(16).base64url` (~128-bit) with 23505-retry ≤5 → 409. LOAD-BEARING-PROVEN LIVE at T-8 against prod with TWO real verified fixtures (owner A `studyhallfixturea` + non-owner co-member B `studyhallfixtureb` on proof server ad62cd12): unauth→401, non-owner→403 (owner-ONLY authz, real verified session), owner→201 new-code ≠ old-code base64url, old-link preview 200→404 + old-join 401 (leaked link DEAD, AC2), new-link preview 200 (admits, AC3); CSPRNG unpredictability proven across 5 distinct consecutive rotations. T-4 real-PG integration: 7 rotate cases EXECUTED nonzero in CI (54/44/46/57/49/103/43ms vs postgres:16 — CI-rule-5 false-green defeated, DB unmocked). T-2 402 unit pass incl. 23505-retry regeneration assertion (capturedCodes[1]≠[0], not mock-count). B-6 /review 0 crit/high (2 P1s documented, 2 P2s test-fixed at 42636bc). Deployed api 48c515e9 SUCCESS via `railway up`; web NOT touched. PR #41 merge 8996230. Known findings → V-2 (both LOW, non-blocking): F28-T8a spec AC1 says 200 but NestJS @Post returns 201 (body correct, no client consumer — cosmetic); F28-T8b 403-vs-404 existence oracle (B-6 accepted-debt, spec-conformant AC4, non-secret UUIDs, findServerDetail precedent). No-rate-limit is a DOCUMENTED decision (P-2 keep-OUT), not a gap. Verified-prod fixtures: studyhallfixturea (owner) + studyhallfixtureb (non-owner member).)
last_updated_wave29: 2026-07-01 (T-9 wave-29 — presence/members CODE-DEBT cleanup, behavior-preserving, NO user-visible/route/screen/endpoint change → annotation-only regen (Action-2 skip: wave_type backend, D-block did not fire, B-3 skipped — no crawl; prior map stays canonical). Two internal fixes: (part1) displayName empty-fallback guard `??`→`||` at servers.service.ts:249 (`GET /servers/:id/members` roster mapping) + presence.gateway.ts:125 (`/presence` handleConnection) — an empty email local-part (email `@…`) or stored-empty `display_name` ('') now falls THROUGH to userId instead of resolving to `''`, so a member/presence row never renders a blank display name; wire UNCHANGED (still bare `ServerMember[]` `[{userId,displayName,avatarUrl,username}]`). (part2) DELETED unused `ServerMembersResponseSchema` + `ServerMembersResponse` type + barrel re-export from packages/shared (0 source consumers, grep-proven); members endpoint was already bare-array, no consumer affected. NO schema/migration. LAYERS: T-1 lint+typecheck green on fd03d27; T-2 407 unit pass incl. 5 NEW displayName-guard tests EXECUTED nonzero in CI (run 28536835436) that are MUTATION-GENUINE — fail under the old `??`, assert user-observable output (`.toBe('user-ghost')`/`.not.toBe('')`, `socket.data.displayName`), exercise real SUT not mock-counts; T-4 both touched service specs executed on fd03d27; T-3/T-5/T-6/T-7/T-8 SKIPPED (no contract-shape change/UI change/heavy/auth surface). B-6 /review no P0/P1/P2. Deployed api+web LIVE on fd03d27 (C-2, /health 200). PR #42 merge fd03d27. Zero findings → V-2. NO fixture/prod probe needed — behavior-delta is not HTTP-observable, unit-covered.)
last_updated_wave57: 2026-07-06 (T-9 wave-57 — DM→server nav papercut FIX (M8 DM-polish; **resolves the wave-51 V-2 finding F-1**). NO new route/screen/endpoint — annotation-only on the existing shell state. Bug: on the DM surface (component state dmHomeActive in apps/web/src/shell/AppShell.tsx, NOT a router route), clicking a ServerRail server icon (selectServer) or the Home button navigated but did NOT clear dmHomeActive, so AppShell's ternary kept rendering <DmHome/> — the first click was swallowed, a 2nd click needed to return to the server/channel view. Root cause: setDmHomeActive(false) lived only on the DM-rail button's onDmHome toggle; server-select + Home never reset it (and the Home button had NO onClick at all — decorative). FIX: AppShell passes an `onExitDmHome = () => setDmHomeActive(false)` callback; ServerRail's server-select onClick calls it UNCONDITIONALLY (so re-selecting the already-selected server also exits, not gated on selectedId changing) and the Home button now wires onClick={onExitDmHome}. onDmHome toggle unchanged (entering DM home preserved). Frontend-only, no schema/api/contract change. Proven by 4 real-component tests (real AppShell+ServerRail, jsdom) MUTATION-VERIFIED at B-6 (reverting the fix fails 3/4): server-select + Home exit DmHome on the FIRST click, re-select-same-server exits, DM-entry regression green; CI e2e green (no regression); live web serves the fix. Deployed api+web SUCCESS @ merge 1361c49 (PR #72). T-3/T-4 skipped (no contract/schema), T-6 (no layout/styling change — WHICH pane renders, not appearance), T-7 (not heavy), T-8 (non-auth, client nav state). **F-1 (wave-51) now FIXED.** Verified-prod fixture: studyhallfixturea.)
last_updated_wave52: 2026-07-05 (T-9 wave-52 — M8 study-group HEADLINE: JOINABLE FOCUS ROOMS (body-doubling) LIVE. NEW UI surface (focus-room panel in the server view, below the study-timer widget) — NOT a route. Extends the wave-49/50 study-group surface with the explicit-JOIN room the ambient timer roster lacked. NEW realtime: a distinct `/study-room` Socket.IO NAMESPACE (separate from /study-timer, /messaging, /presence) — client verbs `subscribe_server_rooms {serverId}` (get + live-subscribe the server open-rooms list), `create_focus_room {serverId,name}` (creator AUTO-JOINS), `join_focus_room`/`leave_focus_room {serverId,roomId}`, room-timer start/pause/reset/config; server events STUDY_ROOM_ROOMS_EVENT `study-room:rooms` (open rooms per server) + STUDY_ROOM_PRESENCE_EVENT `study-room:presence` (per-room roster) + `study-room:timer_update` + `study-room:join_error`. Ephemeral (in-memory ONLY — NO table/migration; rooms + rosters + room-timer anchors are in-memory Maps; a zero-member room is REMOVED). A focus room carries its OWN room-scoped synchronized Pomodoro (in-memory anchors keyed by roomId, reuse the wave-49 pure compute-on-read formulas + an in-memory compare-and-set auto-advance re-implemented against the Map — NOT the DB path, NOT server_study_timer). Membership + room-membership gated (assertMember/assertRoomMember 403; serverId/roomId from client, userId from session; IDOR-safe). NEW shared contracts packages/shared/src/study-room.ts (FocusRoom/roster/StudyRoomTimer + distinct study-room:* events). LOAD-BEARING-PROVEN LIVE at T-5 (two-DISTINCT-user, isolated contexts, captured socket frames): skeleton resolves via subscribe handshake; create auto-joins (roster count 1); B joins → both rosters count 2 live no-reload; A starts room timer → B countdown advances live (roomId-scoped); leave → ephemeral room removal on both. T-8: /study-room session-gated (unauth CONNECT_ERROR) + membership-gated (non-member subscribe/create/join denied on 3 foreign servers, 0 leak) + no mass-assignment (injected userId ignored) + secret-clean. B-6 /review 0 crit/high (High creator-auto-join fixed). Deployed api+web SUCCESS (merge 25c0736 feat #66 + fix 725f7b6 #67 — T-5 caught a live-blocking skeleton-stuck bug [client never emitted the subscribe handshake; unit tests mocked the socket]; fixed + redeployed + re-verified). Multi-spec [d123d9e0 backend + aad849ac UI + ef84b378 room-timer], ~5000 LOC. Scope-fenced: NO voice/LiveKit (slice-2), NO persisted attendance/history, NO scheduled rooms, NO moderation, NO whiteboard. Known finding → V-2: F-1 (LOW, info-disclosure) non-UUID serverId leaks a raw Drizzle error via the gateway catch (request still denied — not an auth bypass; same class as wave-23 non-UUID). T-4 skipped (no schema — in-memory; 40 gateway unit tests), T-7 (not heavy). Verified-prod fixtures: studyhallfixturea + studyhallfixtureb (co-members ad62cd12).)
last_updated_wave62: 2026-07-06 (T-9 wave-62 — M12 OFFLINE-FIRST MOAT #1: DM offline read-cache LIVE (the untouched differentiator half of the founding bet; no competitor plays it). NO new route/screen/endpoint/component → annotation-only regen (Action-2: B-3 touched only apps/web/src/features/sync/{cache,db,types}.ts + apps/web/src/shell/{useDm,dm.test}.tsx — a DATA-SOURCE change on the existing F11 DM surface, zero journey delta; crawl would add/remove no routes). WHAT SHIPPED: the DM conversation list + thread history now render from a Dexie v2 offline read-cache when the network is unreachable (previously BLANK offline). Substrate = Dexie v2 (dmConversations/dmMessages + read-through helpers) reusing the shipped M4 offline spine; useDm writes-through on successful GET /dm/* and, on fetch-failure, falls back to getCachedDmConversations/getCachedDmMessages. HIGHEST-RISK + NAMED EXIT CRITERION: the .version(2).stores() re-states the v1 channels/messages/outbox tables VERBATIM (omitting a table in a later Dexie version deletes it + all M4 data) — head-builder byte-compared the v1 restate, and TWO load-bearing v1->v2 upgrade-preservation unit tests (seed v1-shaped channels/messages/outbox rows → open at v2 → assert survival + v2 DM tables coexist) are GREEN. T-2 CI 497/497 incl dm-cache 15/15 (round-trip, [conversationId+createdAt] ordering, null-lastMessage tie-break, 2× preservation) — independently re-run green by head-tester at gate (dm-cache 15/15 + dm.test.tsx 23/23). T-5 LIVE offline probe (deployed prod web-production-bce1a8, fixture A studyhall-e2e-fixture, 2026-07-06): loaded DM list + studyhallfixtureb thread ONLINE (write-through PROVEN via IndexedDB inspection: cache = 1 conversation / 28 messages, Dexie 'studyhall' DB at v2 stores=[channels,dmConversations,dmMessages,messages,outbox]) → context.setOffline(true) with TRANSPORT-LEVEL offline PROVEN (fetch('/api/health') → TypeError: Failed to fetch) → reload + re-navigate → DM conversation list AND full ordered 28-message thread history STILL RENDER from cache (all 5 sampled message-content markers present, correct order), NOT blank; contrast = the non-cached server-rail showed "Failed to load" (proves genuine offline, cache-fallback is the reason DMs render); restored online → health 200. Screenshots t5-offline-dm-thread-rendered.png. Browser context left OPEN (no browser_close, rule 5). T-3/T-4 SKIPPED (no API/shared-DTO shape change — reuses GET /dm/*, CachedDm* are client types; Dexie v2 migration is client-side, fake-indexeddb-integration-tested via the preservation test), T-6 (no new UI/layout — reuses DM surfaces + shipped connection indicator), T-7 (not heavy), T-8 (no auth/session/rate-limit surface). Merge b28ad3b (PR #77), web deployed SUCCESS. No blocking findings. Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhallfixtureb (peer, shared server ad62cd12).)
last_updated_wave63: 2026-07-06 (T-9 wave-63 — M12 OFFLINE-FIRST MOAT #2: ACADEMIC offline read-cache LIVE (assignments + class schedule). NO new route/screen/endpoint/component → annotation-only regen (a DATA-SOURCE change on the existing AssignmentsPanel + ClassCalendar surfaces, zero journey delta; crawl would add/remove no routes). WHAT SHIPPED: the assignment list + class-schedule calendar now render from a Dexie v3 offline read-cache when the network is unreachable (previously BLANK offline), extending the M4/wave-62 offline spine from messages+DMs to academic content. Substrate = Dexie v3 (cachedAssignments + cachedScheduledSessions [window-keyed] + read-through helpers); the read path writes-through on successful GET and, on fetch-failure, falls back to the cache. HIGHEST-RISK + NAMED EXIT CRITERION: the .version(3).stores() re-states ALL 5 prior tables VERBATIM (channels/messages/outbox/dmConversations/dmMessages; omitting a table in a later Dexie version deletes it + all its data) — head-builder byte-compared the 5-table restate, and a v1→v2→v3 upgrade-preservation unit test (academic-cache.test.ts, seeds all 5 pre-v3 tables → open at v3 → assert ROW survival + new academic tables coexist) is GREEN. T-5 LIVE offline probe (deployed prod, merge 699a619): loaded assignments + schedule ONLINE (write-through PROVEN via IndexedDB inspection: IDB version 30 = Dexie schema v3, stores = all 5 prior + cachedAssignments/cachedScheduledSessions, counts cachedAssignments=2 / cachedScheduledSessions=22) → offline PROVEN transport-level → reload + re-navigate → assignment list AND class schedule STILL RENDER from cache (2 assignments + 22 sessions), NOT blank. Full suites GREEN: web 520/520 + api 731/731; CI green on RE-RUN (first-run study-timer async-race flake, unrelated to wave-63). Merge 699a619 (PR #78), web deployed SUCCESS. No blocking findings. T-3/T-4 SKIPPED (no API/shared-DTO shape change — reuses GET endpoints, Cached* are client types; Dexie v3 migration is client-side, preservation-tested), T-6/T-7/T-8 (no new UI/layout, not heavy, no auth surface). Verified-prod fixture: studyhall-e2e-fixture.)
last_updated_wave67: 2026-07-06 (T-9 wave-67 — M11 SERVER DISCOVERY LIVE: a public-community directory. NEW route/screen page-17 `/discover` + NEW flow F12 + NEW endpoints → full journey entry (not annotation-only). WHAT SHIPPED: `GET /servers/discover` (AuthGuard, is_public=true ONLY, memberCount, ILIKE search over name/topic/description, pagination) + a `/discover` browse page (ServerRail preserved + searchable card grid + honest cold-start empty-state + dark-on-emerald Join per §8 AA) + `POST /servers/:id/join-public` (AuthGuard, is_public-gated). Migration 0024 (`servers.is_public`/`description`/`topic`) live in prod. IMPORTANT: prod directory is EMPTY — no publish-to-directory write path yet (deferred M11 bundle 2bd37c4c), so createServer leaves is_public=false; the /discover page shows the honest empty-state in prod. LOAD-BEARING (head-product P-4 NAMED) PROVEN LIVE at T-8 (fixture A studyhallfixturea, 2026-07-06): the is_public join-gate REJECTS private servers — authed `POST /servers/:id/join-public` → 403 "Server is not open for public joining" on BOTH a member-private server AND the private Fixture Proof Server (ad62cd12); unauth join → 401 (no anon backdoor); unauth discover → 401 (AuthGuard); rate-limit active (429); ALSO CI-verified (servers.service.spec: private join throws Forbidden, insert never reached). T-5 LIVE (Playwright MCP — recurring chrome-absent 67881a58 manifested as a STALE SingletonLock [dead PID 686080 from a prior session], cleared the stale lock file only [not a live browser, rule-5-safe] → MCP worked; no browser_close): /discover renders with ServerRail + Discover rail entry [aria-label "Discover Public Servers", B-6 regression intact] + role/label-queryable search box + h1 + honest "No public communities yet" empty-state (NOT an error). Browse+join proven END-TO-END + NON-ECHO: published a fixture server owned by fixture B that A was NOT a member of → card rendered live (name/topic/description) → clicked the REAL Join button → A actually added (button → "Open", GET /servers returns it) → is_public filter proven (only that 1 public server surfaced from 566 total) → fixture torn down, prod restored to empty. T-6 layout: dark-only theme + card grid + glass header + §8 dark-on-emerald Join button (#0a0a0b text on #10b981, ~7.4:1 AA), 0 material token divergences. CI green: T-1 static + T-2 unit (web 583 + api 752, incl discover filter/search/pagination + join-public private-reject assertion) + T-3 contract + T-4 integration on merge 43d20b2, BOTH services SUCCESS. T-7 perf SKIPPED (small page + paginated endpoint, not heavy). Known → V-2 (feature works, non-blocking): F67-T5-1 (SIGNIFICANT) discover memberCount always 0 [returned 0 with 1 member AND 2 members; DB count correct — server-side aggregation defect, cards understate membership]; F67-T5-2 (LOW/MED) join-public creates member row with role_id=NULL [owner has proper default role — confirm role-less-member RBAC intent]. Verified-prod fixture: studyhall-e2e-fixture (studyhallfixturea).)
last_updated_wave51: 2026-07-05 (T-9 wave-51 — DM surface canonical 3-panel LAYOUT FIX (M8 DM-polish; resolves wave-46 V-2 T-6 finding F9). NO new route/screen/endpoint — annotation-only on the existing DM surface. The app shell (apps/web/src/shell/AppShell.tsx) rendered the server ChannelSidebar (~260px) UNCONDITIONALLY, so on the DM surface (component state dmHomeActive, NOT a router route) it showed the STALE previously-selected server's channel list, cramping DmThread to ~372px @1024. FIX: gate the ChannelSidebar desktop wrapper + mobile overlay drawer + mobile backdrop on {!dmHomeActive && ...} (mirroring the existing MemberListPanel guard) → canonical 3-panel DM (ServerRail 72 + DmConversationList 320 + DmThread flex-1). Frontend-only, no schema/api/contract change (migration ledger untouched at 0023). LOAD-BEARING-PROVEN LIVE at T-5/T-6: DmThread measured 632px @1024 / 888px @1280 (was ~372px), ChannelSidebar absent on DM / present 260px in server view (no regression); mobile (390) backdrop no-strand — open drawer (z-40 scrim) → switch to DM removes BOTH drawer + scrim (the B-6 High fix). B-6 /review caught + fixed 1 High (orphaned mobile backdrop: onDmHome now resets sidebarOpen + backdrop guard adds !dmHomeActive). Deployed web+api SUCCESS @ merge 01399a5 (PR #65); first-run green CI (BUILD rule-10 B-5 CI-parity). Known finding → V-2: F-1 (MEDIUM, PRE-EXISTING, non-blocking) — first-click-swallowed on the DM→server RETURN path; ServerRail selectServer/Home handlers do not clear dmHomeActive (unmodified by this wave; verified pre-wave). Verified-prod fixture: studyhallfixturea. T-3/T-4 skipped (no contract/schema), T-7 (not heavy), T-8 (non-auth).)
last_updated_wave50: 2026-07-05 (T-9 wave-50 — M8 study-group slice 2: study-timer PER-SERVER CUSTOM DURATIONS + F-1 slim-bar fix LIVE. Extends the wave-49 shared timer (no new page/route — annotation on the existing Study Timer surface). NEW REST: `PATCH /servers/:serverId/study-timer/config` {workMinutes 1-120, breakMinutes 1-60} → 200 StudyTimer DTO / 400 invalid / **409 not-idle** / 403 non-member / 401 anon (assertMember-gated, serverId route + userId session, IDOR-safe). NEW schema: server_study_timer += work_duration_ms (default 1500000) + break_duration_ms (default 300000), migration 0023 (additive, backfills existing rows to 25/5). The hardcoded 25/5 (journey-map wave-49 note) is now the DEFAULT; a member sets custom work/break lengths while the timer is IDLE (409 if running/paused — chosen transition semantic, keeps compute-on-read anchor model clean), synced to all members via the existing study-timer:update broadcast, reused schema/service/widget. StudyTimerWidget gains a minimal duration-config affordance (desktop inline + <1024 compact inline reveal, NOT a settings panel). **F-1 FIXED:** the <1024 slim-bar 2px phase left-border (emerald Work / amber Break) now renders — the wave-49 T-5 defect (inline border shorthand clobbering stylesheet border-left) resolved. LOAD-BEARING-PROVEN LIVE: T-5 two-DISTINCT-user (A+B isolated contexts via playwright node pkg) — A sets 45/10 → B's widget reflects it live via captured study-timer:update socket frame (workDurationMs/breakDurationMs, updatedBy=A); Start uses custom 45:00 not 25:00 on both; config-while-running blocked (client removes Apply + server 409); F-1 border computed 2px rgb(16,185,129)/rgb(245,158,11) @800px (vs wave-49 1px grey). T-8 config endpoint IDOR-safe (non-member 403, no mass-assignment) + idle-guard 409 server-side unbypassable + anon/CSRF 401 + secret-clean. T-4 real-PG integration: 8 config cases incl karen-2 self-heal-uses-configured-durations. B-6 /review 0 crit/high (1 fix-up round: config-error surfacing). Deployed api+web SUCCESS (merge 699477, 0023 applied prod-first). PR #64. **First-run green CI, 0 fix-up cycles** (B-5 obs-A CI-parity discipline). Scope-fenced: NO per-user prefs/presets/history/heavy-UI (future slices). T-7 perf SKIPPED (not heavy). Verified-prod fixtures: studyhallfixturea + studyhallfixtureb (co-members ad62cd12).)
last_updated_wave49: 2026-07-05 (T-9 wave-49 — M8 study-group tools slice 1: shared study timer LIVE. NEW UI surface (see "Study Timer" section below): a per-server shared Pomodoro widget in the server view (main column above the message list). NEW REST: `POST /servers/:serverId/study-timer/{start,pause,resume,reset}` (assertMember-gated) + `GET /servers/:serverId/study-timer` (compute-on-read remaining/phase). NEW realtime: Socket.IO `/study-timer` NAMESPACE (distinct from /messaging + /presence) — `study-timer:update` (server-room broadcast of authoritative {phase,runState,endsAt}) + `study-timer:presence` (ephemeral "N studying" roster) + `join_timer_room`/`leave_timer_room` + `study-timer:join_error`. NEW schema: `server_study_timer` (1 row/server, ANCHORS ONLY — no stored counter, no per-server timer loop; migration 0022). Auto-advance Work→Break→Work = one-shot idempotent broadcast-on-transition (WHERE ends_at=$expected) + self-heal-on-read; FORBIDS setInterval/@nestjs/schedule. LOAD-BEARING-PROVEN LIVE: T-5 two-DISTINCT-user (fixtures A `21984eb2…` + B `da74148e…`, two real WebSockets, single-cookie-jar trap avoided) cross-client sync ALIVE ~253ms over /study-timer namespace (the B-6 namespace fix — frontend had ridden /messaging, dead until fixed), roster ephemeral 2→1→2 on join/leave + non-persisted (timer resurrects from server endsAt on reload, roster does not), compute-on-read reload drift 0s/−1s. T-8 IDOR-SAFE: authenticated non-member → 403 on GET + all 4 controls (incl. reading a real foreign RUNNING-timer server, no leak), WS sessionless→Unauthorized, non-member WS join→join_error no leak, anon→401, forged cookie-only POST→401 (CSRF gate enforcing), secret-grep clean. T-4 real-PG integration (12 cases, PG16) caught a pause `ends_at` prod bug units missed (fixed at C-1). B-6 /review 0 crit/high. Deployed api 476d8a0d + web d6f480c0 SUCCESS (merge 3835100, migration 0022 applied prod-first). PR #63. Multi-spec [1387d845 widget + cb81bf03 socket + c3daf6d3 backend + 832b83b7 auto-advance]. Hardcoded 25/5 (custom durations deferred → f4b3659e). Known findings → V-2 (both MEDIUM non-blocking): F-1 slim-bar phase indicator absent <1024px (inline border shorthand clobbers stylesheet border-left, one-line CSS fix, D-3-carry regression); F-2 anti-csrf implicit-not-explicit (PRE-EXISTING project-wide, non-exploitable — live blocks forged POSTs). T-7 perf SKIPPED (not heavy). Verified-prod fixtures: studyhallfixturea + studyhallfixtureb (co-members of server ad62cd12).)
last_updated_wave27: 2026-07-01 (T-9 wave-27 — presence PERFORMANCE, behavior-preserving, NO user-visible/route/screen/endpoint change. Two internal perf fixes: (Spec A) added a DB index `server_members(user_id)` so the who-are-my-co-members query (`getServerIdsForUser`, WHERE user_id) uses an Index Scan instead of a full server_members Seq Scan on every /presence connect/reconnect — proven live by a real-PG EXPLAIN integration test (enable_seqscan=off forcing index eligibility, deterministic); migration 0012 applied to prod (index confirmed on public.server_members). (Spec B) lifted the message-list author-avatar presence dots from ONE subscription-per-row to a SINGLE list-level subscription (O(rows×events)→O(events)) while preserving per-author render-scoping (a dot re-renders only when ITS author flips, via React.memo on a derived scalar). Dots render IDENTICALLY — T-5 live regression PASS ×3 (self emerald online dot, member panel online/offline, a11y reachable) on the new bundle index-Dr2UkTXH.js. No journey/route change → annotation-only. Deployed api 855f1ea1 + web 328b1ae9 (railway up). PRs #40. Multi-spec [6a546c7b server index + 07361daf client lift]. Known carries → V-2: presence-dots.test comment misnames the memo mechanism (cosmetic, B-6 P3); Playwright MCP chrome-absent → bundled-chromium (T-5 rule 1). M5 reminders headline still Resend-key-blocked — park-or-key fork escalated to founder digest.)
last_updated_wave30: 2026-07-01 (T-9 wave-30 — M5 HEADLINE assignment due-date REMINDERS now LIVE. Backend cron + email arc, NO new user-visible route/screen → annotation-only regen (Action-2 crawl skip: wave_type=backend, D-block did not fire, B-3 skipped, apps/web diff empty — prior map stays canonical for routes/screens; F6/F9 reminders node flipped aspirational→LIVE per jenny P-4 carry). WHAT SHIPPED: hourly `@Cron` scan (NotificationsModule/ReminderScanService, no HTTP route) finds assignments due within 24h (strictly future — E2 past-due excluded), emails each server-member NOT marked `done` (LEFT JOIN assignment_status, `IS DISTINCT FROM 'done'` → no-status member IS reminded, mirrors ??'todo'). Send-once via `assignment_reminder` UNIQUE(assignment_id,user_id) + `INSERT ON CONFLICT DO NOTHING RETURNING` (email sent ONLY when a row is newly inserted — TOCTOU-safe at-most-once). Transactional Resend email (inline HTML, subject `Reminder: "<title>" is due soon`, body exposes ONLY title/UTC-due/server — no cross-server data, no PII over-exposure). Migration 0013 (assignment_reminder table, FK CASCADE to assignments) applied prod-FIRST before api cutover. LOAD-BEARING-PROVEN: T-4 real-Postgres integration tier EXECUTED nonzero at C-1 (CI run 28543197997, Test Files 8/Tests 29) — 5 correctness cases (a no-status-member reminded via LEFT JOIN, b done-skipped, c send-once second-scan 0 rows/0 emails, d past-due excluded, e 48h-out excluded), DB unmocked, per-test truncate isolation, head-tester source-audited mutation-genuine. T-2 411 unit pass incl. 4 sendAssignmentReminder email tests (assert composed email, not mock-counts). T-1 lint+typecheck green on db752ce, 0 static bypasses in diff. T-8 JUDGED light source-pass (feature emails real users + reads cross-server member data): no cross-server recipient leak [server-scoped WHERE], send-once [no spam], minimal-PII email body, Resend key server-side/api-scope-only; secret grep 0 leaks. B-6 /review 0 crit/high. Deployed api LIVE on 81dc821 via `railway up` (migration applied before cutover; /health 200; RESEND_API_KEY_AUTH in api scope count=1); web NOT touched (api-only wave). PR #43. Multi-spec [4a4c2715 cron + c5c30363 table + 0ba853e2 email]. Known findings → V-2 (both non-blocking): F30-T4-a email HTML render path integration-stubbed (LOW — unit-covered, legitimate network-boundary stub); F30-T8-a no HTTP surface so standard probes N/A (INFORMATIONAL). B-6 tracked follow-up 4905dc3a (at-least-once retry — current design at-most-once) ALREADY FILED, not re-filed. No prod fixture probe (cron has no HTTP route; correctness proven at real-PG integration tier).)
last_updated_wave32: 2026-07-01 (T-9 wave-32 — M6 who's-in-room voice OCCUPANCY indicator (pre-join), LIVE fail-soft; populated state credential-gated. NO new page route/screen — annotation-only regen (TARGETED F4 update, NOT a full crawl — see note below): extends page-10 (Voice/video study room) pre-join surface + F4 flow. WHAT SHIPPED: pre-join occupancy indicator polls NEW `GET /channels/:channelId/voice/participants` (session + membership gated, `{count, participants[]}`) and renders per design/voice-occupancy-indicator.html (4 states). The "who's-in-room occupancy indicator" MOVED out of the F4 KEEP-OUT/deferred list INTO the live F4 pre-join flow. LOAD-BEARING FAIL-SOFT PROVEN LIVE (T-5, prod, @playwright/test --no-sandbox): with LIVEKIT_* unset the endpoint 503s → indicator degrades to "Occupancy data currently unavailable" chip AND Join CTA stays visible+enabled (broken telemetry never gates Join — S3 screenshot-backed); click Join → graceful Error state, shell intact, no white screen (S5). SECURITY PROVEN LIVE (T-8, prod, two verified fixtures A+B): gate order canViewChannelById→403 FIRST / non-voice→400 / member+creds-unset→503-not-500; non-member 403 body BYTE-IDENTICAL across channel-exists/nil-UUID/random-UUID (NO cross-server occupancy enumeration leak; wave-31 uniform-403 holds); rate-limit active (1×503→29×429); LiveKit secret server-side only, secret-grep clean (2 matches = fake test fixtures). T-6 layout: Error/fail-soft state matches design State 4 at 1440/1280/1024, 0 token violations. CI tier (T-1..T-4, merge 45b08c3, run 28554411114): lint+tsc green, api 449+web 296 green, controller contract spec (200/401/403/400/503), service spec exercises gate→load→creds→RoomServiceClient(MOCK, outermost SDK boundary only — RBAC/db NOT mocked)→lookup on Postgres 16. T-7 perf SKIPPED (not heavy). CREDENTIAL BOUNDARY (honest, not theater): populated occupancy (real participant names/avatars, Empty vs Populated states) needs LIVEKIT_* keys → deferred to founder-supplies-keys; the full RBAC/type/creds-guard security surface IS proven live without keys (gate runs BEFORE RoomServiceClient). N-1 tripwire stands: 3rd cred-blocked M6 wave → park-or-key fork. Known finding → V-2 (LOW-MED, non-blocking): F-32-T-8-1 — malformed non-UUID channelId on authed path → 500 (generic message, no leak) instead of 400/403; missing ParseUUIDPipe on route param; V-2 should ALSO check the wave-31 `POST /channels/:channelId/voice/token` endpoint (shares the identical :channelId param pattern, likely same gap). WHY TARGETED not full-crawl: single small additive surface (one endpoint + one pre-join indicator) on an otherwise unchanged map; F4/page-10 are the only touched nodes; the rest of the map is structurally unchanged this wave — a full prod re-crawl would add cost without new signal. Deployed 45b08c3 (both services SUCCESS at C-2). PR merged; prod DB fixtures torn down + restored+verified. 0 flakes, 0 fix-up cycles. Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B).)
last_updated_wave34: 2026-07-02 (T-9 wave-34 — M6 FINAL slice: voice SCREEN-SHARE (LIVE, proven 2-participant) + audio-only fallback (shipped-but-not-user-reachable). TARGETED F4/page-10 regen, NOT a full crawl — single additive in-room surface (screen-share tile + audio-only banner) on an otherwise structurally-unchanged map; F4 + page-10 are the only touched nodes, and their live evidence is already captured by T-5 (2-participant screen-share) + T-6 (tile layout) against prod, so a full prod re-crawl adds cost without new signal. HEADLINE: the LiveKit connection is NOW LIVE — founder supplied the keys this session, so voice actually connects end-to-end for the FIRST time (waves 31/32/33 were credential-blocked). SCREEN-SHARE PROVEN LIVE (T-5, ceo NON-NEGOTIABLE): two DISTINCT prod users (A sender `21984eb2` + B receiver `da74148e`) in SFU room `RM_6mXtfPMddChR`, server-truth `RoomServiceClient.listParticipants`=2; A's SFU track set gained `SCREEN_SHARE/VIDEO` on share + lost it on stop (grant accepted at the SFU, not just JWT); B rendered the DISTINCT PROMINENT tile (`design/screen-share-tile.html`) + reverted with no orphan. Token grant WIDENED at the API (`voice-token.service.ts` `canPublishSources=[microphone,screen_share,screen_share_audio]`, no camera); WHO can mint UNCHANGED. AUDIO-ONLY FALLBACK — HIGH FINDING → V-2 (honest crux): the fallback is BUILT + unit-tested (hook + banner), and the audio-never-dropped invariant is PROVEN LIVE, BUT it has NO user-reachable trigger in prod — `enterManual()` is unwired to any control-cluster button (`VoiceStudyRoom.tsx:412`; live cluster = `[Mute mic, Share screen, Leave]`) and the auto `ConnectionQuality→Poor` path is on the non-headless LiveKit media plane. So spec-2 AC1 ("opts in via a manual toggle") is BOTH unverifiable-live AND un-invokable — dead UI this wave. NOT claimed green-by-assertion; filed HIGH → head-verifier adjudicates spec-2 AC1 MET-vs-partial before M6 closes. SECURITY PROVEN LIVE (T-8): widening live in decoded JWT (room-scoped, no camera, 1h TTL, sub-bound); auth matrix intact — unauth 401 / malformed :id 400-not-500 / non-member 403 / member 200, load-bearing IDOR pair on a REAL private channel (non-member 403 vs member 200); wave-31 uniform-403 holds; `LIVEKIT_API_SECRET` server-side, secret-grep clean. T-6 layout: screen-share tile structural+token match vs design across 1440/1280/1024, 0 token violations; audio-only banner live-diff DEFERRED (unreachable, component-level compliance only). CI tier T-1..T-4 green on merge 87db7ec (api 468 + web 322 = 790, incl. audio-never-dropped invariant + grant assertion). T-7 perf SKIPPED (not heavy). LOW/info carries → V-2: screen-share tile aria-label empty name (LiveKit participant `.name` unset on mint — cosmetic a11y, cross-ref T-6); two-simultaneous-sharer edge not exercised (single publisher this run). Deployed 87db7ec (api 73938bde + web e211f14d SUCCESS at C-2). Media-plane boundary (real audio/video pixels, organic quality degradation) honestly deferred, not faked. M6 success metric = talk (w31) + occupancy (w32) + screen-share (w34 LIVE) + graceful audio-only degrade (w34 SHIPPED but not user-reachable) → N-block close of M6→M7 is CONDITIONAL on the audio-only-reachability disposition at V-2. Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B).)
last_updated_wave37: 2026-07-02 (T-9 wave-37 — M7 PERSISTENT IN-APP NOTIFICATIONS now LIVE: header bell + notifications panel (app-shell overlay, NO new page route) + 3 new REST endpoints. WHAT SHIPPED: (1) a header bell in the MainColumn top-bar cluster (Search·Pinned·Notifications) with an emerald unread-count badge (9+ cap) — `aria-label="Notifications, N unread"`, count = server unreadCount live. (2) a notifications panel popover (open on bell click) rendering §113 loading skeleton → list state (mention rows newest-first: actor + "mentioned you in" + #channel + relative-time + excerpt, unread rows emerald-tinted + dot) OR empty state ("You're all caught up"). (3) row click → navigates into channel + PATCH marks read + bell decrements + panel closes; "Mark all as read" → POST read-all → bell 0. NEW REST: GET /me/notifications (session-derived, cursor-paginated list + unreadCount), PATCH /me/notifications/:id/read (owner-only mark-read), POST /me/notifications/read-all (bulk mark-read). NO new page route/screen — the bell+panel are an app-shell overlay on the existing /app shell (T-9 crawl confirmed URL stays /app before+after bell click). NO new realtime surface — the bell live-increments on the EXISTING `mention` socket event; reminder-type notifications surface on next fetch (NON-GOAL: live-push). Migration 0015+0016 (notifications table + indexes) applied prod-first at C-2. TWO-READ-MODEL COEXISTENCE (INTENTIONAL, NOT a sync bug — per D-block/jenny): the bell/panel unread count is GLOBAL + SERVER-AUTHORITATIVE (notifications.read_at, via `useNotifications`), whereas the pre-existing per-channel unread-mention badge (`useMentionBadge`, ChannelSidebar) is a SEPARATE per-channel counter; they are independent subsystems by design and DO diverge (T-5 live: bell=7 unread vs #general channel badge=46) — this is deliberate coexistence, NOT a state-sync defect; useMentionBadge is untouched this wave. LOAD-BEARING-PROVEN: T-4 real-Postgres integration EXECUTED nonzero in CI (run 28622699260 `test` job, 6 notifications-authz tests 0-skipped, real-DB latencies 54/173/407/201/203/84ms — owner-404 [B→A's notif markRead → 404, A stays unread, A then marks own OK] + markRead idempotent + markAllRead A-scoped/B-untouched + mention dedup [double-emit → exactly 1 row] + listForUser A-only-scoping+enrichment; DB UNMOCKED) + controller method-drift spec (14, asserts markRead=PATCH/markAllRead=POST/list=GET route metadata — guards the pre-merge HIGH-1 POST→404 regression class). T-2 api 521 + web 333 units green. T-8 security LIVE (two verified fixtures A+B): owner-404 IDOR reproduced live (B PATCH A's notif → 404, A stays unread, no partial mutation), self-scoping (B GET → 0 items, A never leaks, ?userId=<A> ignored), 401 on all 3 verbs guard-first, HIGH-1 POST→PATCH verb-fix confirmed live (old POST verb → 404). T-5 e2e 4/4 flows PASS live (bundled-chromium, MCP chrome-absent carry per T-5 rule 1 — never browser_close): bell+badge, panel §113 states + empty state, TWO-CLIENT mention flow (B @mentions A → A's persistent enriched notif → click → GET channel messages + PATCH read 200 + bell decrement + panel close — genuine sender/receiver, not self-echo), mark-all-read. T-6 layout PASS (emerald pill rgb(16,185,129) on rgb(10,10,11), 9+ cap, unread/read distinct rgba(16,185,129,0.06)+dot vs transparent, no overflow 1440/1280/1024). T-1 lint+typecheck green (run 28622699260, merge 86b7323, 0 TS bypasses). T-7 SKIPPED (not heavy). B-6 code-reviewer's 2 HIGH fixed pre-merge (HIGH-1 mark-read POST→PATCH 404-fix; HIGH-2 reload panel on open); 1 INFO nit (stale UnreadCountResponse doc comment) non-blocking. Deployed prod at C-2 (web bundle index-DCKZ02HB.js, api fa782b68). Known findings → V (both non-blocking): F37-T5-1 (LOW/informational — reminder-TYPE notification rows NOT exercised live because no reminder notifications exist in prod [Resend-key-blocked per wave-30, parked a1299e88]; MENTION rows fully exercised at every layer, reminder-row rendering unit-covered + reminder GENERATION path itself real-PG integration-tested via reminder-scan.spec — honest external-credential gap, consistent with the standing M5 park-or-key fork, NOT coverage theater); F37-T5-2 (INFO — one benign pre-auth 401 on the initial /me/notifications fetch before session hydration, then re-fetches 200; cosmetic, candidate: gate first fetch on session-ready). Prod-state note (not a finding): T-5 generated ~25 mention notifications on fixture A, left clean at unreadCount=0 (read-all); rows persist as history (no DELETE endpoint — documented no-teardown pattern). T-9 independent crawl re-confirmed on deploy index-DCKZ02HB.js: fixture A → /app, bell aria-label "Notifications, 0 unread" (button, role/label query — no test-id), panel opens as overlay (URL stays /app), header + "Mark all as read" + content present, GET /me/notifications 200 live; unauthed GET/PATCH/POST all 401-not-404, old POST verb 404. Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B).)
last_updated_wave35: 2026-07-02 (T-9 wave-35 — M7 PRIVACY controls now LIVE: enforced profile-visibility + account-data view/download + public /privacy + /terms stubs + Sentry observability. THREE routes flip to LIVE this wave (all pre-inventoried): page-16 /settings/privacy (the new product surface), page-2 /privacy + page-3 /terms (compliance stubs, real content). WHAT SHIPPED: (1) /settings/privacy renders an HONEST profile-visibility control — exactly TWO enabled radios [everyone="Visible to classmates" | nobody="Hidden"], auto-saved (PUT /profile/privacy → GET round-trips across reload); the 3-valued server enum (everyone|server-members|nobody) stays locked server-side, UI collapses server-members→Visible per the BOARD Path-A binding. (2) ENFORCED (not cosmetic) roster hiding — GET /servers/:id/members filters server-side by the subject's profileVisibility: nobody → subject ABSENT from every OTHER member's roster while STILL seeing self; server-members → co-member-visible; everyone → visible. (3) Account-data section ("Your data") + "Download my data" → self-scoped studyhall-account-data.json (profile/memberships/activitySummary, caller's own data only). (4) who-can-DM is a DISABLED affordance (BETA badge, aria-disabled, 0 enabled inputs, "preference saved + enforced once available" copy) — NOT a working toggle this wave. (5) Sentry observability wired (beforeSend PII scrub) — no user-visible route. NEW REST: GET/PUT /profile/privacy (session-derived, 400 zod on bad enum), GET /profile/data + GET /profile/data/export (structurally session-scoped — ?userId ignored, no IDOR). NO new realtime surface; migration 0014 applied prod. LOAD-BEARING SECURITY PROVEN LIVE (T-8, two verified fixtures A+B co-members of ad62cd12): CRITICAL#1 roster-hiding — A=nobody → count 1 in B's roster (A absent) / count 2 in A's own (self-present), A=server-members → co-member-visible, A=everyone → restored (server-side, not client hide); CRITICAL#2 data-export IDOR-safe — ?userId=<B> ignored on both routes, always caller's own; auth boundary 401 unauth + malformed bearer (control 404); PUT bad enum → 400 zod (no write, stored value unchanged); roster rows omit email PII (keys exactly [avatarUrl,displayName,userId,username]). 0 CRITICAL/HIGH. T-5 e2e 6/6 flows PASS live (bundled-chromium, MCP chrome-absent per T-5 rule 1 — never browser_close): settings-privacy renders all 4 elements, honest Visible/Hidden persists across reload, disabled DM affordance, self-only JSON download, /privacy+/terms public 200 + footer links reach them, skeleton-not-spinner on shell. T-6 layout: no regressions (dark rgb(10,10,11), 0 horizontal overflow at 1280/390, disabled DM dimmed opacity 0.55). T-1..T-4 CI-verified green on merge 0c71585. T-7 SKIPPED (not heavy); T-9 crawl independently re-confirmed on this deploy — landing hero + /login "Welcome back" + /signup + authed /app shell all intact (NO cross-wave regression), /privacy "Privacy Policy" 1413ch + /terms "Terms of Service" 1758ch real content, authed /settings/privacy 2 radios [everyone,nobody] + disabled DM + download btn + own-email. Prod state left clean (read-only crawl; A stays everyone). Known coverage gap → V-2 (MEDIUM, honest debt not gate-block): NO dedicated automated tests for the new privacy endpoints — behavior proven at live layer (T-8) + code-read (B-6), regression-test absent (candidate follow-up test task). Honest N/A: notifications-panel surface does not exist → states AC N/A (not a fault). LOW cosmetic pre-existing: /privacy+/terms "Last updated: 2024" (same pre-existing © 2024 string, not wave-35-introduced). Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B).)
last_updated_wave38: 2026-07-03 (T-9 wave-38 — M7 AVATAR STORAGE go-live: avatar upload/render + message attachments now LIVE (both were shipped-but-dormant, storage never wired). Founder resolved the wave-37 pause via Path A + supplied 4 Tigris S3 creds. NO new page route/screen → annotation-only (Action-2 skip: wave_type backend+auth, D-block did not fire, B-3 skipped — no crawl; targeted node update only). WHAT SHIPPED: avatar render migrated static-public-URL→presigned-GET because the Tigris bucket is EMPIRICALLY PRIVATE (probed live: anon static GET 403, PutBucketPolicy NotImplemented, per-object public-read ACL ignored, presigned-GET 200). New public/unauth `GET /users/:userId/avatar` → 302 to a 300s presigned GET; confirm persists `users.avatar_key` (migration 0017) + a stable app URL `${PUBLIC_API_URL}/users/:id/avatar?v=<hash>` so all 6+ DTO consumers stay unchanged (zero blast radius) + `<img>` follows the 302 transparently (zero frontend change). 2MB avatar cap + 10MB attachment cap already-shipped (verify-only). CRUX PROVEN LIVE (T-5, prod, 2×): anon no-cookie GET of a real avatar_url → 302 → 200 image/png, `<img>` naturalWidth=64 paints + persists across reload. Attachments ACTIVATED (T-5): presign 200-not-503 → send 201 → attachment presigned-GET 200 byte-identical; >10MB → 413. SECURITY PROVEN LIVE (T-8, fixture): presign/confirm 401 unauth; cross-user confirm key → 400 (IDOR blocked, startsWith incl. trailing /); @Throttle 120/60s → 429@120 retry-after 60; presigned URL X-Amz-Expires=300 GET-only tamper-resistant; no enumeration oracle; secret-grep clean (Tigris creds only in Railway env). T-1..T-4 CI-verified green on merge 8b590e1 (biome+tsc, 524 unit, real-PG avatar-render integration 302/404). T-6/T-7 SKIPPED (non-UI/not-heavy). Deployed api f625a163 SUCCESS (railway up CLI-push; NOT git-triggered); C-2 smoke GET /users/<uuid>/avatar → 404-not-503 confirms creds live. PR #52 squash 8b590e1. Known findings → V-2: **F1 MAJOR (pre-existing, out-of-scope)** — profile-settings entry button is a dead no-onClick button → avatar upload UI NOT reachable by real users through the interface (wave-4-era frontend wiring gap; backend crux works via the app's real fetch pipeline). F-T8-1/2 LOW — GET /users/:id/avatar 500s on NUL-byte userId (needs ParseUUIDPipe) + confirm 500s when own object never uploaded (needs catch). F3 LOW — orphaned oversize objects pre-confirm-reject (documented known-debt). F2 INFRA — Playwright MCP chrome-channel absent → bundled-chromium workaround (never browser_close). Sibling a1299e88 (Resend email domain) STILL founder-blocked → M7 not fully closed. Verified-prod fixture: studyhall-e2e-fixture.)
last_updated_wave39: 2026-07-03 (T-9 wave-39 — SETTINGS-DOORWAY USER MENU: fixes wave-38 F1 (dead settings button). NO new page route/screen → targeted annotation (shell overlay, not a full crawl; T-5 live-crawled the affordance). WHAT SHIPPED: the ChannelSidebar-footer "Your profile and settings" button (was a dead no-onClick button, hover handlers only) now opens a small role=menu popover (NEW apps/web/src/shell/UserMenu.tsx, reusing the MessageList AddReactionPopover pattern; aria-haspopup=menu + aria-expanded; Esc closes+refocuses trigger; outside-click closes; opens upward; dark #27272a tokens) with 3 role=menuitem over EXISTING routes: Profile → /settings/profile (avatar uploader), Privacy → /settings/privacy, Log out → SuperTokens Session.signOut() → /login. P-0 SELECTIVE-EXPANSION (ceo, head-product accepted): a single hardcoded navigate would leave privacy + logout unreachable, so a small menu over existing routes unlocks the whole settings surface; guardrail = existing routes only, no new settings UI. Logout previously had NO UI at all (Karen/jenny verified) — now live. CRUX PROVEN LIVE (T-5, prod, ≥2×, bundled-chromium MCP-fallback): click button → menu opens → Profile → upload avatar → renders naturalWidth=64 + persists across fresh login — a real user reaches+uses the avatar feature via the UI ALONE (closes wave-38 F1). T-8 logout/session PROVEN LIVE: POST /auth/signout 200 → /login; server-side session revoked (GET /profile 200 authed → 401 after logout; /app bounces to landing; httpOnly tokens cleared). T-6 layout PASS (dark tokens, opens upward, in-viewport, no overflow @1280). T-1/T-2 CI-verified green on merge 21f02ee (biome+tsc, 341 web tests incl. 8 UserMenu tests + a logout-reject-still-navigates [C1] guard); T-3/T-4/T-7 SKIPPED (no contract/schema/heavy). B-6 code-reviewer caught + FIXED (91bcb5a) 1 CRITICAL (logout no error-handling → rejected signOut stranded user; fix = try/catch/finally, navigate('/login') always runs) + 1 HIGH (onClose before async orphaned the error; fix = act-then-close). Deployed WEB service 257dacb4 SUCCESS (railway up CLI-push; served-bundle content assertion PASSED — the change-unique "User menu" aria-label present in the live /assets/index-QN5fEltz.js). PR #53 squash 21f02ee, 0 CI fix-up cycles. 0 blocking findings → V-2. Sibling a1299e88 (Resend email domain) STILL founder-blocked → M7 not fully closed; 7525b759 (avatar endpoint hardening, LOW) still open. Verified-prod fixture: studyhall-e2e-fixture.)
last_updated_wave40: 2026-07-03 (T-9 wave-40 — AVATAR ENDPOINT HARDENING (resolves wave-38 F-T8-1/F-T8-2). NO route/screen/UI change → targeted annotation (backend, Action-2 regen-skip). WHAT SHIPPED: two avatar endpoints that returned 500 on malformed/edge input now return clean 4xx. (1) GET /users/:userId/avatar — a NUL/control-byte userId (which reaches the text-keyed DB query and 500s via the pg driver) is now rejected by a boundary guard `/[\\x00-\\x1f\\x7f]/` in users.controller.ts → 400. CRITICAL design choice (P-0 REFRAME off the task's original ParseUUIDPipe proposal): users.id is opaque text (SuperTokens id, not a canonical UUID), and the wave-33 global filter only catches 22P02 (uuid-cast) which never fires on a text column — so the fix is a control-byte boundary reject that imposes NO UUID shape (a valid non-UUID id still 302s/404s, NOT 400 — regression-guard tested + live-proven). (2) POST /profile/avatar/confirm — a valid own-scoped key never uploaded threw uncaught HeadObject NoSuchKey → 500; now checkAvatarSize catches NotFound/NoSuchKey/404 → NotFoundException 404 (re-throws other errors; 503 storage-unset + real S3 failures unaffected). PROVEN LIVE (T-8 + C-2 smoke, prod): %00 + C0/DEL control variants → 400 (zero 500s); valid non-UUID id → 404 / real id → 302 (regression guard holds); confirm-never-uploaded → 404 (avatar_url not persisted); generic 4xx bodies (no leak); happy path 200 + unauth 401 intact. T-1/T-2/T-4 CI-verified green on merge 9c5054d (biome+tsc, 543 api tests incl. new users.controller.spec + files.service NoSuchKey specs); T-3/T-5/T-6/T-7 SKIPPED (no contract/user-flow/UI/heavy). B-6 head-builder + code-reviewer: no crit/high. Deployed api b4a6396b SUCCESS (railway up CLI-push; behavior-proof %00→400 confirms new code serving). PR #54 squash 9c5054d, cleared 1 out-of-scope web flake (server-roles.test.tsx async race — non-regression, backend-only diff) via 1 documented flake-rerun. 0 blocking findings; 1 info (pre-existing x-powered-by:Express banner, out of scope). This EXHAUSTS M7's buildable scope — the only remaining M7 task is a1299e88 (Resend email domain, founder-credential-blocked); the milestone-disposition fork re-fires at wave-41 N-1. Verified-prod fixture: studyhall-e2e-fixture.)
last_updated_wave41: 2026-07-03 (T-9 wave-41 — M8 EDUCATOR TOOLS opened (H2, first slice): Educator/Facilitator role + light moderation now LIVE. First post-MVP milestone (founder Path-B directive: M7/MVP done, moved to M8). NO new page route → targeted annotation (shell-affordance additions to page-13 Roles + page-9 member roster). WHAT SHIPPED: (1) a NEW `moderate_members` boolean permission on the existing per-server boolean-RBAC roles model (migration 0018; NOT a new role-type — P-0 REFRAME); grantable via the existing ServerRolesPage permission-toggle ("Moderate Members"). Educators managing assignments = grant the EXISTING manage_assignments flag (already can()-gated, no authz rewrite). (2) LIGHT MODERATION gated on can(moderate_members): delete-any-message (reuses shipped can()+message:deleted socket fan-out) + member timeout (NEW server_members.muted_until + a send-time mute gate refusing muted senders on BOTH createMessage AND createReply + server-side time-based expiry). Rank guard: a moderator CANNOT delete/timeout a server owner or manage_server/manage_roles holder above them, nor self. (3) member-roster moderation UI (design/member-moderation.html, D-3-adopted): moderation kebab (moderator-only) → role=menu (Time out / Remove timeout) → duration popover 5m/1h/1day → amber muted indicator (visible to ALL). AUTHZ PROVEN LIVE (T-8, prod, fixtures A+B, ALL 7 PASS 0 findings): non-mod→403, educator timeout→200, mute-gate blocks muted member on channel message AND thread reply (403), delete-any rank guard (moderator deletes member msg 204 / owner msg 403), timeout rank guard (owner/self 403), no IDOR (per-server scoped, actor from session, mutedUntil DTO = bare timestamp), secret-grep clean. T-5 UI live PASS (role toggle, timeout flow + muted indicator ×2, non-mod sees indicator-only, keyboard nav); delete-any UI E2E DEFERRED (backend triple-proven T-4+T-8; fan-out reuses shipped deleteMessage path). T-6 layout PASS (1 LOW cosmetic: muted-icon right-edge padding). T-1..T-4 CI-verified green on merge 5a5f79a (biome+tsc, 551 api incl. moderation real-PG integration [rank guard + mute-gate + delete-any] + 354 web incl. 13 member-moderation). B-6 code-reviewer caught + FIXED (03e1102) 2 HIGH authz holes: (a) reply-mute bypass (mute-gate was only on createMessage; extended assertNotMuted to createReply); (b) delete-any missing the rank guard (spec required it on BOTH; added assertDeleteRankGuard). Deployed api c9e34766 + web 856562ad SUCCESS via GraphQL git-connect (serviceInstanceUpdate source.repo + serviceInstanceDeploy latestCommit — the CLI-upload/GraphQL-only deploy contradiction resolved; services now git-connected). PR #55 squash 5a5f79a. Known → V-2: LOW (delete-any UI E2E + 2nd-client fan-out assertion; muted-icon padding; 1 throwaway test server persists — no server DELETE endpoint). M8 `## Success metric` still `_TBD by founder_` — must be set before M8 CLOSES (not this wave). Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B).)
last_updated_wave42: 2026-07-04 (T-9 wave-42 — M8 slice 2: ASSIGNMENT COLLECT/RETURN now LIVE (student submission + educator roster + return-with-comment; NO grading). In-page extension of the Assignments panel (page-14, /servers/:id/assignments) — NO new route → targeted annotation. WHAT SHIPPED: (1) NEW `assignment_submissions` table (migration 0019; id/assignment_id FK CASCADE/user_id/text/attachment on-row cols/submitted_at/returned_at NULL/organizer_comment NULL; UNIQUE(assignment_id,user_id); returned_at+comment folded into initial CREATE per P-0 finding). (2) FOUR endpoints, every :id route derives server_id from the assignment row (IDOR-safe): `POST /assignments/:id/submit` (assertMember; idempotent UPSERT — resubmit updates in place AND clears returned_at+organizer_comment; text and/or single optional attachment; 400 if neither); NEW member-gated `POST /servers/:serverId/assignments/submissions/presign` (assertMember — the shipped organizer presign is organizer-only, so a student needs this; reuses the anti-spoof validateAndHeadAttachment server-scoped HeadObject); `GET /assignments/:id/submissions` (assertOrganizer=manage_assignments; roster w/ submitter identity + resolved attachment URL, submitted_at DESC; empty→200); `POST /assignments/:id/submissions/:submissionId/return` (assertOrganizer; sets returned_at+optional comment; cross-assignment guard — submissionId must belong to path assignment else 400). `mySubmission` now on GET /assignments/:id for the authed member. (3) UI (design/assignment-submissions.html, D-3-adopted after 3 refine iters + head-designer APPROVED): student "Your Work" submit control + own-submission card (submitted-at, attachment chip, emerald Returned badge + educator comment + "Edit submission" resubmit) — hidden for organizers; educator Submissions roster (amber Awaiting / emerald Returned per-row badges) + Return dialog (role=dialog aria-modal, focus trap, Esc close+restore focus, aria-live, optional comment). NO grade/score/rubric anywhere. AUTHZ PROVEN: T-4 14 real-PG integration cases PASS in CI (run 28689560816 — submit/idempotent/resubmit-clears-return, empty→400, non-member→403 IDOR-safe, organizer roster / plain-member→403 / empty-list, return + cross-assignment→400 / non-organizer→403 / unknown→404, no-grade); T-8 CLEAN 0 findings live (unauth→401, IDOR anti-spoof rejects other-server/traversal/injection attachment keys, cross-assignment→400, bad-UUID→400, rate-limit→429, cookies HttpOnly+Secure, no grade leak, secret-grep clean). T-5 live (direct-playwright, MCP chrome-channel broken → executablePath bypass): sign-in + educator roster + Return dialog (Esc+focus-restore, emerald badge) + no-grading PASS; submit + resubmit-clears-return proven LIVE via API; student submit-BUTTON UI blocked (single-account: fixture A owns all servers, fixture B broken — task c50f3040). T-6 layout PASS @1440/1280/1024 + token compliance PASS (3 LOW cosmetic: return modal vs anchored popover, focus-ring alpha, empty student-name slot when displayName null). B-6 head-builder REWORK→APPROVED (fixed a return-route submission-`id` contract drift: roster DTO now exposes id) + /review CLEAN (fixed H1 data-loss: listAssignments now includes mySubmission so a reload doesn't overwrite a returned submission; L1 hide student submit from organizers). PR #56 squash 07ebda95; deployed api+web to 07ebda95 SUCCESS via GraphQL git-connect commitSha-pinned (migration 0019 applied to prod first via public proxy). T-7 SKIPPED (not heavy). Known → V-2: LOW (student submit-button UI E2E needs a non-organizer fixture [c50f3040]; attachment presign not integration-covered [S3 creds]; 3 T-6 cosmetics; unit-coverage for new service methods). M8 `## Success metric` still `_TBD by founder_` (surfaced non-blocking). Verified-prod fixture: studyhall-e2e-fixture (A).)
last_updated_wave43: 2026-07-04 (T-9 wave-43 — M8 slice 3: CLASS SCHEDULING now LIVE (educator-authored scheduled sessions + member class calendar; NO reminders/RSVP/timezone/ICS). NEW in-shell "Schedule" surface on /servers/:id (ChannelSidebar tab alongside Assignments) — no new top-level route → targeted annotation. WHAT SHIPPED: (1) NEW `scheduled_sessions` table (migration 0020; mirrors assignments — id/server_id FK CASCADE/organizer_id text FK/title/description NULL/starts_at/ends_at/recurrence text app-enum {none,weekly}/recurrence_until NULL/is_deleted; INDEX(server_id,starts_at)). (2) FIVE endpoints, every :id route derives server_id from the session row (IDOR-safe): `POST /servers/:serverId/scheduled-sessions` (assertOrganizer=can(manage_assignments)); `GET /servers/:serverId/scheduled-sessions?from&to` (assertMember; **weekly occurrences expanded compute-on-read within a 90-day-capped window, no materialized rows**); `GET /scheduled-sessions/:id` (assertMember); `PATCH /scheduled-sessions/:id` (assertOrganizer); `DELETE /scheduled-sessions/:id` (assertOrganizer, soft-delete). Reuses the shipped `manage_assignments` permission (NO new scheduling flag — closed 5+-flag RBAC). Validation: endsAt>startsAt + weekly recurrenceUntil>=startsAt (Zod .datetime() refines + service defensive guards on BOTH create AND update — B-6 H1/M2 + the T-4-caught createSession guard, all live on e7f1f7a). (3) UI (design/class-scheduling.html, D-3-adopted after 2 refine iters + head-designer APPROVED): date-grouped class calendar/AGENDA (NOT month-grid) with session cards (title/time/Weekly-chip/organizer); educator authoring modal (role=dialog aria-modal, focus trap, recurrence None/Weekly + until, save-failed error, endsAt>startsAt validation); session detail drawer (organizer Edit/Delete + delete-confirm role=dialog + not-found state; member read-only) — all organizer controls gated on manage_assignments. NO grading/reminders/RSVP/timezone/ICS/month-grid. AUTHZ PROVEN: T-4 22 real-PG integration cases PASS in CI (run 28693093402 — create/authz-403/validation-400/update+single-field-PATCH-bypass-400/IDOR-derive/softDelete-excludes/get-200-403-404/list-order/weekly-expansion-count+shared-id+90d-cap/none-once/no-leak); T-8 CLEAN 0 findings live (unauth→401, IDOR random→404 + serverId-smuggle stripped, bad-UUID→400, validation→400, recurrence 90d cap bounds response no-DoS, rate-limit→429, no leak). T-5 live (direct-playwright): all 6 scenarios PASS (create/weekly-5-occurrence/edit/delete-confirm/validation/no-non-goals); student read-only view blocked (single-account, fixture-B broken → RBAC proven at T-4). T-6 layout PASS + token compliance PASS (0 violations) @1440/1280/1024; 1 MAJOR responsive → V-2 (at 1024-min the members panel doesn't collapse when the detail drawer opens → agenda card crushed to 28px, no overflow; 1280/1440 desktop-first clean). B-6 head-builder REWORK→APPROVED (fixed a return... no — B-6 /review found+fixed H1 updateSession range-bypass + M2 datetime-validation + M1 weekly-edit occurrence-collapse; M3 bad-UUID→500 deferred but doesn't manifest on scheduling routes [400]). PR #57 squash 7b0bc478; deployed api+web to 7b0bc478 then api→e7f1f7a (defense-in-depth guard) SUCCESS via GraphQL git-connect commitSha-pinned (migration 0020 applied to prod first via public proxy). T-7 SKIPPED (not heavy; recurrence bounded). Known → V-2: MAJOR T6-F1 (1024 detail-drawer members-panel-collapse); LOW (T5-F1 Esc focus-restore to BODY not trigger; T5-F2 detail panel stale after edit; T6-F3 modal CTA copy; unit-coverage for service methods). M8 `## Success metric` still `_TBD by founder_` (re-surfaced non-blocking escalated). Verified-prod fixture: studyhall-e2e-fixture (A).)
last_updated_wave44: 2026-07-04 (T-9 wave-44 — M8 POLISH/HARDENING (no new surface/route → targeted annotation): 6 pre-triaged V-2/T-6 follow-ups on the shipped M8 core, all LIVE. (1) HEADLINE — the wave-43 MAJOR responsive defect T6-F1 is RESOLVED: the class-scheduling session-detail now renders as a proper overlay dialog (role=dialog aria-modal, Esc + backdrop dismiss, focus trap, inert background, focus-restore) at =<1024 instead of crushing the agenda card to 28px; 1280/1440 inline side-panel unchanged (verified live @1024/1280/1440, card 311px readable). (2) A11y: SessionForm Esc restores focus to the New-session trigger (WCAG 2.4.3); modal-stacking gated so Esc closes the top form not the overlay behind; detail panel refreshes after edit; CTA "Save". (3) assignment-submissions polish (return focus-ring alpha 0.4, username fallback for empty displayName, stale manage_channels->manage_assignments comment doc-only). (4) muted-member indicator right-gutter padding. (5) scheduled-session DTO now emits createdAt/updatedAt (additive, non-breaking) closing the wave-43 V-1 projection gap. (6) test coverage: NEW 16 submission-service + 15 scheduling/recurrence-expansion unit tests (CI run 28695990855 green, 1091 specs); delete-any 2-client E2E authored + PASS (fixture-B verified WORKING -> c50f3040 resolved; moderator affordance + non-mod-hidden verified live, socket fan-out best-effort). B-6 /review ran 3 rounds catching+fixing the a11y H1/M2 + a modal-stacking regression the fix introduced -> CLEAN. NO schema/migration. PR #58 squash 4522101; api+web deployed to 4522101 (new bundle index-CX7LuM3C.js). T-7/T-8 SKIPPED (not heavy; no auth-boundary change). Known -> V-2: LOW cosmetic detail-panel double-fetch flicker; muted-padding live-unverified (no timed-out fixture member). This clears the accumulated M8 debt from waves 41/42/43; M8 discretionary phase (study-groups/DMs/search) awaits the founder success-metric (checkpoint surfaced). Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B, verified working).)
last_updated_wave45: 2026-07-04 (T-9 wave-45 — M8 TECH-DEBT HYGIENE (TEST-INFRA + biome cleanup; NO user-visible/route/screen/endpoint/schema/API/auth change → annotation-only regen, Action-2 crawl-skip: wave_type=infra+ui-behavior-identical, D-block did not fire, B-3 touched only a byte-identical hook refactor — prior map stays canonical for all routes/screens). HEADLINE — the LONG-RECURRING Playwright E2E-runner blocker (task 67881a58, carried w16/w22/w23/w25/w26/w38/w42/w43 as "MCP chrome-channel-absent → manual executablePath / bundled-chromium workaround") is NOW FIXED at the runner level: apps/web/playwright.config.ts sets channel:undefined on all 3 projects + a config-level PLAYWRIGHT_BROWSERS_PATH=~/.cache/ms-playwright override (executes on every invocation path — playwright test, pnpm exec, npm-script) so the runner resolves its OWN bundled chromium instead of the absent /opt/google/chrome/chrome; the per-wave manual executablePath bypass is GONE (grep executablePath repo-wide = 0). ACCEPTANCE PROVEN LIVE (T-5): the canonical fixed entry `pnpm --filter @studyhall/web e2e` launched bundled chromium and ran the suite 5/5 GREEN against the live Railway deploy WITH the broken ambient PLAYWRIGHT_BROWSERS_PATH=/opt/ms-playwright present (the exact failure condition the fix neutralises) — 2 unauthenticated smoke + setup(fixture sign-in) + create-server + delete-any-message(2-context) — no channel/executable-not-found error, no bypass, exit 0 on both a smoke-only and a full-authed run, 0 flakes. SIBLING biome cleanup (4e994e96): apps/web/src/shell/useTyping.ts buildTypingLabel — 6 non-null assertions (typers[N]!) → per-branch element-type casts bound after each length guard; T-1/T-2 source-audited byte-identical output for 0/1/2/3/4+ typers, compiles to identical JS, no user-visible change, no TS bypass introduced (grep = 0), CI lint+typecheck green. LAYERS: T-1 lint+typecheck green on merge 8bb4e51 (0 static bypasses in diff); T-2 354 web units green (buildTypingLabel gap noted); T-5 acceptance proof above; T-3/T-4/T-6/T-7/T-8 SKIPPED (no contract/schema-service/UI-surface/heavy/auth change; T-8 always-run secret-grep + CI secret-scan clean). B-6 /review clean; C-1 7/7 CI green; C-2 web deployed + verified SUCCESS, health 200. PR #59 squash ae22380. Known → V-2 (both non-blocking debt): F1 LOW — buildTypingLabel (5-branch transition table) has no dedicated unit test, so the byte-identical claim rests on review+typecheck+e2e not a table test (candidate table-test follow-up). F2 MEDIUM (PRE-EXISTING wave-44 code, OUT of wave-45 runner-fix scope) — delete-any-message.spec.ts step-7 2-client fan-out is a SOFT-CHECK (waits 8s, logs DELIVERED/NOT_DELIVERED_IN_WINDOW, PASSES regardless; observed NOT_DELIVERED this run) → genuine single-client-realtime honesty gap for the delete fan-out path; the same spec's RBAC/IDOR portions (mod-delete visible to A, hidden from non-mod B) ARE hard-asserted + green, backend fan-out proven wave-41; recommend a deterministic fan-out assertion (await joinChannel ack before delete) in a future test-hardening wave. Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B, working).)
version: 0.32
status_legend:
  - "✅ Live: page renders correctly with real content in production"
  - "🟡 Live but degraded: renders but missing data, broken interaction, or minor known issue"
  - "🟠 Coded but blocked: route exists in code but redirects/crashes/blank in production"
  - "❌ Not built: documented in flow but no matching route in code"
  - "🚫 Deferred: explicitly out of scope"
  - "🆕 Design-only: designed but no code route yet"
---

# User Journey Map — StudyHall

Canonical inventory of every screen / route / surface + flow cross-reference. Regenerated at T-9 Journey each wave from production state. v4 onboarding baseline (founder away, automatic mode — enumerated from v3 flows/features; revisable at v10). StudyHall is a dark-themed desktop communication app, so the inventory is product-screen-heavy; marketing is minimal for a `self-use-mvp` stage. All entries `❌ Not built` until the first wave ships.

## Page inventory

| # | Page | Route | Persona(s) | Related flows | Tools/modules | PD |
|---|------|-------|-----------|---------------|---------------|----|
| 1 | Landing | `/` | visitor | signup-entry | design tokens, hero, **[wave-81] FullPageScroll wrapper — scrolls through sections; fixed nav stays anchored** | full |
| 2 | Privacy policy | `/privacy` | visitor | — | **[wave-81] FullPageScroll — scrollable** | **[stub — compliance, H2 expansion deferred]** |
| 3 | Terms | `/terms` | visitor | — | **[wave-81] FullPageScroll — scrollable** | **[stub — compliance, H2 expansion deferred]** |
| 4 | Signup | `/signup` | P1 | F1 | auth, profile | full |
| 5 | Login | `/login` | P1 | F1 | auth | full |
| 6 | Forgot / reset password | `/forgot-password`, `/reset-password` | P1 | F1 | auth, email | full |
| 7 | Email verify | `/verify` | P1 | F1 | auth, email | full |
| 8 | App home (no server) | `/app` | P1 | F2, F7 | server rail | full |
| 9 | Server channel view (main 3-pane) | `/servers/:id/:channelId` | P1,P2 | F3, F5, F10 | messaging, presence, channel mgmt, offline sync, RBAC, **notifications bell + panel (wave-37, LIVE — app-shell overlay in MainColumn header, no new route)** | full |
| 10 | Voice/video study room | `/servers/:id/voice/:channelId` | P1 | F4 | WebRTC SFU, voice-room UI, presence, **pre-join occupancy indicator (wave-32, LIVE fail-soft)**, **screen-share tile (wave-34, LIVE — proven 2-participant)**, **audio-only banner (wave-34, shipped but not user-reachable — HIGH → V-2)** | full |
| 11 | Create server | `/app` (modal/flow) | P2 | F7 | server mgmt, channel mgmt | full |
| 12 | Invite preview / join | `/invite/:code` | P1 | F2 | invite system, server mgmt | full |
| 13 | Roles Management (full-screen overlay) | `/servers/:id` shell → "Server settings — Roles" button (no dedicated route) | P2 | F8 | RBAC, role mgmt, member-role assign, per-channel visibility | full |
| 14 | Assignments panel | `/servers/:id/assignments` | P1,P2 | F6, F9 | assignment module, notifications | full |
| 15 | User settings — profile | `/settings/profile` | P1 | F1 | profile mgmt, file upload, **[wave-81] FullPageScroll wrapper — FOUNDER BUG FIXED: full form (through academic-identity save) now scroll-reachable** | full |
| 16 | User settings — privacy | `/settings/privacy` | P1 | F1 | privacy controls, **[wave-81] FullPageScroll — scrolls to Delete-account danger zone** | full |
| 17 | Discover public communities | `/discover` | P1 | F12 | server discovery (`GET /servers/discover` — AuthGuard, is_public=true only, memberCount, ILIKE search, pagination), searchable card grid, honest empty-state, dark-on-emerald Join (`POST /servers/:id/join-public` — is_public-gated) **(wave-67, LIVE — prod directory empty until publish-to-directory ships, deferred M11 bundle 2bd37c4c)** | — |

**Compliance-surface quota (self-use-mvp, ≤~10%):** 2 stub pages (Privacy policy, Terms) of 16 ≈ 12% — within tolerance; stubbed, no full fan-out. `/settings/privacy` is a product surface (feature 16), not a compliance-doc page → full PD.


## Deployment status — wave-1 (M1 foundation, shipped)

Live on Railway: web https://web-production-bce1a8.up.railway.app · api https://api-production-b93e.up.railway.app/health (200).
Verified via HTTP + CI RTL component tests (live-browser crawl deferred — Playwright MCP chrome-channel absent in env; a CI chromium job is queued for the next UI wave).

| Surface | Status | Note |
|---|---|---|
| App shell chrome (server rail + channel sidebar + main column) | 🟡 Live but degraded | renders live (dark theme, ConnectionStateIndicator) with placeholder content; no real servers/channels/messages yet |
| `GET /health` (api) | ✅ Live | 200 `{status:ok,service,version}` |
| All other pages (auth, real server view, assignments, voice, settings) | ❌ Not built | per per-page-pd; auth = next wave (b9118041) |


## Deployment status — wave-3 (auth frontend, shipped)
Live: web https://web-production-bce1a8.up.railway.app (SPA + client routes) · api auth backend.
| Surface | Status | Note |
|---|---|---|
| /login, /signup, /forgot-password (+reset), /verify-email | ✅ Live | supertokens-auth-react custom forms wired to live backend |
| /settings/profile | ✅ Live | display_name edit (GET/PATCH /profile); username/avatar/accent 'coming soon' (→ 2a655960) |
| Verify-email banner (app shell, unverified) | ✅ Live | unverified users reach shell + banner; /me 200 emailVerified:false |
| First-run: signup→verify→profile→app-home | ✅ Wired | core flow live (curl-verified); full browser click-through deferred to CI chromium job (c51589cd) |

## Deployment status — wave-4 (M1 profile customization, shipped)
Live: web https://web-production-bce1a8.up.railway.app · api https://api-production-b93e.up.railway.app/health (200). HTTP/code-level verification (browser crawl deferred — Playwright chrome-channel absent, tracked c51589cd). Migration `0001` applied to prod Postgres: users +username (unique, lower() idx) / avatar_url / accent_color.
| Surface | Status | Note |
|---|---|---|
| /settings/profile | ✅ Live | username field (taken/available + 409 on dup), accent-color picker — both persist + render across shell; replaces wave-3 'coming soon' stubs |
| GET/PATCH /profile (4 fields) | ✅ Live | display_name + username + accentColor live-verified (set→200, dup→409, bad→400); avatarUrl field present |
| App shell avatar + accent render | ✅ Live | avatar initials-fallback + accent CSS var render from /profile |
| POST /profile/avatar/presign + /confirm | ✅ Live | wave-38: Tigris bucket creds wired → presign returns 200 (no longer 503); confirm persists `users.avatar_key` + a stable app URL, server-side 2MB cap (413 AVATAR_TOO_LARGE) live-verified. Session-gated (401 unauth); confirm key-ownership check (`avatars/${userId}/`) blocks cross-user IDOR (→400) |
| GET /users/:userId/avatar (avatar render) | ✅ Live | wave-38 NEW public/unauthenticated endpoint → 302 redirect to a 300s presigned GET (Tigris buckets are private → static public URLs 403, so render goes through this redirect). @Throttle 120/60s (429-verified). 404 no-avatar / 503 storage-unset. CRUX proven live: anonymous GET of a real avatar_url → 302 → 200 image bytes, renders + persists across reload |
| Avatar real-upload round-trip | ✅ Live (fully reachable) | wave-38 shipped the backend round-trip (presign→PUT→confirm→render, 84e09891); wave-39 fixed the F1 gap — the shell "Your profile and settings" button now opens a user menu → Profile → /settings/profile uploader. PROVEN LIVE end-to-end via the UI ALONE (T-5 wave-39: click button → menu → Profile → upload → renders naturalWidth=64, persists across fresh login). Avatar feature is now user-reachable. |
| Shell user menu (ChannelSidebar footer) | ✅ Live | wave-39 NEW app-shell overlay. The footer "Your profile and settings" button (was a dead no-onClick button) now opens a role=menu popover (aria-haspopup/expanded; Esc closes+refocuses; outside-click closes; opens upward, dark tokens) with 3 items over existing routes: Profile → /settings/profile, Privacy → /settings/privacy, Log out → SuperTokens signOut → /login. Reuses the MessageList role=menu pattern. Logout was previously unreachable (no UI) — now live: T-8 proved session server-side revoked (GET /profile 200 authed → 401 after logout; /app bounces; httpOnly cleared). |

## Deployment status — wave-7 (M2 servers/channels, first slice — LIVE)
Live: web https://web-production-bce1a8.up.railway.app · api https://api-production-b93e.up.railway.app/health (200, live-verified at T-9). Migration `0002_certain_miek.sql` (servers / server_members / categories / channels) applied to prod Postgres. PR#17 merge 585112f. Verification: HTTP/code-level + live probe (C-2: POST /servers 201, GET /servers lists, GET /servers/:id shows #general; unauthed→401 live-verified at T-9; unverified/non-member→403). Authed full-browser click-through deferred (chrome channel + verified prod fixture absent — c51589cd; L-flag for a persistent verified fixture).
| Surface | Status | Note |
|---|---|---|
| App home `/app` — server rail with REAL servers | ✅ Live | rail lists the caller's servers from `GET /servers` (membership-scoped, server-side innerJoin); selected server highlighted (`aria-current`); empty + loading states render. Replaces wave-1 placeholder rail. |
| Create server (page 11) — `/app` modal | ✅ Live · ✅ E2E-covered (wave-16) | "+" opens single-step name modal → `POST /servers {name}` → 201 → server appended + selected → `#general` shown. Client-side name validation (empty/whitespace disabled; trimmed); API-failure banner (`role=alert`). Atomic server-side seeding: owner membership + default "General" category + `#general` channel in one txn. **TEST COVERAGE (wave-16):** authed Playwright E2E (`apps/web/e2e/create-server.spec.ts` + storageState harness `auth.setup.ts`) — signs in as the verified prod fixture, creates a uniquely-named server, asserts it in the rail by exact accessible name, selects it, asserts auto-seeded `#general` in the channel sidebar. Ran 4/4 in CI `e2e` job vs live prod; closes the wave-7 V-3 / T-9-significant "no authed-browser e2e on create-server" gap. Edge cases (empty-name, API-failure banner, max-servers) deferred. **TEST COVERAGE (wave-17 — real-PG rollback):** the atomic seeding txn is now real-Postgres integration-tested (`apps/api/test/integration/create-server-rollback.spec.ts` + reusable `pg-harness.ts`). Three cases vs real Postgres 16: positive commit (all 5 row-kinds), mid-txn failure on channels insert → real ROLLBACK → 0 orphan rows across all 5 tables, first-insert failure → 0 rows. Fault injected INSIDE the real open transaction (patched SUT pool `connect()`), no-orphan asserted via a separate cross-connection harness pool. Ran 3/3 in CI `test` job vs the Postgres 16 service (PR#29, merge dfb65ca). Proves the "one txn" atomicity claim above is genuinely atomic (no partial seeding survives a mid-txn failure). |
| Channel sidebar (within server view) | 🟡 Live but degraded | renders categories + channels from `GET /servers/:id` when a server is selected (no-server prompt / loading / error states all present). Active channel hardcoded to `#general` — no channel routing yet (out of M2 first-slice scope). Full 3-pane messaging (page 9) NOT built (no messaging/presence this wave). |
| `POST /servers` (api) | ✅ Live | AuthGuard + verify-required + Zod (name 1–100, trimmed); 201 ServerResponse. Atomic txn (server + owner-member + General category + #general). |
| `GET /servers` (api) | ✅ Live | membership-scoped (innerJoin server_members on session userId); returns only the caller's servers; [] when none. |
| `GET /servers/:id` (api) | ✅ Live | 404-before-403 (exists-check then member-check — no existence fingerprinting); returns nested categories+channels for members. userId always from session, never body/params. |
| Server settings / invites / roles (page 13, F8) | ❌ Not built | RBAC/invites/roles are later M2+ milestones; create-server first slice is STRUCTURE only. |

**Access-control note (T-8 verified):** server creation + both reads are auth-gated (401 unauthed, 403 email-unverified) and membership-scoped server-side — the boundary is enforced in NestJS (AuthGuard + session-derived userId + DB innerJoin / 404-before-403), not in the UI. IDOR via param substitution is closed (userId never read from request body/params).

## Deployment status — wave-8 (M2 invites/join, LIVE)
Live: web https://web-production-bce1a8.up.railway.app · api https://api-production-b93e.up.railway.app/health (200, live-verified at T-9). Migration `0004` (invites table 9 cols + servers.invite_code unique) applied to prod Postgres. PR#18 merge 8716b4e. Verification: HTTP/code-level + live probe (C-2 throwaway-fixture: invalid preview 404, valid preview 200-minimal, post-delete 404; unauthed join 401; unauthed createInvite 401 — all re-confirmed at T-9). Authed full-browser click-through + authed join deferred (no persistent verified prod fixture — 4a2ad286; e2e for the new public route is a recorded coverage gap → V-2).
| Surface | Status | Note |
|---|---|---|
| Invite preview / join (page 12) — `/invite/:code` | ✅ Live | NEW public route. Anonymous-reachable preview renders server name + member count ONLY (8 states: loading / valid+Join / unauthed / unverified / already-member / joining / joined / invalid). 68 web RTL tests cover all states. Authed browser join not live-probed (fixture 4a2ad286); no Playwright e2e on this route yet (V-2 finding). |
| `GET /invites/:code` (api, public preview) | ✅ Live | Public, no auth. Returns minimal `{server:{id,name,memberCount}}` ONLY — NO channels / members / presence / owner / invite internals. Invalid code → 404 generic (no existence fingerprinting / leak). Live-verified minimal + 404. |
| `POST /invites/:code/join` (api, verified join) | ✅ Live | AuthGuard + EmailVerification REQUIRED (401 unauthed / 403 unverified — live). Atomic max_uses consume: conditional `UPDATE...WHERE uses<max_uses RETURNING` + throw-on-zero-rows rolls back member insert (per-row lock serializes concurrent joiners → exactly one wins at max_uses=1; TOCTOU fixed 92cc0f3, concurrency-tested). Idempotent re-join: `ON CONFLICT(server_id,user_id) DO NOTHING`, uses NOT incremented on re-join. Authed success path covered by 179 tests + CI integration (not live-probed). |
| `POST /servers/:id/invites` (api, create invite) | ✅ Live | AuthGuard + member-gated: 401 unauthed (live), 403 non-member. Mints CSPRNG `base64url(randomBytes(16))` (~128-bit, non-enumerable over UUID PKs) → `{code,url}`. Two-tier: per-server permanent `servers.invite_code` (auto-set at server creation) + ad-hoc `invites` rows with optional max_uses. |
| Invite-share modal (within server) | ✅ Live | Copy-link share UI per design/invite-share.html (D-3 APPROVED). **wave-9: defaults to the permanent per-server `invite_code` link on plain open** (8b) — mints NO ad-hoc `invites` row on open (regression-guarded). Now also surfaces the limited-invites revoke list (see below). Entry point for F8 invite step. |
| Invite revoke (revoke endpoint / UI) | ✅ Live | **wave-9: NEW.** `POST /invites/:code/revoke` — owner-or-creator gated (server-side, userId from session, no IDOR): 401 unauthed (live-verified), 403 non-owner/non-creator (test-covered), permanent code → 404 (not revocable via this path). After revoke, both `GET /invites/:code` preview + `POST /:code/join` → 404 (shared validateInviteActive honors `invites.revoked`). Idempotent re-revoke. UI: limited-invites list + trash + two-step confirm + revoked state, per D-3. Session-scoped list (no list-ad-hoc GET endpoint — honest gap). |

**Access-control note (T-8 verified, invites = access-control surface):** invite codes are CSPRNG non-enumerable (no sequential id surface — UUID PKs); the public preview is minimum-summary ONLY (the D-block caught + stripped the mockup's channels/members leak; the live API was verified minimal); join is verify-required (401/403) with atomic max_uses (one winner under concurrency, loser rolled back) and idempotent re-join (no use double-count). No residual leak / enumeration / overshoot. The boundary is enforced in NestJS, not the UI.

## Deployment status — wave-9 (M2 invite-completion: revoke + permanent-default + backfill, LIVE)
Live: web https://web-production-bce1a8.up.railway.app · api https://api-production-b93e.up.railway.app/health (200, live-verified at T-9). NO new migration (invites table + servers.invite_code shipped wave-8); 8a backfill ran as run-once prod script (clean no-op, 0 rows). PR#19 merge 371b9fe. Changes are to the EXISTING `/invite/:code` + share-modal surface — no new route. Verification: HTTP/code-level + live probe (T-9 re-confirmed: revoke unauthed 401, preview unknown 404, server-detail unauthed 401, health 200). 196 tests (123 api + 73 web). Authed revoke/join browser e2e still deferred (no persistent verified prod fixture — 4a2ad286; recorded V-2 gap, carry-forward not regression).
| Surface | Status | Note |
|---|---|---|
| Invite revoke (page 12 / F8) — `POST /invites/:code/revoke` + revoke UI | ✅ Live | NEW. Owner-or-creator gated, no IDOR (userId from session). 401 unauthed (live), 403 non-owner/creator (test), permanent code 404. Revoked invite → 404 on preview + join (shared validateInviteActive). Idempotent re-revoke. Completes invite lifecycle: create → join → revoke. |
| Invite-share modal permanent-default (8b) | ✅ Live | Modal defaults to the per-server permanent `invite_code` link on plain open; mints NO ad-hoc `invites` row on open (regression-guarded). |
| `servers.invite_code` backfill (8a) | ✅ Ran clean | App-side idempotent run-once script (WHERE invite_code IS NULL, randomBytes base64url, 23505 retry). 0 rows on prod (no pre-existing servers needing backfill). Not auto-migrate/pgcrypto. |

**Deferrals (non-blocking, info-severity):** ~~permanent `invite_code` rotation is irrevocable — no rotate endpoint (deferred d058283d, Gemini flag; 0 prod servers so no live exposure).~~ **CLOSED wave-28:** owner-ONLY `POST /servers/:id/invite-code/rotate` now regenerates the permanent code (CSPRNG), invalidating a leaked link — see wave-28 section below. Limited-invites revoke list is session-scoped — no list-ad-hoc GET endpoint (honest gap, not a defect).

## Deployment status — wave-10 (M2 RBAC capstone — the access-control core, LIVE)
Live: web https://web-production-bce1a8.up.railway.app · api https://api-production-b93e.up.railway.app/health (200, live-verified at T-9). Migration `0004_green_madripoor.sql` (roles table + channel_permission_overrides + server_members.role_id FK) applied to prod Postgres; `db:backfill-roles` ran idempotent-clean (0 servers → no-op). PR#20 merge `3cf63bf`. Verification: 270 tests (173 api incl. all 6 security conditions + 97 web) green in CI against real Postgres 16; live 401 unauthed boundary verified across every role/override/member endpoint (C-2) and independently re-confirmed at the gate (`POST/GET /servers/:id/roles → 401`). **M2 success metric "channels per role" now MET → M2 feature-complete.** Authed full-browser click-through + 403-non-permitted live-probe still deferred — no persistent verified-prod fixture (4a2ad286, now 4 waves running; ESCALATION-CRITICAL → L). Concurrent owner-demote race is unit-modelled, not multi-connection-executed (prod code has the correct `SELECT FOR UPDATE` lock).

| Surface | Status | Note |
|---|---|---|
| Roles Management overlay (page 13, F8) | ✅ Live | NEW. Full-screen overlay opened from the channel-sidebar "Server settings — Roles" button (`data-testid=server-settings-btn`, `aria-label="Server settings — Roles"`); rendered within the existing `/servers/:id` shell, NOT a dedicated route. Roles list / create / delete, 4 fixed permission toggles (no free-form matrix — the spec-violating matrix tab was caught + replaced at D-3), per-channel visibility editor, member-role assignment, owner-lockout 409 surfaced honestly. 97 web tests; 5 a11y fixes per design/server-roles.html. (UI tests lean on `getByTestId` for state hooks — recorded a11y-as-contract note, not a gate blocker.) |
| Channel sidebar — per-role visibility | ✅ Live | `GET /servers/:id` channel list is now ROLE-GATED server-side: `findServerDetail` filters via `getVisibleChannelIds` so non-visible channels are ABSENT from the response (no enumeration). Replaces wave-7's show-all-channels behaviour. |
| `GET /servers/:id/roles` + `POST /servers/:id/roles` + role CRUD (api) | ✅ Live | AuthGuard + `can(manage_roles)`; 401 unauthed live-verified (gate-confirmed). userId from session (no IDOR). |
| `PATCH /servers/:id/members/:userId/role` (assign role) | ✅ Live | `can(manage_members)`; self-promote blocked at controller + service (defence-in-depth); 401 unauthed live. |
| `GET/POST/DELETE /servers/:id/channels/:channelId/overrides` (api) | ✅ Live | ChannelPermissionGuard reads ROUTE PARAMS only (body-spoof rejected — test proves route-param wins); `can(manage_channels)`; 401 unauthed live. Private channel default-DENY unless override `can_view=true`. |
| Owner-lockout (demote / remove / leave) | ✅ Live | Transactional: `db.transaction` + `SELECT FOR UPDATE` row-lock; last-owner action → 409 (`ConflictException`, asserted). transferOwnership atomic. Concurrent-race serialization unit-modelled (not multi-connection-run). |

**Access-control note (T-8 verified, RBAC = the access-control CORE):** `can()` is server-side and default-DENY (owner_id superuser; else explicit role flag; no membership / no role / null role_id / false flag → false). userId is always session-derived — IDOR via param/body substitution is closed. Channel enumeration is closed server-side (non-visible channels absent from the list, not merely hidden in the UI). Private channels are default-deny. Owner-lockout is transactional with a row-lock + last-owner-409 invariant. The boundary is enforced in NestJS, not the UI. The 401 door is live-verified on every new endpoint; the 403-non-permitted path is CI-tested (270 tests incl. 6 conditions) but NOT live-probed — gated on the verified-prod fixture (4a2ad286), the recurring M2 live-verification gap.

## Deployment status — wave-12 (M3 real-time messaging — the conversational core, LIVE)
Live: web https://web-production-bce1a8.up.railway.app · api https://api-production-b93e.up.railway.app/health (200, live-verified at T-9). Migration `0005` (messages table: UNIQUE(channel_id,idempotency_key); channel_id+created_at index; FKs channel cascade / author) applied to prod Postgres BEFORE new code served. PR#23 merge `168c45f`. Deployed via Railway CLI `up` source-upload (NEW image; the 404→401 route-probe proved the new revision serves M3). **M3 success metric "two-client message delivery <1s" now MET LIVE — message:new in 93ms/87ms across two runs.** 316 tests (200 api: WS-auth / channel-guard / idempotency / fan-out + 116 web: msg UI / optimistic / dedup) green in CI against real Postgres. Verification: HTTP/code-level + live two-client Socket.IO probe (C-2: A POST → B `message:new` 93ms/87ms; non-joined third client no-leak; WS-upgrade unauth → `connect_error Unauthorized`; 401 unauthed REST re-confirmed at gate). Authed full-browser click-through still deferred — no persistent verified-prod fixture (4a2ad286, recurring carry-forward, not a regression). Single-pod in-memory Socket.IO adapter (multi-pod fan-out → later milestone).

| Surface | Status | Note |
|---|---|---|
| Server channel view — real-time chat (page 9, the 3-pane main) | ✅ Live | NEW. The main column now renders a real message list + composer with live delivery. Message-row 3 states (pending `[aria-busy]` optimistic / sent / failed `[role=alert]` + retry); composer (auto-grow, Enter-send); list (`role=log` `aria-live`, empty-state, load-older keyset pagination). Messages arrive in real time via the `/messaging` Socket.IO room for the active channel. 116 web RTL tests (optimistic send, dedup, 3-state rows). Replaces wave-7's hardcoded-`#general` placeholder main column. |
| `POST /channels/:id/messages` (api, send) | ✅ Live | AuthGuard + ChannelMessageGuard (channelId-only `@Param` — IDOR-safe; private channel default-DENY via canViewChannelById). author_id = session getUserId (no authorId in SendMessageSchema — no spoof). Idempotent: UNIQUE(channel_id,idempotency_key) ON CONFLICT return. Live: 401 unauthed (gate-re-confirmed), 403 non-permitted. On success, emits `message:new` to the channel room. |
| `GET /channels/:id/messages` (api, list) | ✅ Live | AuthGuard + ChannelMessageGuard (same private default-DENY). Keyset pagination (load-older). Live: 401 unauthed (gate-re-confirmed). |
| `/messaging` Socket.IO gateway (api, real-time) | ✅ Live | WS-upgrade auth at CONNECT: `io.use()` reads sAccessToken from handshake cookie (+ `auth.accessToken` fallback) → getSessionWithoutRequestResponse → assertClaims(isVerified) → reject unauth (socket.data.userId set fail-closed). `join_channel` re-derives canViewChannelById server-side; `@OnEvent` → `server.to('channel:id').emit` room-only (NEVER broadcast-all). LIVE: unauth socket → `connect_error Unauthorized`; non-joined socket receives NOTHING (no cross-channel leak); two-client delivery 93ms/87ms. Single-pod in-memory adapter. |

**Access-control note (T-8 verified, messaging = the auth + channel-access + WS-auth core):** all four invariants are server-side and live-verified — (1) channel-gating via ChannelMessageGuard (channelId-only route param, IDOR-safe, private default-DENY), (2) WS-upgrade auth rejected at connect (real Socket.IO client, not a dead namespace), (3) no cross-channel leak (room-scoped emit, live non-joined client got nothing), (4) author no-spoof (author_id session-derived, no client authorId). Idempotency dedups duplicate sends. The boundary is enforced in NestJS + the gateway's `io.use()`, not the UI. 401 REST door live-re-confirmed at the gate; the two-client <1s + no-leak + WS-unauth-reject paths are live-probed (synthetic two-client Socket.IO fixture — the authoritative substitute below canary DAU). Live-socket eviction on RBAC revoke is out of M3 scope (join-time gate is correct → H2); the null-idempotency-key send race is unreachable on the prod path (client always sends a key → V cleanup).

## Deployment status — wave-13 (M3 message lifecycle: edit / delete / reactions — LIVE)
Live: web https://web-production-bce1a8.up.railway.app · api https://api-production-b93e.up.railway.app/health (200, live-verified at T-9). Migration `0006` (messages soft-delete cols `is_edited` / `edited_at` / `is_deleted` / `deleted_at`; `message_reactions` table; idempotency UNIQUE `message_reactions_message_user_emoji`) applied to prod Postgres BEFORE new code served. PR#24 merge `427d5d6`. Deployed via Railway CLI `up` source-upload (NEW image; 404→401 route-probe on the new-only PATCH/DELETE/reactions paths proved the new revision serves the lifecycle). **No new route** — extends the existing page-9 messaging surface (message-row now carries edit/delete/reaction affordances + tombstones). ~350 tests (api: edit author-only + delete author||moderator + reactions idempotent + 5 gateway events; web: inline-edit / tombstone / reaction-pills) green in CI against real Postgres. Verification: HTTP/code-level + live round-trip (C-2: edit 200 `isEdited:true`; reaction toggle `{reacted:true}`→`{reacted:false}` idempotent; delete 204 → list shows tombstone `isDeleted:true content:null`) + two-client Socket.IO realtime + gate 401 re-confirm. Authed full-browser click-through + cross-user authz live-probe deferred — no persistent verified-prod fixture (4a2ad286, recurring carry-forward, not a regression). Single-pod in-memory Socket.IO adapter.

| Surface | Status | Note |
|---|---|---|
| Server channel view — message lifecycle (page 9, the 3-pane main) | ✅ Live | NEW affordances on the existing message-row: **inline edit** (author-only; hover/keyboard row-action → inline editor → `PATCH` → "(edited)" label), **delete** (author or moderator; confirm → tombstone row, content gone), **reactions** (reaction-pills with `reactedByMe` highlight + add-reaction popover; idempotent click-to-toggle). All update in real time for every joined client. 131 web RTL tests; UI per design/server-channel-view.html (D-3 APPROVED), keyboard-accessible row-actions. |
| `PATCH /channels/:id/messages/:msgId` (api, edit) | ✅ Live | AuthGuard + ChannelMessageGuard. **Author-only**: `author_id === session userId` else 403; deleted message → 409; not found → 404. Sets `is_edited`/`edited_at`, updates content. Live: 401 unauthed (gate-re-confirmed); edit 200 `isEdited:true`. Emits `message:updated` to the channel room. |
| `DELETE /channels/:id/messages/:msgId` (api, soft-delete) | ✅ Live | AuthGuard + ChannelMessageGuard. **Author OR moderator**: `author_id === userId` OR `can(serverId, manage_channels)` — **serverId resolved from `channels.server_id` server-side, NEVER request-trusted**. Soft-delete tombstone (`is_deleted`, `content` cleared → DTO content `null`). Idempotent double-delete → 204. Live: 401 unauthed (gate-re-confirmed); delete → tombstone. Emits `message:deleted`. |
| `POST /channels/:id/messages/:msgId/reactions` (api, react) | ✅ Live | AuthGuard + ChannelMessageGuard. **Idempotent toggle** via UNIQUE(message_id,user_id,emoji): absent → INSERT → `{reacted:true}` + `reaction:added`; present → DELETE → `{reacted:false}` + `reaction:removed`. userId always session-derived (no body spoof); `reactedByMe` aggregated per-caller. Live: 401 unauthed (gate-re-confirmed); toggle true→false. |
| `/messaging` gateway — lifecycle events (api, real-time) | ✅ Live | Four new `@OnEvent` handlers (`message.updated`/`message.deleted`/`reaction.added`/`reaction.removed`) each fan out via `server.to('channel:id')` — **room-only, NEVER broadcast-all**. LIVE two-client: receiver B got `message:updated` 90ms / `reaction:added` 87ms / `message:deleted` 112ms (all <1s); non-joined third socket received NOTHING (no cross-channel leak). Single-pod in-memory adapter. |

**Access-control note (T-8 verified, message lifecycle = an authz surface):** all lifecycle invariants are server-side and verified at source + unit + live — (1) **edit author-only** (`author_id !== userId → 403`), (2) **delete author||moderator** with serverId resolved from the channel row (not request-trusted) and the moderator-allowed path proven to call `can()` with the resolved serverId, (3) **reactions idempotent** (UNIQUE-backed toggle, session-derived userId, no spoof), (4) **room-only fan-out** (all five emit handlers room-scoped; live non-joined client got nothing). The 401 REST door is live-re-confirmed at the gate (PATCH edit / POST reaction → 401); the two-client <1s + no-leak paths are live-probed. The cross-user 403 path (non-author edit/delete) is proven by committed unit tests (non-author 403 + moderator-allowed both asserted) — NOT live-probed, gated on the verified-prod fixture (4a2ad286, the recurring carry-forward). **Info-severity (→ V):** reaction emoji has no documented allowlist/shape-validation — confirm not arbitrary at B-block. The boundary is enforced in NestJS + the guard, not the UI.

## Deployment status — wave-14 (M3 PRESENCE layer — online/offline + typing + member-list, LIVE with 1 known defect)
Live: web https://web-production-bce1a8.up.railway.app · api https://api-production-b93e.up.railway.app/health (200, live-verified at T-9). NO migration (presence is in-memory server state — Map<userId,Set<socketId>> ref-count + typing TTL; co-membership from existing `server_members`). PR#26 merge `ef6afbf`; api+web new revisions deployed. **No new page route** — adds the page-9 member-list panel + a sibling `/presence` Socket.IO namespace (reuses the /messaging SuperTokens WS-upgrade auth). Tests: 419 in CI green against real Postgres (api 251 incl. +31 NEW presence unit tests closing a zero-coverage gap; shared 37 NEW presence Zod contract tests; web 131). Verification: HTTP/code-level + **two-client wire-level LIVE** (socket.io-client, two DISTINCT verified users). Single-pod in-memory adapter (no Redis — H2 scale deferral KI-1).

| Surface | Status | Note |
|---|---|---|
| Member-list panel (page 9, right pane) | ✅ Live | NEW. Right-hand panel: "MEMBERS" header, grouped **Online / Offline** with count ("OFFLINE — 2"), rows = avatar (initials fallback) + name + presence dot. Reflects ACTUAL server membership via `GET /servers/:id/members` (no new membership model). Responsive: visible at 1440/1024, **collapses <1024** (hidden at 900/768) per design §9 lg-breakpoint, no layout break. Live-verified at all 4 breakpoints with 2 real co-members rendered. Live DOM group-move (Online↔Offline) not directly E2E-captured (F-5 LOW); underlying fan-out proven at wire level. |
| `/presence` Socket.IO namespace (api, real-time) | ✅ Live | NEW sibling namespace. WS-upgrade auth reuses `installWsAuthMiddleware` (SuperTokens session + email-verified claim). `handleConnection`: ref-count up, join `presence:server:<id>` rooms, emit `presence:snapshot` (co-members' states). First socket → `presence:online` to co-member rooms; last socket → `presence:offline`. **Multi-tab safe** (ref-count; 2nd tab no self-flap — live-verified). All emits room-scoped (no global broadcast). |
| `presence:online` / `presence:offline` fan-out | ✅ Live | LIVE two-client (distinct verified users A,B co-members): B receives `presence:online{A}` 311ms after A connects, `presence:offline{A}` 79ms on A disconnect — genuine cross-user, not self-echo. **NO-LEAK**: B only ever received co-member ids (0 foreign); membership-scoped via `server_members` co-member resolution. |
| `presence:snapshot` on join | ✅ Live | Joining socket receives co-members' current online/offline states. Live: B's snapshot included co-member A. |
| `GET /servers/:id/members` (api) | ✅ Live | NEW. AuthGuard + member-gate. Live: unauthed → **401**; member (fixture) → **200** roster `[{userId,displayName,avatarUrl}]`; unverified/non-member → **403** (email-verify claim returns 403 before service member-gate — correct guard order). Member-gate `ForbiddenException('Not a member')` query lacks a dedicated test (F-3b LOW); covered via controller 403-propagation + live. |
| Typing indicators (`typing:start`/`typing:stop` → `typing:active`) | 🟡 Live but DEGRADED | Channel-scoped: `join_channel` re-checks `canViewChannelById` (never client-trusted) → typing fans out ONLY to `presence:channel:<id>` room. NO-LEAK proven (B not joined → 0 typing events). Self-exclusion + 5s TTL auto-expire + disconnect-clears-ghost all live-verified. **DEFECT F-4 (HIGH → V-2): co-members NEVER see who is typing** — `emitTypingActive` broadcasts ONE actor-excluded list to the whole room, so every recipient gets the typer filtered out; typers list always `[]` at B. Task 58633934 core AC ("members see '<name> is typing…'") UNMET in prod. Fix: emit full list + client self-filter, or per-recipient exclusion. |

**Access-control note (T-8 verified, presence = a scoping surface):** membership-scoping is the load-bearing security requirement and it HOLDS — verified with TWO DISTINCT VERIFIED users (not multi-tab): cross-user presence fan-out works (online 311ms / offline 79ms), NO presence leak (B received only co-member ids, 0 foreign), typing is channel-scoped with NO leak (non-joined B got nothing), WS upgrade rejects both unauthenticated AND unverified sessions, members endpoint is 401/403-gated, secret grep clean. The single FAIL (F-4) is a realtime-CORRECTNESS defect (typing payload empty), NOT a scoping/security leak — surfaced HIGH to V-2 (recommend blocking for spec task 58633934). The boundary is enforced in NestJS + the gateway room-scoping, not the UI.

## Deployment status — wave-15 (M3 @mentions — parse/resolve/persist + realtime + my-mentions + autocomplete + pills/unread, LIVE)
Live: web https://web-production-bce1a8.up.railway.app · api https://api-production-b93e.up.railway.app/health (200, live-verified at T-9). Migration `0007_massive_chamber.sql` (`message_mentions` table: id, message_id FK→messages ON DELETE CASCADE, mentioned_user_id FK→users, created_at, UNIQUE(message_id,mentioned_user_id), index (mentioned_user_id,created_at)) applied to prod Postgres BEFORE new code served (drizzle-kit migrate via public proxy; count 7→8). PR#27 merge `fd86540`. Deployed via Railway CLI `up` source-upload (NEW image; `GET /me/mentions` 404→401 route-flip proved the new revision serves). **No new page route** — extends the existing page-9 messaging surface (composer gains @autocomplete; message-row gains mention pills; channel sidebar gains the unread-mention badge). 471 tests (37 shared + 292 api + 142 web) green in CI against real Postgres. Verification at T-9: HTTP/code-level + **live two-client wire-level** (socket.io-client + REST, two DISTINCT verified users A+B) + browser UI pass (bundled-chromium; the Playwright MCP swarm was blocked by an absent chrome channel — T5-F1, worked around). Single-pod in-memory Socket.IO adapter.

| Surface | Status | Note |
|---|---|---|
| Composer @mention autocomplete (page 9) | ✅ Live | NEW. Typing `@`+query surfaces a member-picker (`role=listbox` "Matching members") sourced from `GET /servers/:id/members` (now carrying `username`). Keyboard nav with `aria-activedescendant` on the textarea; Enter selects (does NOT send); Escape dismisses; click-to-select inserts the canonical `@username` token. Live-verified: listbox + option + aria-activedescendant wired; Escape clears. |
| Mention pills (page 9 message list) | ✅ Live | NEW. `@username` tokens render as styled pills via React JSX (XSS-safe, escaped). VIEWER-targeted pill has distinct emphasis (emerald tint `rgba(16,185,129,0.1)` + emerald text `#6ee7b7`, WCAG AA 10.08:1) vs other-user pill (`--surface-700` `#27272a` + white-92, 14.89:1). Non-member/unknown token → plain text, no pill. Tombstoned/edited-out message → no pill. Live-verified at 1440/1280/1024. |
| Unread-mention badge (channel sidebar) | ✅ Live | NEW. Channel row shows an unread-mention count badge; live-verified `general\n4` (count) → `general` (cleared) on opening the channel (markChannelRead). Driven by the realtime `mention` event + `GET /me/mentions` bootstrap. Author NOT self-badged (server-side exclusion). |
| `GET /me/mentions` (api) | ✅ Live | NEW. AuthGuard; `viewerUserId = session.getUserId()` ONLY (no param/path/body — structurally cannot read another user's). Membership-scoped (re-joins server_members on viewer), soft-deleted excluded, cursor-paginated (most-recent-first). Live: unauthed → **401**; B sees only B's mentions; A sees only A's (not A-authored-to-B); `?userId=B` ignored (no IDOR). |
| `message_mentions` data plane (parse/resolve/persist/edit-diff) | ✅ Live | NEW. On create AND edit, `@username` tokens parsed word-boundaried (`(?:^|\s)@([a-zA-Z0-9_-]+)`) and resolved ONLY to server members with a non-null username; unknown/non-member → plain text, no row. Persisted UNIQUE(message_id,mentioned_user_id) ON CONFLICT DO NOTHING (idempotent). Edit removes no-longer-mentioned rows + inserts added (diff). `MessageResponse.mentions[]` round-trips. Live-verified: A@B resolves; @nonmember → []; self-mention persists; edit add→[B] / remove→[]. |
| `/messaging` gateway — per-user room + `mention` event (api, real-time) | ✅ Live | NEW (the B-6 H-1 fix). Every socket joins `user:<userId>` on connect (session-derived; no spoof). `@OnEvent('mention.created')` emits `mention` to `user:<mentionedUserId>` — decoupled from channel rooms, so a mentioned user NOT viewing the channel still gets notified (the original dead-feature). One emit per recipient; author excluded server-side. LIVE two-client: B (NOT in the channel room) received the `mention` event (correct channel+recipient) + 0 `message:new` leak. |

**Access-control note (T-8 verified, @mentions = an authz + scoping surface):** all load-bearing invariants are server-side and live-verified with TWO DISTINCT VERIFIED users — (1) **my-mentions authz** session-derived, IDOR-closed (B sees only B, A sees only A and NOT the message A authored mentioning B, `?userId=` ignored, 401 unauthed), (2) **membership-scoped resolution** (non-member token → plain text, no row, no notify), (3) **two-client realtime ALIVE** (B not in channel room received the per-user `mention` event, 0 channel-room leak — the H-1 dead-feature is genuinely fixed), (4) **author not self-badged** (self-mention → 0 realtime events), (5) **no XSS** (pills render via escaped React JSX). Secret-grep clean; gitleaks passed at C-1. **Coverage gaps → V-2 (non-blocking):** message_mentions real-Postgres integration tier still absent (2-wave carry 02fa8011, MEDIUM — V-2 must issue an explicit disposition); Playwright MCP swarm blocked by chrome-channel-absent (MEDIUM, T5-F1, worked around via bundled chromium). Accepted B-6 carries: M-1 (index ASC), M-2 (client interior-dot tokenizer divergence), M-3 (non-idempotent create re-select), M-4 (edit-diff not transactional), L-1..L-6. The boundary is enforced in NestJS + the gateway room-scoping, not the UI.

## Deployment status — wave-28 (M5-adjacent: invite-code rotation — owner-ONLY leaked-link invalidation, LIVE)
Live: api https://api-production-b93e.up.railway.app/health (200, live-verified at T-9). NO migration (writes existing `servers.invite_code` UNIQUE column in place). PR#41 merge `8996230`. Deployed via Railway CLI `up` (api deployment 48c515e9 SUCCESS; web NOT touched). **No new page route** — extends the existing wave-8 invites surface (page-12 / share-modal); the regenerate-link UI is keep-OUT / demand-gated this wave. Verification: full LIVE authorization matrix (T-8, active) against prod with two persistent verified fixtures + real-PG integration tier (T-4, 7 cases) + 402 unit (T-2).

| Surface | Status | Note |
|---|---|---|
| Invite-code rotate (page 12 / F2, F8) — `POST /servers/:id/invite-code/rotate` | ✅ Live | NEW. **Owner-ONLY** (`server.owner_id !== callerId → 403`; NOT owner-or-creator — a permanent code has no creator; conscious owner-ONLY bypass of the reserved-but-unwired `manage_server` RBAC flag, product-decisions L331-335). callerId session-derived (no IDOR). CSPRNG-regenerates `servers.invite_code` (`randomBytes(16).base64url` ~128-bit) with 23505-retry ≤5 → 409. Returns `{ invite_code }`. **LIVE-verified (T-8, two real fixtures — owner A + non-owner co-member B on proof server ad62cd12):** unauth → **401**; non-owner member → **403**; owner → **201** new-code ≠ old-code base64url; old-code preview **200→404** + old-join **401** (leaked link DEAD, AC2); new-code preview **200** (admits, AC3). CSPRNG unpredictability proven across 5 distinct consecutive rotations. Closes the wave-9 irrevocable-permanent-code gap (d058283d). |

**Access-control note (T-8 verified, invite-code rotation = a credential-rotation authz surface):** owner-ONLY authz is the load-bearing invariant and it HOLDS — proven LIVE against prod with a REAL non-owner verified session getting 403 (not merely the owner getting success), callerId session-derived (IDOR-closed), old leaked link genuinely invalidated (preview 200→404 across the real resolver), new code CSPRNG-unpredictable. No-rate-limit is a DOCUMENTED decision (P-2 keep-OUT), not an omission. Findings → V-2 (both LOW, non-blocking): F28-T8a (spec AC1 "200" vs NestJS @Post 201 — cosmetic, body correct, no client consumer); F28-T8b (403-vs-404 existence oracle — B-6 accepted-debt, spec-conformant AC4, non-secret UUIDs). The boundary is enforced in NestJS (AuthGuard + in-service owner check), not the UI.

## Flows cross-reference

### F1 — Sign up & create profile (P1)
- Entry: `/` → `/signup` (or `/invite/:code` → `/signup`)
- Steps: `/signup` → `/verify` → `/settings/profile` (first-run) → `/app`
- Features: 1, 2, 16

### F2 — Join a study server (P1) — invite preview + verified join LIVE (wave-8)
- Entry: `/invite/:code` (public preview: name + member count only) → (signup/login + email-verify if needed) → `POST /invites/:code/join` (atomic max_uses, idempotent re-join) → `/servers/:id/:channelId`
- Live: public preview (`GET /invites/:code` 200-minimal / 404), verified join gate (401 unauthed / 403 unverified). Authed join success path covered by 179 tests + CI (authed browser flow deferred — fixture 4a2ad286; route e2e gap → V-2).
- Features: 6

### F3 — Real-time messaging (P1) — LIVE (wave-12 conversational core; wave-13 message lifecycle)
- Entry: `/servers/:id/:channelId` → select channel → read (`GET /channels/:id/messages`, keyset load-older) → compose (auto-grow composer, Enter-send, optimistic pending row) → send (`POST /channels/:id/messages`, idempotent) → message arrives in real time for every joined client via the `/messaging` Socket.IO room (`message:new`).
- **Message lifecycle (wave-13, LIVE):** on your own row → **edit** (inline editor → `PATCH /channels/:id/messages/:msgId`, author-only → "(edited)" + `message:updated`); **delete** (author or moderator → `DELETE …/:msgId`, soft-delete tombstone + `message:deleted`); **react** (reaction-pill / add-reaction popover → `POST …/:msgId/reactions`, idempotent toggle + `reaction:added`/`reaction:removed`). All lifecycle changes fan out room-only in real time. Threads NOT built (later milestone).
- **@mentions (wave-15, LIVE):** in the composer, type `@`+query → member-picker autocomplete (keyboard nav, Enter selects without sending) inserts the canonical `@username`. On send/edit, `@username` tokens are parsed + resolved to server members + persisted (`message_mentions`) and round-trip on fetch (`MessageResponse.mentions[]`). Rendered as mention **pills** (viewer-targeted pill has distinct emerald emphasis). A mentioned user gets a realtime `mention` event on their per-user room even when not viewing that channel (the H-1 fix), driving an **unread-mention badge** on the channel row that clears on view. `GET /me/mentions` lists the viewer's mentions (session-derived authz, cursor-paginated, membership-scoped). Non-member tokens stay plain text; self-mentions persist but never self-badge.
- Live: send + list + lifecycle REST gated (ChannelMessageGuard, 401 unauthed / 403 non-permitted, private default-DENY; edit author-only, delete author||moderator with server-side serverId resolve); WS-upgrade auth rejects unauth at connect; room-scoped fan-out (no cross-channel leak). **M3 metric MET LIVE: two-client delivery — message:new 93ms/87ms (wave-12); message:updated 90ms / reaction:added 87ms / message:deleted 112ms (wave-13); all <1s; non-joined client no-leak.** Authed full-browser click-through + cross-user authz live-probe deferred (fixture 4a2ad286, carry-forward).
- Features: 7, 8, 9

### F4 — Voice/video study room (P1)
- Entry: server view → click voice channel → `/servers/:id/voice/:channelId` → **pre-join occupancy indicator (who's-in-room, wave-32)** → **Join voice** (connect-on-demand, not auto) → in-room audio → mic mute/unmute → **share screen (wave-34, LIVE)** → Leave
- **LiveKit connection is now LIVE (wave-34):** founder supplied the LiveKit keys this session, so voice ACTUALLY CONNECTS end-to-end for the first time — two real participants reached the LiveKit SFU (`wss://claudomat-test-sgf9259q.livekit.cloud`, server-corroborated), where waves 31/32/33 could only run credential-blocked fail-soft paths. In-room audio + mic mute/unmute + own-presence are now on a live media plane.
- **Adopted design (wave-31, audio-first first slice):** `design/voice-study-room.html` — 5 states (pre-join / connecting / in-room populated / in-room alone / error). Audio-first avatar-name tiles (camera OFF default, no video), mic toggle + Leave, own presence shown. LiveKit token minted server-side (`POST /channels/:channelId/voice/token`, session + membership gated).
- **Screen-share (wave-34, LIVE — PROVEN 2-participant):** a member in the room can start sharing their screen (browser-native picker); the screen-share track publishes to the LiveKit SFU and other members subscribe + render it as a DISTINCT, PROMINENT tile (`design/screen-share-tile.html`; `max-w-[1000px]` centered, "LIVE SHARE" emerald pill, "Presenting" label, avatar/participant chips demoted to a strip below); stopping the share unpublishes cleanly and the layout reverts (no orphan tile). **LOAD-BEARING PROVEN LIVE (T-5, ceo NON-NEGOTIABLE):** two DISTINCT prod users (A `21984eb2` sender + B `da74148e` receiver) in one SFU room `RM_6mXtfPMddChR` — server-truth `RoomServiceClient.listParticipants`=2; A's SFU track set gained `SCREEN_SHARE/VIDEO` on share and lost it on stop (grant accepted at the SFU, not just in the JWT); B rendered the prominent tile + reverted with no orphan. Token grant WIDENED at the API (`voice-token.service.ts` `canPublishSources=[microphone, screen_share, screen_share_audio]`, no camera) — a client-only publish is NOT server-rejected; same member gate mints (WHO can mint unchanged). This MOVED out of the F4 KEEP-OUT/deferred list into the live in-room flow.
- **Audio-only / low-bandwidth downgrade (wave-34, SHIPPED but NOT USER-REACHABLE — HIGH finding → V-2):** the audio-only fallback (unsubscribe inbound video while audio continues uninterrupted, with a calm banner `design/audio-only-state.html` + restore affordance) is BUILT and unit-tested (hook + `AudioOnlyBanner` component), and the audio-never-dropped invariant is PROVEN LIVE (mic present at every step, fallback code structurally never touches audio subscriptions). BUT it has **NO user-reachable trigger in the live build**: `enterManual()` exists yet is not destructured/wired to any control-cluster button (`VoiceStudyRoom.tsx:412`; live cluster is exactly `[Mute mic, Share screen, Leave]`), and the auto `ConnectionQuality→Poor` path lives on the non-headless LiveKit media plane. So spec-2 AC1 ("opts in via a manual toggle") is BOTH unverifiable-live AND un-invokable by a real user — the banner + hook are dead UI this wave. Filed HIGH → V-2 (head-verifier adjudicates whether spec-2 AC1 is MET-vs-partial before M6 closes; fix = wire the toggle, a small B-3 add, then re-run S3). This MOVED out of the F4 KEEP-OUT list to reflect what shipped, annotated with its honest not-yet-reachable state.
- **Pre-join occupancy indicator (wave-32, LIVE — fail-soft; populated state credential-gated):** on the pre-join surface the who's-in-room indicator polls `GET /channels/:channelId/voice/participants` (session + membership gated, `{count, participants[]}`) and renders per `design/voice-occupancy-indicator.html` (4 states: Loading / Populated / Empty / Error). **Fail-soft is the load-bearing invariant and it HOLDS live:** with `LIVEKIT_*` unset in prod the endpoint 503s → the indicator degrades to the calm "Occupancy data currently unavailable" chip AND the **Join voice CTA stays visible + enabled** (broken occupancy telemetry never gates the core action — proven live at T-5 S3, screenshot-backed). Clicking Join with creds unset degrades gracefully to the design's Error state ("Couldn't connect… / Try again"), shell intact, no white screen. **Credential-gated state (honest boundary):** the indicator SHIPS and its Error/fail-soft state is live-verified; the **populated** occupancy (real LiveKit participant names/avatars, Empty vs Populated) needs `LIVEKIT_*` keys to render live and is deferred to founder-supplies-keys — the RBAC/type/creds-guard **security** surface is fully proven live without keys (see access-control note below). **Malformed-UUID robustness (wave-33, LIVE):** a non-UUID `channelId` on this authed path now returns **400** (was 500) via the project-wide `22P02`→`BadRequestException` global filter — see the access-control note below. N-1 tripwire stands: 3rd cred-blocked M6 wave → park-or-key fork.
- **Deferred to later M6 waves (KEEP-OUT):** camera/video grid, speaking/voice-presence rings, reconnection UI, multi-room switching, screen-share annotation/drawing, multi-share grid beyond the design's one-prominent-share behavior, quality/resolution selector, recording. (Screen-share and low-bandwidth/audio-only downgrade MOVED OUT of this list into the live in-room flow above — screen-share PROVEN LIVE; audio-only shipped-but-not-user-reachable, HIGH → V-2.)
- **Screen-share access-control + security (T-8 verified LIVE, wave-34):** the capability widening is proven live in the decoded minted JWT (`canPublishSources=[microphone, screen_share, screen_share_audio]`, NO camera, room-scoped to the requested channel, sub bound to caller, 1h TTL) — the token SHAPE is unchanged (`{token,url}`), only the grant capability widened. The widening did NOT change WHO can mint: auth matrix intact live — unauth → **401**, malformed non-UUID `:channelId` → **400** (not 500), missing/non-member channel → **403**, and the load-bearing IDOR pair on a REAL private voice channel returns **403** for the authed non-member vs **200** for the allowed member. The wave-31 uniform-403 default-deny holds. `LIVEKIT_API_SECRET` stays server-side (api-scope only; secret-grep clean, no committed value).
- **Access-control (T-8 verified LIVE, wave-32, occupancy = a membership-gated cross-server read):** `GET /channels/:channelId/voice/participants` is auth + membership gated with the gate order proven live (canViewChannelById → **403 FIRST**; non-voice channel → **400**; member on voice with creds unset → **503**, not 500). The load-bearing enumeration control HOLDS — a non-member gets a **byte-identical 403 body** whether the channel exists, is a nil UUID, or a random valid UUID (no cross-server occupancy enumeration leak; wave-31 uniform-403 fix holds). Rate limit active (1×503→29×429, no state leak). LiveKit secret is server-side only (no `VITE_`/`LIVEKIT_*` in the web bundle; Railway api carries no `LIVEKIT_*`). F-32-T-8-1 (from wave-32) is **RESOLVED (wave-33, verified LIVE):** malformed non-UUID `channelId` on the authed path now returns **400** (generic `{"statusCode":400,"message":"Bad Request"}`, no leak) — not the prior 500. The root-cause fix is a global exception filter mapping Postgres `22P02` (invalid-uuid cast) → `BadRequestException`, applied project-wide across all UUID-typed route params (not a per-route `ParseUUIDPipe` sweep); proven live on voice AND non-voice routes, with the auth boundary (401 guard-first / uniform-403 / 503 creds-guard) unchanged. The boundary is enforced in NestJS, not the UI.
- Features: 13

### F5 — Offline-first (P1) — the wedge — LIVE (wave-20 spine; wave-21 live connection-state + multi-page catch-up)
- Entry: any authed surface on connection drop → read cache (Dexie read-through) → compose → durable outbox (Dexie) → reconnect → outbox drains exactly-once + in-order → catch-up loops forward via `?after=` cursor until null (recovers ALL missed messages, no data loss past page 1)
- **Live connection-state (wave-21):** the app-shell ConnectionStateIndicator reflects REAL socket state (online / reconnecting / offline) via source-priority (socket-authoritative); state in text not color alone, `role=status` + `aria-live`. Replaces wave-20's hardcoded "online".
- Live: web deployed; invariants (honest signal + no-data-loss multi-page catch-up) proven deterministically via fake-indexeddb + CI (live two-client offline round-trip deferred — Playwright chrome-absent KI). M4 multi-wave (re-homed tech-debt remains).
- **Offline read-cache surface coverage (wave-62 M12):** the Dexie read-through cache now covers **DMs** (conversation list + thread history) in addition to channels — the DM surface was blank offline before wave-62 and now renders from cache. LIVE-verified on deployed prod (see F11 § Offline read-cache). This is the first M12 offline-first-moat slice — the untouched differentiator half of the founding bet.
- Features: 12

### F6 — View & track assignments (P1)
- Entry: `/servers/:id/assignments` → list → mark to-do/done → **automated due-date reminder email (LIVE wave-30)**
- **Reminder touchpoint (LIVE wave-30, M5 headline):** a backend hourly `@Cron` scan (NotificationsModule / ReminderScanService) finds assignments due within the next 24h (strictly future — past-due excluded), and emails every server member who has NOT marked the assignment `done` (LEFT JOIN `assignment_status`, `IS DISTINCT FROM 'done'` → no-status members ARE reminded). One email per member per assignment (send-once via `assignment_reminder` UNIQUE(assignment_id,user_id), migration 0013). No app screen/route — the touchpoint is the transactional Resend email (subject `Reminder: "<title>" is due soon`; body exposes only title / UTC due date / server name). Recipients are server-scoped (no cross-server leak). This was DEFERRED wave-15→wave-29 (Resend key pending); now LIVE.
- Features: 15

### F7 — Create server + channels (P2)
- Entry: `/app` → Create server (+ in rail) → single-step name modal → `POST /servers {name}` (seeds owner membership + default "General" category + `#general` server-side) → new server selected in rail, `#general` shown. (M2 STRUCTURE only; icon/template/invite are later milestones.)
- Features: 5
- **E2E coverage (wave-16):** authed Playwright happy-path E2E live in CI (`e2e` job, 4/4 vs prod) — the first authed browser test for this flow.

### F8 — Invite + roles/permissions (P2) — invite lifecycle LIVE (wave-8/9); roles/permissions LIVE (wave-10 RBAC capstone)
- Entry (invite, LIVE): server view → invite-share modal (copy link, defaults to permanent per-server `invite_code` on open — wave-9 8b) OR `POST /servers/:id/invites` (member-gated, CSPRNG `{code,url}`) → shareable `/invite/:code`. Two-tier: permanent per-server `invite_code` + ad-hoc max_uses invites. **Revoke (wave-9): owner-or-creator → `POST /invites/:code/revoke` → revoked invite 404s on preview + join.** Lifecycle complete: create → join → revoke.
- Entry (roles/permissions, LIVE wave-10): server view → channel-sidebar "Server settings — Roles" button → Roles Management overlay → create/delete role (4 fixed permission toggles) → assign role to member (`PATCH /servers/:id/members/:userId/role`) → set per-channel visibility (`channel_permission_overrides` via ChannelPermissionGuard). Channel list is now role-gated server-side (non-visible channels absent). Owner-lockout enforced: last-owner demote/remove/leave → 409. `can()` server-side default-deny gates every management op. Invite-code rotation still deferred (d058283d).
- Features: 10, 11

### F9 — Post assignment / pin schedule (P2)
- Entry: `/servers/:id/assignments` → create assignment (title/desc/due) → appears for members → F6 → **due-date reminder email auto-sent within 24h of due (LIVE wave-30)**
- Features: 15

### F10 — Persistent in-app notifications (P1) — LIVE (wave-37)
- Entry: any authed `/app` surface → **header bell** (MainColumn top-bar cluster; emerald unread badge, 9+ cap, `aria-label="Notifications, N unread"`) → click → **notifications panel popover** (app-shell overlay, NO route change — URL stays `/app`) → §113 loading skeleton → list state (mention rows newest-first: actor + "mentioned you in" + #channel + relative-time + excerpt; unread rows emerald-tinted + dot) OR empty state ("You're all caught up") → click an unread row → navigate into channel + `PATCH /me/notifications/:id/read` (bell decrements, panel closes) OR "Mark all as read" → `POST /me/notifications/read-all` (bell → 0).
- **REST:** `GET /me/notifications` (session-derived list + unreadCount, cursor-paginated), `PATCH /me/notifications/:id/read` (owner-only mark-read; cross-owner → 404 anti-existence-leak), `POST /me/notifications/read-all` (bulk mark-read). All 3 session-scoped (no userId param/body — structural IDOR-closed), 401 unauthed guard-first.
- **Realtime:** NO new socket surface — the bell live-increments on the EXISTING `mention` event (`useNotifications` subscribes `onMention`); reminder-type notifications surface on next fetch (NON-GOAL: live-push).
- **Two-read-model coexistence (INTENTIONAL, not a sync bug):** the bell/panel unread count is GLOBAL + server-authoritative (`notifications.read_at` via `useNotifications`); the pre-existing per-channel unread-mention badge (`useMentionBadge`, ChannelSidebar) is a SEPARATE per-channel counter. They are independent subsystems by design and DO diverge (bell=7 vs channel badge=46 live) — deliberate coexistence, not a state-sync defect. `useMentionBadge` untouched this wave.
- Live: bell + 3 endpoints proven live (T-8 owner-404 IDOR + self-scoping + 401 all verbs; T-5 two-client mention flow B→A). Reminder-TYPE row live-exercise deferred (no reminder notifications in prod — Resend-key-blocked, parked a1299e88; mention rows fully exercised; F37-T5-1 LOW).
- Features: 15 (surfaces mention + assignment-reminder notifications)

### F11 — Direct messages (P1) — LIVE (wave-46 M8 slice 1)
- Entry: any authed `/app` surface → server-rail button `aria-label="Direct Messages"` → **DM home** (conversation-list rail + main pane). New-DM affordance → **StartDmPicker** modal → pick recipient(s) by display name (respecting who_can_dm; restricted-target create → inline 403, modal stays open) → confirm "Open DM" (1:1) / "Create Group" (≤10) → thread opens → type in composer (Enter sends) → optimistic render (~11ms) → real-time delivery to the other participant via Socket.IO `dm:message` (no reload) → reply reverses direction. Offline: send → "Sending…" pending → drains on reconnect. States: loading / loaded / empty-list / empty-thread / error / offline-pending / failed.
- **REST:** `POST /dm/conversations` (who_can_dm pre-write, find-or-create 1:1 dedup, cap ≤10), `GET /dm/conversations` (caller-scoped list + last-message preview), `POST /dm/conversations/:id/messages` (participant-gated 404-non-leak, idempotent), `GET /dm/conversations/:id/messages?cursor=` (participant-gated keyset). All session-scoped (callerId from session, never body/param — structural IDOR-closed), 401 unauthed guard-first, 404 non-participant (anti-existence-leak).
- **Realtime:** NEW `dm:message` event on the auth-gated `/messaging` namespace → participant per-user rooms (`user:<userId>`) incl. sender; client dedups by id. Offline via the generalized outbox (kind:dm|channel).
- **Smoke (T-9 / T-5 LIVE, 2026-07-04):** ✅ 2-client fan-out A→B verified with a real on-wire `dm:message` frame at CLIENT B + DOM (no reload), both runs; reply B→A ✅; start-picker + who_can_dm ✅; optimistic + offline→reconnect ✅; 4 DM authz invariants held under pen-test ✅.
- **Known → V-2 (feature works; non-blocking):** F4 HIGH cursor boundary-dup (ms-vs-µs); F6/F7 MAJOR self-message double-render + "Unknown user"; F1/F3 displayName-UUID. See findings-aggregate.
- **Offline read-cache (wave-62 M12 offline-first moat #1, LIVE 2026-07-06):** the DM conversation list + thread history now render from a Dexie v2 offline cache when the network is unreachable (previously blank offline). NO new route/screen/endpoint/component — data-source change only on the existing F11 DM surface (reuses `GET /dm/*`; `CachedDm*` are client types). Substrate: Dexie v2 (`dmConversations`/`dmMessages` + read-through helpers in `apps/web/src/features/sync/cache.ts`), the v1 channel/message/outbox schema re-stated VERBATIM in `.version(2)` (M4 store preserved — byte-compared + 2 v1→v2 preservation unit tests). `useDm` write-through on successful fetch; on fetch-failure falls back to `getCachedDmConversations`/`getCachedDmMessages` so neither surface goes blank. **T-5 LIVE offline probe (deployed prod web-production-bce1a8, fixture A, 2026-07-06):** loaded DM list + `studyhallfixtureb` thread online (write-through: cache = 1 conversation / 28 messages), then `context.setOffline(true)` (transport-level offline PROVEN — `fetch('/api/health')` → `TypeError: Failed to fetch`); reloaded + re-navigated → DM conversation list AND full ordered thread history STILL RENDER from cache (28 msgs, correct order; contrast: the non-cached server-rail shows "Failed to load" = genuine offline), NOT blank. Restored online → health 200. States extended: loading / loaded / **offline-cache-hit** / empty-list / empty-thread / error / offline-pending / failed.
- **Server-blind E2E encryption (wave-79 M13 leg-3a, LIVE 2026-07-08):** 1:1 DMs are now END-TO-END ENCRYPTED (server-blind) with an HONEST fail-closed indicator. NO new route/screen — annotation-only: the DM view gains an encryption indicator + the DM send/receive path now carries a ciphertext envelope; TWO new REST endpoints added. **New REST:** `PUT /profile/encryption-key` (self, AuthGuard — stores/rotates caller's public key: 200 store, 200 rotate/replace, 400 unsupported-algorithm/oversized/empty, 401 unauth); `GET /profile/:userId/encryption-key` (viewer-from-session, **who_can_dm-gated** via DmService.canDm/enforceWhoCanDm seam — NOT profile_visibility: 200 PublicKeyResponse `{userId,publicKey,algorithm,createdAt}` when permitted, **byte-identical uniform 404** for every not-permitted/blocked/nonexistent/no-key/malformed case (no oracle), 401 unauth; self-fetch always permitted; ThrottlerGuard rate-limited). **Encrypted-envelope DM path:** the outgoing `POST /dm/conversations/:id/messages` accepts EITHER plaintext `content` OR an encrypted envelope `{ciphertext, senderKeyRef, envelopeVersion}` (mutually exclusive — server rejects both/neither/partial); when encrypted, `dm_messages.content IS NULL` + ciphertext persisted (SERVER-BLIND — server never decrypts, passes ciphertext through); `GET /dm/conversations` last-message preview shows an "Encrypted message" placeholder for a NULL-content encrypted last message. **Client crypto (web):** first DM use generates an ECDH-P256 keypair via Web-Crypto; the PRIVATE key is a NON-EXTRACTABLE CryptoKey in the `studyhall` Dexie `encryptionKeys` table (id `self`) — never sent (export('pkcs8') throws InvalidAccessError); the public key is registered via PUT; outgoing messages to a keyed peer are AES-GCM-encrypted client-side, incoming envelopes decrypted client-side. Plaintext fallback when the peer has no key (GET→404). **HONEST INDICATOR (ship-blocker, fails closed):** the DM header + per-message carry an encryption indicator (`data-testid` dm-header-encryption-indicator / dm-msg-encryption-indicator) with 6 states matching `design/e2e-indicator.html`; the SOLE lock affordance (`data-testid="e2e-lock-affordance"`, emerald `#10b981` filled shield-check, "End-to-end encrypted") renders ONLY for a provably-encrypted message — every other state (plaintext-fallback=open-lock, group=slashed-shield, cannot-decrypt=key, loading=spinner) shows "Not encrypted"/error with a NON-lock glyph; NEVER a padlock without proof of encryption. **Group DMs (≤10) are OUT OF SCOPE for encryption in leg-3a** (1:1 peer-key model only) → group sends plaintext-fallback + shows NOT-encrypted (never a false lock); group E2E deferred to a future leg. Accepted v1 posture: private-key-browser-only, no key backup, no multi-device, plaintext-fallback for keyless peers; undecryptable history degrades honestly to a calm "cannot decrypt on this device" state. **T-5/T-8 LIVE (deployed prod, fixtures A studyhall-e2e-fixture + B studyhall-e2e-fixture-b, co-members of proof server ad62cd12, 2026-07-08):** two-client — A generated keypair on first use (public registered via PUT, private never on wire), sent `E2E-PROBE-SECRET-K7Qx9M` as a ciphertext envelope (plaintext never on wire); B decrypted + read it; server API returns `content:""`+ciphertext (server-blind LIVE); emerald lock shown ONLY on the encrypted DM (count=1) and 0 on plaintext history / server channels; who_can_dm 404 no-oracle matrix byte-identical live (nobody+key / nonexistent / malformed all `{"message":"Encryption key not found",...,404}`); timing-flat (no F5 oracle); ThrottlerGuard active (F8 resolved). Migrations 0031 (user_encryption_keys, user_id TEXT FK, no private-key column) + 0032 (dm_messages content→nullable + ciphertext/sender_key_ref/envelope_version/deleted_at) applied to prod. Merge `0fa0f5f` (PR #98), api+web deployed SUCCESS. Findings→V-2: F-T5-1 (medium) client-side auth-guard race bounces DM route to `/` on transient DM-load 401 (session cookie still valid, recovers on re-nav); F-T8-1 (info) group/no-key indicator not live-constructible (verified structurally); F-T8-2 (low) server doesn't validate senderKeyRef vs registered key.
- Features: DM (1:1 + small-group ≤10, private messaging; offline-readable list + thread; 1:1 server-blind E2E encryption + honest fail-closed indicator).

### F12 — Discover public communities (P1) — LIVE (wave-67 M11 server discovery; directory empty in prod until publish path ships)
- Entry: authed → server-rail Discover entry (`aria-label="Discover Public Servers"`, active `aria-current="page"`) → `/discover`. Page = ServerRail (unchanged) + full-page directory: sticky glass header (`h1` "Discover Communities" + search `input[aria-label="Search servers"]`) + card grid. Type in search → debounced ILIKE query over name/topic/description. Each result card: name, member count, description, topic chip, dark-on-emerald **Join** button (`aria-label="Join <name>"`). Click Join → `POST /servers/:id/join-public` → member added → button flips to **Open** (`aria-label="Open <name>"`) → server appears in rail. States: loading skeletons / loaded grid / **honest cold-start empty ("No public communities yet")** / search-no-match / error / joining (spinner) / joined (Open).
- **REST:** `GET /servers/discover` (AuthGuard; **is_public=true ONLY** — 566 total servers, only public ones surface; `memberCount`, ILIKE `q` search over name/topic/description, cursor/page pagination; 401 unauthed). `POST /servers/:id/join-public` (AuthGuard; **is_public-gated — a private server is REJECTED 403** "Server is not open for public joining", even for an existing member; 401 unauthed; adds caller to server). Migration 0024 (`servers.is_public`/`description`/`topic`) live.
- **Smoke (T-9 / T-5 + T-8 LIVE, 2026-07-06, fixture A `studyhallfixturea`):** ✅ `/discover` renders live with ServerRail + Discover entry + search box + h1 + **honest empty-state** (prod directory genuinely empty — publish-to-directory write path deferred, M11 bundle 2bd37c4c). ✅ Browse+join proven **END-TO-END, non-echo**: published a fixture server owned by fixture B that A was NOT in → card rendered (name/topic/description) → clicked real Join → **A actually added** (button → "Open", `GET /servers` returns it) → teardown clean. ✅ **is_public filter proven** (only the 1 public server surfaced from 566). ✅ **T-8 LOAD-BEARING** (head-product P-4 NAMED): private server NOT joinable via join-public — **403** on both a member-private AND the private Fixture Proof Server; **401** unauth join + **401** unauth discover; **429** rate-limit active; also CI-verified (servers.service.spec: private join throws Forbidden, insert never reached). ✅ **T-6 layout** dark-only + card grid + **§8 dark-on-emerald Join** (`#0a0a0b` on `#10b981`, ~7.4:1 AA), 0 material divergences.
- **Known → V-2 (feature works; non-blocking):** F67-T5-1 (SIGNIFICANT) `GET /servers/discover` returns `memberCount:0` for every server (0 with 1 member AND 2 members; raw DB count correct) — cards always understate membership, server-side aggregation defect; F67-T5-2 (LOW/MED) `POST /servers/:id/join-public` creates the member row with `role_id=NULL` (owner has a proper default role) — confirm role-less-member RBAC intent. See findings-aggregate.
- Features: public server discovery (searchable directory + gated public-join).

## Orphan / reachability audit (v4 step 4)
- Every MVP feature (1–16) consumed by ≥1 page. ✓
- Every page connects to ≥1 flow (stubs 2,3 reachable from Landing footer). ✓
- DMs now LIVE (M8 slice 1, wave-46) — DM home reachable from the `/app` server-rail "Direct Messages" button; F11 smoke-asserted. ✓ (was H2-deferred; promoted to MVP-present in M8.)
- Discover now LIVE (M11 server discovery, wave-67) — page-17 `/discover` reachable from the server-rail Discover entry (`aria-label="Discover Public Servers"`); F12 smoke-asserted live (browse+join E2E + is_public join-gate). ✓ Note: prod directory renders the honest empty-state until the publish-to-directory write path ships (deferred M11 bundle 2bd37c4c) — reachability of the populated grid is code+DB-proven (T-5 published-fixture probe), not yet organically reachable in prod.

## Per-page PDs
`command-center/product/per-page-pd/<page>.md` — full PDs for pages 1, 4–16; stubs for `privacy.md`, `terms.md`. Links resolve after v4 step 3 fan-out.

---

## Regeneration cadence

Rebuild at **T-9 Journey** from current production state cross-referenced with `design/` mockups. See `claudomat-brain/blocks/test/stages/T-9-journey.md` and `command-center/testing/test-writing-principles.md`.


## Wave-18 — M3 thread replies (LIVE)
Web/api live (200, T-9-verified). Migration 0008_dazzling_bushwacker.sql (messages.thread_parent_id self-FK + reply_count NOT NULL DEFAULT 0 + last_reply_at + index messages_thread_parent_created_idx) applied to prod before new code served (ledger 8→9, C-2). PR#30 merge 16c72b6; api ce25ddc2 + web 594b0bdc SUCCESS. **No new page route** — extends page-9 / F3: message-row gains an in-list thread affordance (reply count + last-reply, hidden when reply_count==0) opening a thread panel (parent pinned + replies oldest-first + composer) rendered as a ≤1024px fixed role="dialog" overlay with focus-trap; reply send reuses the top-level optimistic outbox. NEW REST: POST /messages/:parentId/replies?channelId= + GET /messages/:parentId/replies (one-level; reply-of-reply/cross-channel rejected; tombstone-excluded; keyset ASC). NEW realtime: thread:reply:created / thread:reply:deleted over /messaging channel rooms. **LOAD-BEARING-PROVEN live two-client (A+B co-members):** B (joined non-author) received thread:reply:created (payload match); non-joined socket received nothing (room-scoped no-leak); thread:reply:deleted decremented reply_count to 0 live. **Access-control (T-8):** IDOR fix parent-derived — createReply + listThreadReplies authz on canViewChannelById(userId, parent.channel_id); ?channelId= can't bypass (cross-channel → 400 first); non-member 403 (3 IDOR unit tests); live 401 on all thread routes. Perf: denormalized reply_count/last_reply_at (no count-on-read N+1) + covering index. **Coverage gaps → V-2 (non-blocking):** F-2 (thread Zod safeParse units), F-4 (real-PG thread integration spec — wave-17 pg-harness makes it cheap), F-3 (affordance test-id nit). Carries: 9 pre-existing biome warnings (4e994e96); Playwright chrome-channel-blocked (realtime via socket.io wire probe); prod test-data accumulation (no DELETE /servers/:id). Fixtures: studyhallfixturea + studyhallfixtureb.


## Wave-19 — M3 file/image attachments (LIVE) — M3 SUCCESS METRIC FULLY MET
Web/api live (T-9-verified). Migration 0009 (attachments table: message_id NOT NULL FK→messages CASCADE, uploader_id, channel_id, object_key, filename, content_type, size_bytes; index message_id) applied to prod before new code served (ledger 9→10). PR#31 merge dbf6b25; api 8ef2c228 + web 8d3e0c36 SUCCESS. **No new page route** — extends page-9 / F3: composer gains a paperclip attach button + staged-preview strip (image-thumb/file-chip tiles, remove, upload progress, error tile); message row renders attachments — inline constrained image preview (max-h-320, click → lightbox dialog w/ Esc/backdrop/focus-trap) + file chip (icon+name+size, download) + broken-image fallback chip; 0-N; tombstone-safe. Reuses the existing FilesService + Railway Buckets (avatars). NEW REST: POST /channels/:channelId/attachments/{presign,confirm}; message-send body += attachments[] (ValidatedAttachment descriptors). Upload flow: presign → client-direct PUT → confirm (HeadObject ≤10MB + content-type allowlist) → send-with-attachments. **Access-control + integrity (T-8 ratified):** send-time validateAndHeadAttachments re-derives size+type from server HeadObject (client claims discarded) + anchored channel-key regex ^attachments/<channelId>/... → CLOSES cross-channel key-swap IDOR + path-traversal + ≤10MB size-bypass + content-type spoof; presign/confirm channel-membership-gated (non-member 403, rule-4); persisted row = server-derived values; live presign/confirm 401 unauth. Render url = presigned-GET (Railway Buckets private). Perf: presigned-GET batch-resolved (no N+1), client-direct upload. **M3 success metric NOW FULLY MET: reactions ✓ + threads ✓ + attachments ✓.** Coverage gaps → V-2 (non-blocking): F-1 (no integration/e2e for wired path → 02fa8011 harness), F-2 (live two-client upload not run — Playwright chrome absent), F-3..F-7 (lows). Carries: 9 pre-existing biome warnings (4e994e96); C-block CI lessons (gate-on-per-job-conclusions 3rd-instance, B-5 local-vs-CI divergence, flaky server-roles test) → L-2; prod test-data accumulation. Fixtures: studyhallfixturea + studyhallfixtureb.


## Wave-20 — M4 offline-first SPINE (the wedge — first slice, LIVE)
Web/api live (T-9-verified: api /health 200, web / 200, GET ?after= unauthed 401-not-404). NO migration (Dexie client-side IndexedDB; forward cursor = keyset query; drizzle ledger unchanged, highest 0009). PR#32 merge bff9f12; api d26fe078 + web 2aac8438 deployed SUCCESS (new revisions distinct from baselines). **No new page route** — extends page-9 / F5: the existing composer is now offline-enabled (durable Dexie outbox BACKS the M3 optimistic pending/failed states, no separate path); reads served read-through from a Dexie cache when offline; on reconnect the outbox drains EXACTLY-ONCE + IN-ORDER then catches up via the forward cursor. NEW REST: GET /channels/:id/messages?after=<cursor>&limit= (ASC forward keyset; malformed→400; non-member 403; ?after= precedence over ?cursor=). NEW client: StudyHallDB (messages [channelId+createdAt] / channels / outbox [state+createdAt]; lazy/guarded singleton + injectable IDBFactory) + read-through cache + durable outbox (enqueue stable-key-once / drain sequential oldest-first stop-on-failure / retry). **WEDGE PROVEN (exactly-once + in-order) via fake-indexeddb (per-test IDBFactory, no real timers):** stable idempotencyKey once-at-enqueue replayed verbatim; sequential oldest-first drain; stop-on-failure (a later message NEVER sends ahead of an earlier un-sent one — key2PostCount=0 across 3 drains); concurrent-drain re-entrancy guard; id-tiebreak; server ON CONFLICT(channel_id,idempotency_key) dedup (idempotency-lock). 10 outbox + 15 db-unit + 6 forward-cursor + 2 idempotency-lock + wave-17 real-PG harness. api 346 + web 176 green. **Access-control (T-8):** ?after= shares the single @Get @UseGuards(AuthGuard, ChannelMessageGuard) — route-agnostic default-deny, channelId from route params only (IDOR-safe); genuine guard.spec non-member 403 + body-vs-param IDOR-closed; tautological test deleted; live 401. Dexie store client-local (no new server authz surface). **M4 MULTI-WAVE — this is wave 1 (spine).** OUT (2nd M4 wave): connection-state indicator, pending/failed UI polish, catch-up UI + pagination loop. Gaps → V-2 (non-blocking): M1 (POST-succeeds-delete-fails window untested — server dedups), M3 (catch-up one-page no-loop — 2nd-wave scope). Carries: L1-L4, 9 biome warnings, 6 re-homed M3 tech-debt (M4 backlog), Playwright chrome-absent. Fixtures: studyhallfixturea + studyhallfixtureb.


## Wave-21 — M4 offline UX wave-2 (live connection-state + multi-page catch-up loop, LIVE)
Web live (T-9-verified via CI + C-2: web deploy 032dc384 SUCCESS, baseline 2aac8438 advanced, web root 200; api NOT redeployed — no api diff; NO migration, drizzle ledger unchanged at 0009). PR#33 merge 9c48007 (squash; 7/7 CI jobs success). FRONTEND-ONLY (apps/web). Canary SKIPPED (DAU<1000). **No new page route, no new REST/realtime surface** — annotation-only regen extending page-9 / F5 (the wave-20 "OUT" items now shipped). Two user-observable changes:

1. **Connection-state indicator now reflects REAL socket state** (was hardcoded `"online"` in wave-20). The ConnectionStateIndicator on the app shell now derives online / reconnecting / offline from a live hook (`useConnectionState`) using SOURCE-PRIORITY (socket-authoritative over navigator); state is carried in TEXT not color alone — `online` (sr-only) / `reconnecting` (amber spinner + "Reconnecting…") / `offline` (danger dot + "Offline — messages will send when you're back"), `role=status` + `aria-live`, no `getByTestId`.
2. **Reconnect catch-up now LOOPS** (was single-page in wave-20) — a multi-page offline window now fully drains via the `?after=` forward cursor: on reconnect the outbox drains exactly-once + in-order, then catch-up pages forward until the server returns null, recovering ALL missed messages past page 1 (no data loss).

**LOAD-BEARING INVARIANTS PROVEN (mutation-sanity met, not theater):**
- **Honest connectivity signal** — `useConnectionState.test.tsx` D1/D2 exercise source-priority + socket-authoritative as transition cases; the AppHome wiring test asserts "Reconnecting…"/"Offline" render from the LIVE hook (would fail if still hardcoded "online").
- **No-data-loss multi-page catch-up** — `multiPageCatchup.test.ts` against fake-indexeddb (real Dexie, per-test IDBFactory, no real timers): 3-page in-order recovery, dedup-by-id exactly-once, terminate-on-null, MAX_ITERS bound preserves all 100 pages. T-4 RATIFIED real (consumes the wave-20 `?after=` cursor, no mocked SUT).

Tests: CI run 28475903958 lint+typecheck+test+secret-scan all success on merge SHA; web 193 EXECUTED (incl multiPageCatchup.test.ts + useConnectionState.test.tsx, confirmed not skipped-but-green), api 346 unchanged. **T-9 gate APPROVED — 0 Critical, 0 High.** Gaps → V-2 (non-blocking): M1 (concurrent double catch-up loop on overlapping reconnect — dedup-safe, perf-only 2x round-trips), M2 (fire-and-forget cache write-through — happy-path masked, reload re-seeds), M3 (online-while-reconnecting — subsumed by M1), L1 (SSR default-online — inert, client-only), L2 (resume-after-mid-loop-failure proven by code+server-contract reasoning, not an executing test — test-completeness gap, shipped no-loss IS executing-test-proven via MAX_ITERS), L3 (socket.io v4 manager-event listeners may not fire — no leak, intent served by disconnect + getSocketState().active). Carries: 9 pre-existing biome warnings, 6 re-homed M3 tech-debt (M4 backlog), Playwright chrome-absent (live two-client offline round-trip deferred — invariants proven deterministically via fake-indexeddb + CI e2e). **M4 stays multi-wave** (re-homed tech-debt remains). Fixtures: studyhallfixturea + studyhallfixtureb.


## Wave-22 — M5 assignments (academic tooling OPENED, LIVE)
Web/api live (T-9-verified: api /health 200, web / 200, GET /servers/:id/assignments unauthed 401-not-404). Migration 0010 (assignments + assignment_status[UNIQUE(assignment_id,user_id)] + assignment_attachments; FKs CASCADE) applied to prod before new code served (ledger 10→11). PR#34 merge 108f4a3; api 7ffaeaea + web 66f4c715 SUCCESS. **Page 14 "Assignments panel" /servers/:id/assignments (F6/F9) now BUILT/LIVE** — organizer (server owner OR can(manage_channels)) posts an assignment (title/desc/due/optional-attachment), members view due-sorted alongside chat + mark a PERSONAL to-do/done; assignment-card with amber 'due soon' (<48h) + red 'overdue' (--danger-text #f87171) chips, per-member checkbox toggle. NEW REST: POST/GET /servers/:serverId/assignments, GET/PATCH/DELETE /assignments/:id, PUT /assignments/:id/status, assignment attachment presign. **Access-control (T-8 RATIFIED, multi-tenant):** organizer authz = rbac can(userId, serverId, manage_channels) single call site (non-organizer post/edit/delete → 403; manage_assignments dedicated flag = G2 follow-on); per-member status isolation (toggleStatus inserts SESSION user_id, AssignmentStatusSchema has no user_id field — A can't set B's status); non-member list → 403; /assignments/:id IDOR-safe (server_id from the row); the cross-server attachment-key IDOR was caught by /review + fixed (anchored ^attachments/<serverId>/ key, row/route-derived). Soft-delete HIDES (is_deleted=false on all read paths), status rows preserved not cascaded. New token --danger-text #f87171 (6.30:1, D-3 contrast fix) + --text-muted 0.55. **M5 academic tooling OPENED — multi-wave:** reminders (cron+Resend, deferred) + further academic tooling remain. Coverage gaps → V-2 (non-blocking): F22-T-1 controller-IDOR-assertion test-debt, F22-T-3 rowToDto N+1 (0-user scale). Carries: client organizer-CTA owner-only (→ /me/roles), 9 biome warnings, biome-format-drift CI lesson, Playwright chrome-absent. Fixtures: studyhallfixturea + studyhallfixtureb.


## Wave-35 — M7 privacy controls (enforced profile-visibility + data view/download + stubs, LIVE)
Web/api live (T-9-verified on deploy 0c71585: api /health 200; web /, /login, /signup, /app, /settings/privacy, /privacy, /terms all 200; /profile/privacy + /profile/data + /profile/data/export unauthed 401-not-404). Migration 0014 applied to prod. PR #49 merge 0c71585. **THREE routes flip aspirational→LIVE (all pre-inventoried in the page table, so routes_added is a status flip, not new rows):** page-16 `/settings/privacy` (F1, new product surface), page-2 `/privacy` + page-3 `/terms` (compliance stubs, now serving real content).

| Surface | Status | Note |
|---|---|---|
| User settings — privacy (page 16) — `/settings/privacy` | ✅ Live | NEW. Renders "Privacy Settings" + honesty statement, an HONEST profile-visibility control (exactly 2 enabled radios: `everyone`="Visible to classmates" / `nobody`="Hidden"; auto-saved, no Save button), **a REAL enabled "Show my online status" presence toggle (wave-80, LIVE)** — `role=switch`, ENABLED (aria-disabled null, pointer-events auto, opacity 1), emerald-on `#10b981` / grey-off `#52525b` DS tokens, binary online/offline copy ("Turn it off to appear offline to everyone" — NO last-seen framing per P-4), auto-saves via PUT /profile/privacy, GET hydrates + defaults true. NOT the disabled whoCanDm-Beta affordance — presence is a live feature so the toggle actually works. The "Your data" account-data section (real profile + membership summary) with "Download my data", and a DISABLED who-can-DM affordance (BETA badge, `aria-disabled`, 0 enabled inputs — NOT a working toggle). **Presence toggle honor PROVEN LIVE (wave-80 T-5 two-client): A toggles OFF → co-member B sees A offline in ~126ms WITHOUT A reconnecting (proactive server-side emit); ON → B sees A online. Own-visibility-only (hidden A still sees co-members). Audit event written (privacy_settings_changed w/ showPresenceFrom/To, no PII).** T-9 authed crawl: profile-visibility radios [everyone,nobody], 1 enabled presence switch [showPresence], disabled DM block, download button, own-email account data. |
| Privacy policy (page 2) — `/privacy` | ✅ Live | NEW-LIVE public stub (no auth). T-9 crawl: 200, h1 "Privacy Policy", 1413 chars real stub content. Reached from landing footer (`/privacy` href). Cosmetic LOW (pre-existing, not wave-35): "Last updated: 2024". |
| Terms (page 3) — `/terms` | ✅ Live | NEW-LIVE public stub (no auth). T-9 crawl: 200, h1 "Terms of Service", 1758 chars. Reached from landing footer (`/terms` href). |
| Profile-visibility enforcement — `GET /servers/:id/members` | ✅ Live | Roster now filtered SERVER-SIDE by each subject's `profileVisibility`: `nobody` → subject ABSENT from every OTHER member's roster while still seeing self; `server-members` → co-member-visible; `everyone` → visible. Enforced, not a client hide. |
| `GET/PUT /profile/privacy` (api) | ✅ Live | AuthGuard; session-derived. GET returns `{profileVisibility, whoCanDm, showPresence}` (showPresence added wave-80, migration 0033 `users.show_presence` boolean NOT NULL DEFAULT true). PUT is now **PARTIAL** (`UpdatePrivacySchema.partial()` — B-6 F1): a client sends ONLY the changed field; the service merges present keys onto current DB state (no cross-tab clobber — visibility-only PUT preserves showPresence, LIVE-verified wave-80 T-8). Validates enums + `showPresence: boolean` → 400 zod on bad value (no write). Live 401 unauthed. Known LOW (wave-80 F-T3-1): unknown keys stripped+200, not rejected+400 (mass-assignment safe). |
| `GET /profile/data` + `GET /profile/data/export` (api) | ✅ Live | Structurally session-scoped — no userId path/query param changes the subject; `?userId=<other>` silently ignored (no IDOR). Export → self-only `studyhall-account-data.json` (profile/memberships/activitySummary). Live 401 unauthed. |
| Account deletion — Danger Zone (page 16, `/settings/privacy`) | ✅ Live (wave-72) | NEW. "Delete your account" Danger-Zone section (danger `#b91c1c`) → acknowledgment-gated confirm dialog (portaled, focus-trap): destructive confirm DISABLED until the "I understand my account will be deactivated and my personal data removed" checkbox is checked. On confirm → `POST /profile/delete` → on success sign out + redirect to `/login`; on owner-block 409 the dialog surfaces "Transfer or delete the servers you own…" + the blocking server list, non-destructive (no logout). Copy reconciled to SOFT-delete (no email-verify / 30-day-grace / permanent-purge promise). T-5/T-6 verified live (Fixture A owner-block path). |
| `POST /profile/delete` (api) — right-to-erasure | ✅ Live (wave-72) | AuthGuard; callerId ALWAYS from session (no userId param → no-IDOR). Body `{confirm:true}` (400 zod otherwise). SOFT-delete: owner-of-any-server → **409** blocked (server list; NOT deleted); else atomic SERIALIZABLE txn scrubs PII (display_name→"Deleted user", username→null, email→`deleted+<id>@deleted.invalid`, avatar_url/**avatar_key**→null) + sets `users.deleted_at` + leaves all servers; then best-effort session-revoke. Reversible (no hard SuperTokens delete). Live 401 unauthed. |
| Privacy activity audit log (page 16, `/settings/privacy`) | ✅ Live (wave-73) | NEW. "Your privacy activity" read-only panel: a plain-language reverse-chron list of the caller's OWN privacy events (account deletion / data export / privacy-settings change with visibility from→to / block / unblock) with relative timestamps; loading skeleton, empty state, error+retry. Own-scoped (no-IDOR). T-5 live-verified the hook→endpoint→panel path end-to-end (an event appeared after a real settings change). |
| `GET /profile/privacy-events` (api) | ✅ Live (wave-73) | AuthGuard; session-only callerId (NO userId param → no-IDOR — T-8 live-verified Fixture A + B each see only their own events); returns the caller's PrivacyEventListResponse (own events, created_at DESC, limit 100). Live 401 unauthed. |
| Privacy-events audit log — write hooks (api) | ✅ Live (wave-73) | Append-only `privacy_events` table (migration 0028) + AppendPrivacyEvent service. NON-BLOCKING best-effort after-commit write hooks at 4 shipped seams: account deletion (account_deleted), data export (data_exported), privacy-settings change (privacy_settings_changed, context {visibilityFrom/To, whoCanDmFrom/To}), block/unblock (user_blocked/user_unblocked). A logging failure NEVER fails/rolls back the user action. False-event gated (idempotent block/unblock + no-op settings do NOT log). Context is PII-free (enum values + ids only — T-8 verified). Regime-independent M10 leg. |
| Re-auth block for deleted accounts (both doors) | ✅ Live (wave-72) | A deleted user (`deleted_at IS NOT NULL`) CANNOT re-authenticate through EITHER door: (i) SuperTokens `signIn` override → `WRONG_CREDENTIALS_ERROR`; (ii) `getSession`/`refreshSession` overrides → `UNAUTHORISED`. Covers fresh login, existing access token, refresh rotation, AND the WebSocket upgrade path (uses `getSession`). T-8 live-verified all three vectors reject. |
| Sentry observability | ✅ Live | Backend + frontend error monitoring wired with `beforeSend` PII scrub. No user-visible route/screen. |

**Access-control note (T-8 verified LIVE, two verified fixtures A+B co-members of ad62cd12 — this wave's core promise is ENFORCED privacy):** CRITICAL#1 roster-hiding proven live — A=`nobody` → B's roster count 1 (A absent) while A's own roster count 2 (self-present); A=`server-members` → co-member B sees A; A=`everyone` → restored. CRITICAL#2 data-export IDOR-safe — `?userId=<B>` ignored on both `/profile/data` and `/profile/data/export`, always caller's own data. Auth boundary 401 (unauth + malformed bearer; control 404 confirms genuine guard). PUT bad enum → 400 zod (stored value unchanged). Roster rows omit `email` (keys exactly `[avatarUrl,displayName,userId,username]`). 0 CRITICAL/HIGH. The boundary is enforced in NestJS + the roster query filter, not the UI.

**Account-erasure note (wave-72, T-9 verified LIVE on deploy 69ad79b — the symmetric right-to-ERASURE half of the shipped right-to-ACCESS):** account self-deletion is LIVE. T-8 live probes (Fixture A + fresh throwaway): no-IDOR (`POST /profile/delete` unauth → 401, userId body ignored); owner-block 409 (Fixture A blocked + intact); erasure (throwaway → 200 `{status:'deleted'}`); **BOTH re-auth doors reject the deleted user** — signIn → `WRONG_CREDENTIALS_ERROR`, pre-deletion access token → 401, pre-deletion refresh token → 401. Secret grep 0; rate-limit active. Erasure is atomic (SERIALIZABLE txn) + reversible (soft-delete, no hard SuperTokens delete). **P0 resolved this wave:** an earlier deploy (e5bfba1) white-screened the SPA (`require is not defined` — a CJS `@studyhall/shared` barrel shipped raw `require()` into the browser bundle via a namespace-import workaround); fixed forward (shared now emits ESM, PR #89 / 69ad79b), redeployed, prod re-verified rendering (bundle has zero `require()`), T-5/T-6 re-ran green. Non-blocking → V-2: MEDIUM session-token-storage (pre-existing header-mode tokens, app-wide, not a wave-72 regression), LOW service-worker-stale-bundle-once-on-return, cosmetics. Fixtures: studyhall-e2e-fixture (A, owns Fixture Proof Server) + a self-deleting throwaway.

**routes_added (wave-72):** none (Danger Zone lives within the existing `/settings/privacy`); NEW endpoint `POST /profile/delete`.

**routes_added:** `/settings/privacy`, `/privacy`, `/terms` (all pre-inventoried; status flipped to LIVE). **routes_removed:** none. **regressions:** none — T-9 crawl independently re-confirmed landing / login / signup / authed /app shell intact on deploy 0c71585. **Coverage gap → V-2 (MEDIUM, honest debt, non-blocking):** NO dedicated automated tests for the new privacy endpoints (behavior proven at the live layer T-8 + code-read B-6; regression test absent — candidate follow-up test task). Honest N/A: notifications-panel surface does not exist → states AC N/A. Fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B).

---
## Direct Messages (M8 — LIVE + now STARTABLE via UI, T-9-verified against shipped prod)
**last_updated_wave47: 2026-07-04 (T-9 wave-47 — M8 DM entry-point completion: DMs are now genuinely STARTABLE through the real Start-DM picker UI. RESOLVES wave-46 F-A CRITICAL (DMs shipped-but-unstartable-via-UI) + wave-46 F7 ("Unknown user" author). NO new route/screen — annotation-update: the DM screen/flow already existed; this wave added ONE read-only endpoint + rewired the picker's data source. merge 4db10675 (api+web deployed, NO migration — read over existing tables). WHAT SHIPPED: (1) new `GET /dm/candidates` — DISTINCT server co-members of the caller, who_can_dm≠'nobody', self-excluded, deduped, bare `DmCandidate[]` {userId,displayName,avatarUrl}; (2) StartDmPicker rewired from `GET /servers/:id/members` (serverId-gated → dead-end when no server context) to `GET /dm/candidates` (no serverId gate); (3) DmHome `currentUserId = profile.userId` (true opaque users.id, not username) → self-exclusion + optimistic-author now resolve in the same id-space (cures F7). T-9 HEADLINE (T-5, real UI clicks NOT an API shortcut — the wave-46 lesson): Fixture A login→DM home→open picker→picker LISTS co-member Fixture B→select→Open DM→thread opens→message sends with CORRECT author, "Unknown user" ABSENT; 2 runs, no flake. Candidate-privacy fence (T-8 live): co-members only (no global directory leak), self-excluded, bidirectional (A↔B), NO email/who_can_dm in DTO, IDOR callerId-spoof via ?userId/?callerId/X-User-Id all IGNORED (session-derived), unauth→401, secret-grep clean, rate-limited (ThrottlerGuard). Fresh-head-tester gate APPROVED with independent source spot-checks (no mock-the-SUT; real WHERE clause / DTO-strip / IDOR fence). Findings→V-2 all low/info: throttle policy inconsistency (/dm/candidates throttled ~4/burst vs /dm/conversations not — safe direction, root of the message-poll 429s); who_can_dm='nobody' exclusion + negative-isolation not live-proven (2-member proof-server fixture gap — fence proven ACTIVE by positive results, only counter-example controls missing); getDmCandidates has no LIMIT/pagination (fine at MVP scale). Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B), co-members of proof server ad62cd12.)**
**Surface:** DM screen — conversation-list rail + open thread (message log `role="log"`) + composer + Start-DM picker modal (`dialog "Start a new direct message"`). New member-facing surface inside the `/app` shell, reached via the server-rail button `aria-label="Direct Messages"` (distinct non-server destination). Design: `design/direct-messages.html` — ✅ layout PASS 1440/1280/1024, dark-theme tokens consumed (surface-950/900/800, emerald accents), T-6-verified. **Picker chrome byte-identical to wave-46 (wave-47 changed only the data source); render-confirmed dark @1280 (populated list + "No people match" empty state).**
**Routes/endpoints (T-3/T-4/T-8-verified LIVE):** `GET /dm/candidates` **(wave-47 NEW — server co-members, who_can_dm≠'nobody', self-excluded, deduped, bare `DmCandidate[]`; 200 authed / 401 unauth; callerId session-derived, IDOR-safe)**, `POST /dm/conversations` (who_can_dm-enforced pre-write, cap ≤10, find-or-create 1:1 dedup → 200/400/403), `GET /dm/conversations` (last-message preview, caller-scoped → 200), `POST /dm/conversations/:id/messages` (participant-gated 404-non-leak, idempotent `UNIQUE(conversation_id,idempotency_key)` → same row on replay → 200/400/404), `GET /dm/conversations/:id/messages?cursor=&limit=` (participant-gated, keyset ASC pagination → 200/404); Socket.IO `dm:message` real-time fan-out on the auth-gated `/messaging` namespace to participant per-user rooms incl. sender; offline-tolerant send via generalized outbox (kind:dm|channel discriminated union).
**Flow (wave-47 — NOW STARTABLE end-to-end via UI):** open DM home (server-rail DM button) → click "New message" (Start-DM affordance) → picker LISTS real candidates (server co-members from `GET /dm/candidates`; searchable by display name; "No people match" empty-filter state) → select recipient(s) (restricted-target create returns 403 shown inline without closing) → "Open DM" (1:1) or "Create Group" (≤10) → thread opens → send/receive real-time (2-client fan-out proven wave-46 with real ws frame), optimistic render (~11ms), offline-tolerant (pending → drains on reconnect). States: loading / loaded / empty-list / empty-thread / empty-candidates ("No one to message yet — join a study server with others") / error / offline-pending / failed.
**T-9 verified (LIVE):** **[wave-47] DMs STARTABLE end-to-end through the REAL picker UI — A opens picker → picker lists co-member B → select → Open DM → thread opens → message sends with CORRECT author (F7 "Unknown user" GONE), 2 runs no flake ✅; search filter + "No people match" empty state ✅; Esc-closes-picker ✅; candidate-privacy fence (co-members only, self-excluded, bidirectional, no DTO leak, IDOR callerId-spoof ignored, unauth 401) ✅.** [wave-46] 2-client real-time DM fan-out A→B (real `dm:message` ws frame + DOM) ✅; reply B→A ✅; optimistic send ✅; offline→pending→reconnect ✅; all 4 DM authz invariants under active pen-test ✅; secret-grep clean ✅.
**Known defects surfaced to V-2 (NOT blocking the map — feature works):** **RESOLVED wave-47: F-A CRITICAL (DMs unstartable-via-UI — entry point now works) + F7 MAJOR ("Unknown user" author — currentUserId=profile.userId fix).** STILL OPEN from wave-46: (F4 HIGH) `listMessages` cursor pagination duplicates the boundary message + drops the last (ms-vs-µs `encodeCursor` truncation, ASC direction); (F6 MAJOR) sender's own message double-renders (`useDm.ts:205` socket dedup missing optimistic-by-idempotencyKey vs the M1 sender-fan-out echo); (F1/F3 MEDIUM/MINOR) participant `displayName` falls back to the raw userId UUID when unset (server-side). wave-47 NEW (all low/info → V-2): throttle policy inconsistency on `/dm/candidates`; who_can_dm='nobody' + negative-isolation not live-proven (fixture gap); getDmCandidates no LIMIT. See `process/waves/wave-46/blocks/T/findings-aggregate.md` + `process/waves/wave-47/blocks/T/findings-aggregate.md`.
**Deferred (later M8 slices):** DM message search, read receipts, reactions, typing indicators, attachments, group-DM admin, per-user block/report.

---
## Study Timer (M8 study-group tools slice 1, wave-49 — LIVE + verified in prod at T-5/T-8)
**Surface:** shared study-timer widget in the server view (a persistent panel/bar in the channel column). A synchronized Pomodoro (25/5 Work/Break) all server members see, with Start/Pause/Reset + an ephemeral "N studying" presence roster (who's viewing the live timer). Design: `design/study-timer.html`.
**Routes/endpoints (backend, wave-49 B-block):** `POST /servers/:serverId/study-timer/{start,pause,resume,reset}` (assertMember), `GET /servers/:serverId/study-timer` (compute-on-read remaining/phase); Socket.IO `study-timer:update` (server-room broadcast) + `study-timer:presence` (ephemeral roster). Persisted single-row-per-server (anchors only; NO server timer-loop). Hardcoded 25/5 (custom durations deferred → f4b3659e).
**Flow:** a member starts a focus session → everyone in the server sees the same countdown → auto-advances Work→Break→Work → members see "who's studying with me" (body-doubling). States: idle / running-Work / running-Break / paused / loading / error.
**Distinct from:** the online/voice PRESENCE module — the timer roster ("studying now" via ph-timer badge) is scoped to the live timer, not general online status.
**Deferred (later study-group slices):** custom durations, study sessions (joinable rooms), collaborative whiteboard, timer history/stats, educator-only control gate.
**Note:** journey entry finalized/verified at wave-49 T-9.

## /discover — Public server directory (wave-67, M11) [PENDING BUILD]
- **Route:** `/discover` (authed). Entry: Discover affordance on ServerRail (near create-server "+").
- **Screen:** ServerDiscoverPage — searchable card grid of PUBLIC servers (name, description, topic, member count) + one-click Join.
- **API:** `GET /servers/discover?q&limit&offset` (AuthGuard, is_public=true only); `POST /servers/:id/join-public` (AuthGuard, is_public-gated).
- **States:** loading skeletons / results / honest cold-start empty / no-search-match / retryable error / joining / joined.
- **Flow:** logged-in student → Discover → browse/search public communities → Join (reuses membership core) → server appears in rail + auto-select.
- Note: design canonical at design/server-discover.html. Full journey regen at wave-67 T-9.

## Server Settings — Overview / Publish-to-directory (wave-68, M11 WRITE-HALF — LIVE, T-9-verified in prod)
**last_updated_wave68: 2026-07-06 (T-9 wave-68 — the M11 WRITE-HALF: an owner can now PUBLISH their server to /discover + set description/topic, closing the loop that made wave-67's directory permanently empty; AND the discover `memberCount:0` bug (wave-67 F67-T5-1) is fixed. merge 1b5a184, BOTH services deployed SUCCESS. NO migration (is_public/description/topic exist from wave-67 migration 0024).)**

**Surface (NEW):** the **Server Settings — Overview** surface, a full-screen dialog (`role=dialog`, `data-testid="server-overview-settings"`) reached via the gear button in the ChannelSidebar (`aria-label="Server settings — Overview"`, `data-testid="server-settings-btn"`; sibling "Server settings — Roles" preserves the untouched roles matrix). Owner-only: the publish control renders/enables ONLY for the server owner (`isOwner` gate). Two cards: **Public directory** (a `role=switch` "List in public directory" toggle — dark-on-emerald DS §8) + **Server profile** (Description textarea, 500-char cap w/ counter; Topic input, 100-char cap w/ counter). Save Changes button. Design canonical: `design/server-settings.html` (Overview shell). T-6 dark-theme layout PASS @1440 — left nav rail (Overview active emerald / Roles), two settings cards, char counters, Owner badge.

**Routes/endpoints (T-3/T-4/T-5/T-8-verified LIVE):**
- `PATCH /servers/:id` **(wave-68 NEW — AuthGuard, OWNER-ONLY)** — partial update `{is_public?, description?, topic?}`, returns the updated server (ServerDetail shape), touches no other columns. **Owner → 200; non-owner → 403 "Not authorized to update this server" (row UNMODIFIED); missing server → 404 "Server not found".** Owner-gate is server-side (NestJS), not merely a UI hide.
- `GET /servers/discover` memberCount **CORRECTED** (wave-67 shipped it always 0; wave-68 rewrites the aggregation so memberCount = the real `server_members` count). No response-shape change. A public server with N members now shows N; 0 members shows a correct 0.

**Flow (write-half, now closed end-to-end):** owner opens a server they own → gear → **Overview** settings (pre-populated from the real server state: unpublished/empty initially) → toggle "List in public directory" ON + set description + topic → **Save** (`PATCH /servers/:id` → 200) → post-save reconcile persists in-place AND on dialog close+reopen (the B-6 fix — no revert) → the server now appears in **/discover** with its name/description/topic + the CORRECT member count + a Join affordance → owner can toggle OFF + Save to **RETRACT** (server disappears from /discover; partial update leaves description/topic intact). States: pre-populated / editing / saving / saved-reconciled / error (non-destructive banner) / published-in-directory / retracted.

**T-9 verified (LIVE, prod, fixture A owner of `ad62cd12` "Fixture Proof Server", real member count = 2):**
- **T-5 full loop ✅** — Overview opens pre-populated (switch=false, empty desc/topic reflecting real state) → toggle ON + desc + topic → Save → `PATCH /servers/ad62cd12` **200** → write PERSISTED to Postgres (is_public=t, desc+topic set) → close+reopen dialog RE-POPULATES from persisted state (no revert — B-6 reconcile proven) → **/discover flips 0→1 server**, the card renders with correct name/desc/topic AND **"2 members" (the real count, NOT 0 — memberCount fix proven live at UI + API)** + a Join button → unpublish (toggle OFF + Save) → **/discover flips 1→0 (retract works)**; partial update kept desc/topic (only is_public changed). 2 runs, no flake.
- **memberCount fix ✅ (the wave-67 bug, now closed)** — `GET /servers/discover` returned memberCount **2** for the published server vs DB ground-truth of exactly 2 members; rendered "2 members" on the /discover card. Was permanently 0 in wave-67.
- **T-8 owner-only publish (LOAD-BEARING) ✅** — authenticated non-owner (fixture A) `PATCH`ing a server owned by fixture B → **403** + the target row confirmed **UNMODIFIED in Postgres** (is_public still false, desc/topic still null — the "HACKED"/"pwned" payload had zero effect). Missing server → **404**. Owner-only publish enforced SERVER-SIDE (not just UI-hidden). Non-owner UI-gate (control hidden for non-owners) is CI-covered (13 overview tests: owner-sees / non-owner-doesn't).
- **T-6 layout ✅** — Overview settings surface + populated /discover cards match the dark-theme design system; no material divergence.

**CI corroboration (T-1/T-2/T-3/T-4 green on merge SHA):** web 603 + api 764 unit; the real-Postgres integration tier RAN GREEN (NOT mocked): memberCount real-count 0/1/2, private-exclusion, updateServer non-owner→403 row-unmodified (AC9 — the live-DB guard the wave-67 mocked unit test lacked, which let the memberCount:0 bug ship green).

**routes_added:** `PATCH /servers/:id` (owner-gated). **screens_added:** Server Settings — Overview surface. **routes_removed:** none. **regressions:** none — Roles matrix untouched; /discover read-half + Join intact. **Coverage/scope notes (→ V-2, non-blocking):** LiveKit media-plane N/A (no voice/video surface this wave). Prod left CLEAN: T-8 fixture deleted, target server restored to private, /discover back to its honest cold-start empty state (0 public servers). Verified-prod fixture: studyhall-e2e-fixture (A); T-8 non-owner fixture (B-owned server) created + deleted for the test.

## Moderation — Report dialog + Owner report inbox (wave-69, M14 Trust & Safety slice 1 — LIVE, T-9-verified in prod)
**last_updated_wave69: 2026-07-07 (T-9 wave-69 — M14 moderation FIRST bundle LIVE: every publicly-reachable actor (server/member/message) is now REPORTABLE, and owners/mods have a report INBOX to action reports (timeout / delete message / dismiss) — the M14 public-launch-gate leg-1 primitive. NO new route (report dialog + inbox are overlays on existing surfaces). merge 5fdd2bb / PR #84, BOTH services deployed SUCCESS; migration 0025 [reports table + FKs + index] applied to prod before the api deploy. THE 4 AUTHZ PATHS PROVEN LIVE at T-8 on the deployed revision: no-IDOR [spoofed reporter_id ignored, server-derived], moderate_members gate [owner A→200 / non-mod B→403], rank-guard route-through [timeout on owner→403 no side effect], cross-server tamper [target_server_id!==route serverId→404 pre-mutation]; secret-grep clean; CI integration tier re-proved all 4 vs postgres:16. T-5 live: report submit 201 [correct target, reporter_id server-derived] + owner inbox resolve/dismiss loop + moderator-gate. T-6: desktop layout faithful to D-3 + tokens correct [danger #b91c1c, dark-on-emerald primary]. TWO open findings → V-2/V-3: F1 MAJOR own-content report leak [MainColumn.tsx:343 currentUserId=username should be userId; isOwn always false → Report shows on own message/member + own-message Edit missing; non-security]; T6-M1 CRITICAL mobile inbox off-screen [fixed inset-0 trapped in ChannelSidebar translateX drawer → portal to body]. INFO: rate-limit present [10/60s]. Fixture A studyhall-e2e-fixture owner of ad62cd12.)**
- **Surfaces (NEW, no new route — dialog + panel):** (1) a **Report dialog** (`role=dialog`, modal, focus-trapped) reachable from a consistent **flag/Report affordance** on three target types — a public **discovery server card**, a **member row**, and a **message hover-action bar**. Bounded reason textarea (char-capped) + Submit (emerald, dark-on-emerald DS §8) + Cancel (ghost). States: default / validation / submitting / success / network-error (non-destructive, dialog stays, reason intact). (2) an **Owner/moderator Report inbox** — a `moderate_members`-gated list of open reports (target + reason + reporter + time), each row with actions **Timeout** / **Delete** (danger, `--danger-btn` #b91c1c AA) / **Dismiss** (ghost). States: loading (skeleton rows) / list / empty / actioning / resolved (row leaves) / error (row stays + announced).
- **Planned API (B-block, spec tasks 9f2bb017 seed + siblings):** `POST /reports` (session-derived reporter, target_type server|member|message); `GET /servers/:serverId/reports` (`moderate_members`-gated); `POST …/reports/:id/resolve` (action loop → Timeout routes ModerationService.setMemberTimeout / Delete routes MessagesService.deleteMessage / Dismiss marks dismissed). Server-side authz hard ACs: no-IDOR (session callerId), `moderate_members` gate, rank-guard via route-through (can't action owner/manage_server/manage_roles), cross-server tamper guard (target_server_id === serverId).
- **A11y:** modal focus-trap + `role=dialog` + aria; toast feedback announced via `role=alert`/`aria-live=assertive` (errors) + `role=status`/`polite` (success) — the WCAG 4.1.3 requirement caught by the D-3 head-designer gate; viewport allows pinch-zoom.
- **Design canonical:** `design/moderation-report.html` (D-3 APPROVED wave-69, both dual reviewers + head-designer attempt-2). DESIGN-SYSTEM token added: `--danger-btn: #b91c1c` (reusable danger-button fill). Full journey regen at wave-69 T-9.

## Block — user-to-user block affordance + blocked-users settings list (wave-70, M14 Trust & Safety slice 2 — LIVE, T-9-verified in prod)
**last_updated_wave70: 2026-07-07 (T-9 wave-70 — M14 user-to-user BLOCK now LIVE: a student blocks another user → the blocked user's DMs + content are hidden bidirectionally, cross-server. The M14 launch-gate leg 3 + the last mvp-critical Trust & Safety slice before the founder-reserved public-directory GO. merge a2c006a / PR #86, migration 0026 (user_blocks) applied to prod before the api deploy; BOTH services deployed SUCCESS. THE BLOCK + DM HIDE PROVEN LIVE at T-8 (2 fixtures A/B, all 13 checks): no-IDOR (spoofed blocker_id ignored, session-derived), self-block 400, exists 404, idempotent; DM HIDE BIDIRECTIONAL at all 5 seams (B→A createConversation/sendMessage 403, candidates exclude both ways, convo hidden, listMessages 403, reverse A→B 403), unblock restores — NO leak on any DM path; CI 19-case integration re-proved all 5 seams vs postgres:16. T-5 block-flow live: dialog a11y/focus-trap/mobile-bottom-sheet, spec-D own-row suppression (no Block/Report on own row), unblock. TWO findings → V-2: FINDING-1 MAJOR (member-row still shows "Block" after blocking — server + /settings/privacy list correct, but the row doesn't reflect blocked state); FINDING-2 LOW (blocked-users list shows UUID not display name — GET /blocks lacks profile enrichment). Also shipped: the member-row own-Report suppression fix (spec-D). Public-launch GO stays founder-reserved. Fixtures A studyhall-e2e-fixture + B studyhall-e2e-fixture-b.))**

**wave-71 polish (LIVE, T-9-verified, merge 670c46e / PR #87):** the Block UI is now complete — (1) the member-row Block affordance REFLECTS state (Block↔Unblock toggles LIVE without reload, via a shared useBlocks store; proven live at T-5), and (2) the "Blocked users" settings list shows the blocked user's real display name + avatar (GET /blocks enriched via a listBlocks LEFT JOIN, BlockListItem DTO), not raw UUIDs. No schema change; block authz + DM HIDE untouched (wave-70 T-8 proof stands). This closes M14's 2 UI-polish follow-ons → M14's Trust & Safety scope is fully shipped at launch quality; the public-launch GO + next-milestone theme pick are FOUNDER-RESERVED (surface at wave-71 L-1/N-1).
- **Surfaces (NEW):** (1) a **Block/Unblock affordance** on a member row / profile / DM header — opens a **confirm dialog** (modal, focus-trapped, danger --danger-btn #b91c1c confirm + ghost cancel, mobile bottom-sheet, toast a11y). NOT shown on the viewer's own row. (2) a **"Blocked users" list** in USER settings (/settings/privacy — user-level): rows (avatar + name + inline Unblock), loading skeleton, empty state.
- **Planned API (B-block, wave-70 spec bc5986a9):** POST /blocks {blockedUserId} (AuthGuard, session blocker_id, self-block→400, exists→404, idempotent); DELETE /blocks/:blockedUserId (204 idempotent); GET /blocks (own list, no IDOR). NEW user_blocks table (cross-server, no server_id; UNIQUE pair). **DM HIDE predicate** on DmService (createConversation gate / sendMessage reject / getDmCandidates exclude / listConversations + listMessages hide — bidirectional), reusing the DM-visibility idiom. Block is the M14 launch-gate leg 3 (user-to-user safety).
- **Design canonical:** design/block-ui.html (D-3 APPROVED wave-70). Orthogonal to the who_can_dm privacy policy (separate). Full journey regen at wave-70 T-9.
- Also this wave: the member-row own-Report suppression fix (spec-D, MemberListPanel isSelf guard) — completes the wave-69 own-content-leak remediation on the member surface.

**Monetization substrate note (wave-74, M9 — backend-internal, no new user-facing route):** a `subscriptions` tier model (every server resolves to 'free' by default) + an `EntitlementsService` (tier→capability caps from a founder-tunable placeholder config) now back a read-only entitlement gate in `createServer` (servers-per-owner cap; free placeholder=100,000 → non-restrictive; enforceable once real tiers are assigned). No user-facing surface this slice (the "Your plan" display + Stripe/checkout are fenced to the next M9 slice, founder-reserved). Live on d79dd18.

## Server Settings — "Your plan" panel + billing endpoints (wave-75, M9 mock-payment freemium upgrade path — LIVE, T-9-verified in prod)

**Merge 3b94e276 (PR #93). Deployed api + web SUCCESS. wave_type = backend + ui + auth(payments). Mock/test payment only — real Stripe fenced (founder keys, rule 6); tier config + prices brain-set.**

WHAT SHIPPED: a "Your plan" panel on the existing per-server **Settings → Overview** surface (extends the wave-68 Server-Settings node — NO new page route; panel reuses PrivacyActivityPanel chrome). Three new REST endpoints under the billing module, all behind the verification-REQUIRED `AuthGuard`:

| Endpoint | Auth / gate | Behaviour | Live-verified |
|---|---|---|---|
| `POST /servers/:serverId/billing/tier` (api, tier change) | AuthGuard + owner-only | Body `{targetTier}` Zod→400; owner-check 404→403→mutate (no-IDOR, side-effect-free 403); mock `BillingProvider` upserts `subscriptions` ON CONFLICT(server_id); same-tier 200 idempotent. Returns 200 `ServerPlan {serverId,tier,entitlements}`. | 200 owner (free→server_pro→school); 403 non-owner (tier UNMODIFIED); 400 invalid; 404 unknown; 401 unauth/malformed |
| `GET /servers/:serverId/billing/plan` (api, read) | AuthGuard + owner-or-member | Returns 200 `ServerPlan`; 403 non-member; 404 unknown; 401 unauth. | 200 shape matches DTO + canonical caps exactly (free 2048/10/false) |
| `GET /servers/:serverId/educator-tools/status` (api, entitlement gate stub) | AuthGuard + EntitlementGuard('educatorAdminTools') | 200 `{enabled:true}` when tier=school; 403 fail-closed on free/server_pro. Models ONLY the gate; real educator tools fenced. | free→403 fail-closed; school→200 unlock (end-to-end) |

CANONICAL TIER_CAPS now live (brain-set): free {storage 2GB, callCapacity 10, educatorAdminTools false}, server_pro {50GB, 50, false}, school {500GB, 100, true}. free.maxServersPerOwner stays 100_000 (NON-REGRESSION guard — must exceed largest owner count 646; wave-74 free-cap incident not reintroduced; create-server → 201 confirmed live for Fixture A).

**M9 SUCCESS METRIC MET LIVE (T-5, prod web-production-bce1a8, Fixture A):** on a server Fixture A owns, opened Settings→Overview → "Your plan" shows CURRENT PLAN Free / Storage 2 GB; selected Server Pro radio → clicked "Switch plan (test mode — no charge)" → the displayed tier + limits refreshed to **Server Pro / 50 GB / voice 50 IMMEDIATELY with no reload**, and the change **survived a full close+reopen** of the settings surface (persistence, not just optimistic re-render). Mock-checkout disclosure visible ("test checkout — StudyHall does not charge your card and no payment is taken"; reference prices Free $0 / Server Pro $8mo / School $99mo shown). 0 console errors. Panel token-compliant (dark base rgb(10,10,11), zinc-800 button, 6px radius, 40%-white muted disclosure 12px, no overflow) at 1440.

SECURITY (T-8, prod, Fixtures A+B): no-IDOR (B POST tier on A's server → 403, tier unchanged, side-effect-free); educator gate fail-closed→unlock (free 403 → school 200); AuthGuard verification-required (unauth + malformed bearer → 401 on all 3 endpoints; 401 precedes 403 — the P-4-caught SessionNoVerifyGuard hole is closed); non-regression create-server 201; secret-grep clean.

DATA: no schema change — reuses wave-74 `subscriptions` (UNIQUE(server_id), migration 0029). Tier change = INSERT ON CONFLICT(server_id) DO UPDATE.

KNOWN findings → V-2 (non-blocking, 0 critical): (medium) `GET /educator-tools/status` has no owner/member check — any authed user reads the boolean tier-status of any tier-unlocked server (no mutation/PII; the FENCED real educator tools MUST add a member gate). (medium, process) the authored pg-harness `subscriptions`-upsert integration test (`apps/api/test/integration/billing-subscriptions-upsert.spec.ts`) typechecks+lints clean but is CI-pending on a follow-up PR (no local pg); upsert effect proven end-to-end live. (low) pg-harness `truncateTables()` omits `subscriptions`. (low) benign act() warnings on 19 pre-existing settings tests.

Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B). Prod left clean: two throwaway servers (0c8192da, 37a55026) reverted to free (no DELETE /servers endpoint exists — rows persist empty, same limitation as prior waves); shared proof server ad62cd12 never mutated.

## Educator Admin Console + server analytics (wave-76, M13 leg-1 — LIVE, T-9-verified in prod)

**Merge d8d4d9e6 (PR #95). Deployed api + web SUCCESS (/health 200). wave_type = backend + ui + auth(authz). Read-only aggregates over already-shipped tables — no schema change, no new telemetry infra. Builds on the M9 (wave-75) educatorAdminTools entitlement substrate.**

- **Screen:** Educator Admin Console — `apps/web/src/shell/EducatorAdminConsole.tsx` (new), mounted inside `ServerOverviewSettings` on the per-server **Settings → Overview** surface (below the "Your plan" panel; NO new page route). Canonical design: `design/educator-admin-console.html`.
- **Access (client gate + server authz):** owner OR educator member (a role with `manage_assignments`) on a school-tier server (`educatorAdminTools=true`); HIDDEN (component returns null) for non-owner/non-educator OR non-school tier — mirrors the ServerPlanPanel gating idiom. Server is authoritative (403 regardless of client gate).
- **Renders (loaded dashboard, `educator-console-dashboard`):** 4 groups — Total Members (with Educators/Students split) · Messages (volume) · Assignments Total (graded scope + submissions) · Recent Activity (message_sent / assignment_submitted / session_scheduled counts). Read-only counts/rollups only. States: loading (spinner) / loaded / empty-zero / forbidden (403) / error (retry).
- **Endpoints:** `GET /servers/:serverId/educator-tools/analytics` (NEW → 200 `ServerAnalytics`) + `GET /servers/:serverId/educator-tools/status` (wave-75, NOW owner/educator-gated — closes the wave-75 T8-F1 leak; boolean `{serverId,enabled}` contract PRESERVED). Composed authz on both: `AuthGuard + EntitlementGuard(educatorAdminTools) + EducatorAccessGuard` (owner OR manage_assignments via `RbacService.can`, default-deny).

**LIVE VERIFICATION (T-3/T-4/T-5/T-8, prod, Fixtures A owner + B verified member):**
- **Aggregate correctness (T-4):** live /analytics counts match APP_DB ground truth EXACTLY on the rich "Fixture Proof Server" — members 2, messageVolume 482 (soft-delete correctly excludes 275 deleted; not 757), assignmentCount 2 (excludes 5 deleted; not 7), submissions 2, sessions 24, roleBreakdown reconciles to memberCount. Empty school server → all-zero aggregates 200 (not error).
- **Console UI (T-5):** LOADED (4 groups, data = API) on rich school server · ZERO-state on empty school server · HIDDEN on free-tier server. All three states proven live via Playwright (fresh page load).
- **Authz (T-8, crown jewel):** owner+school→200 · educator(manage_assignments)+school→200 · non-owner/non-educator member (verified Fixture B, NULL role)+school→**403 with EducatorAccessGuard message** (authz-layer, NOT email-verif — the wave-75 T8-F1 leak is CLOSED) · owner+free→403 (entitlement) · unauthenticated→401 · malformed :serverId→400 (not 500). /analytics response = counts/rollups only, 0 PII.
- **Layout (T-6):** dark tokens (SURFACE_800 bg rgb(28,28,31), HAIRLINE border), 7 inline-SVG icons, 7 stat cards, no horizontal overflow.

**KNOWN findings → V-2 (non-blocking, 0 critical):** (LOW) unknown serverId → 403 uniformly, not the spec-text's 404 — deny-is-deny / no server-existence enumeration (security-POSITIVE; matches spec block 682e0912's own "non-member → 403"); recommend reconciling /status + /analytics spec text to 403. (LOW) mid-session tier upgrade (free→school) doesn't reveal the console until a page reload — the `educatorToolsEnabled` effect in ServerOverviewSettings doesn't re-run on an external tier change; fresh loads always correct (UX follow-up).

**Prod left CLEAN:** both test servers reverted to free (no school-tier server remains); throwaway T-8 members + the test educator role deleted; throwaway user app-DB rows deleted; Fixture B restored to baseline NULL role. Verified-prod fixtures: studyhall-e2e-fixture (A, owner) + studyhall-e2e-fixture-b (B, verified member of Proof Server).

## [wave-77] Cross-server member profile card (M13 leg-2)  ·  [wave-78] polish: 5th state + clearable role
- **Surface:** cross-server member profile card — opened from MemberListPanel (clicking a member), rendering the target's self-declared academic identity (PublicProfile). Canonical design: `design/member-profile-card.html`. States: loaded / loading / hidden ("Profile Unavailable", no retry) / partial / **[wave-78] error (retryable "Couldn't load profile" + "Try again", amber DS accent)** — the error state is distinct from hidden: reached ONLY on a client-observable transport failure (non-HttpError throw OR 5xx); EVERY other HttpError status (401/403/404/410/429) collapses to the byte-identical hidden state (FAIL-CLOSED anti-oracle). No new design surface — reuses the hidden container + DS tokens.
- **Access:** any authenticated member; the server-side profile-view endpoint enforces profile_visibility (everyone / server-members via explicit shared-server check / nobody), user_blocks (bidirectional), and deleted_at suppression, FAIL-CLOSED to hidden. NO verification badge (self-declared, educator/staff = plain text).
- **Endpoints:** GET /profile/:userId (cross-server PublicProfile) + GET/PATCH /profile (self, academic fields). ProfilePage academic-identity editor at /settings/profile — **[wave-78] the academic-role select's empty "Not specified" option now CLEARS the role (was a dead no-op): PATCH /profile {academicRole:null|''→null} → 200, persists SQL NULL; absent field leaves it unchanged (undefined≠null); non-enum → 400.**

**last_updated_wave77: 2026-07-07 (T-9 wave-77 — M13 leg-2 CROSS-SERVER PORTABLE ACADEMIC IDENTITY now LIVE: students carry self-declared academic identity (pronouns/bio/institution/program/academic_role/academic_year) that renders on a cross-server member profile card, gated STRICTLY by profile_visibility. merge 633f362e / PR #96; migration 0030 (6 nullable academic columns on users, no backfill) applied to prod BEFORE the api deploy; BOTH services deployed SUCCESS (/health 200). wave_type = backend + ui + auth(privacy). NEW SURFACES: (1) ProfilePage academic-identity EDITOR at /settings/profile — pronouns/bio/institution/program/academicRole(select student|educator|staff)/academicYear, client-validated to shared Zod bounds; (2) cross-server MemberProfileCard opened from MemberListPanel (portal to body, 4 states loaded/loading/hidden/partial; adopted design/member-profile-card.html); NO verification badge (self-declared, academicRole = plain text). NEW ENDPOINTS: GET/PATCH /profile (self, academic fields; SessionNoVerifyGuard; 200|409 username-conflict|400 invalid) + GET /profile/:userId (cross-server PublicProfile; SessionNoVerifyGuard; 200 visible | uniform 404 hidden | 401 unauth). CROWN-JEWEL PRIVACY PROVEN: T-8 LIVE (Fixtures A target + B viewer, co-members of ad62cd12) — nobody→404 hidden / everyone→200 visible / server-members co-member→200 visible / block bidirectional (B→A and A→B)→404 hidden+restore-on-unblock / NO email in PublicProfile (live grep) / unauth→401 / UNIFORM 404 across nobody+blocked+nonexistent+malformed-:id (no info-leak oracle); the server-members STRANGER-NOT-LEAKED case + soft-delete + fail-closed-unknown proven by the merge-blocking 13-case profile-visibility integration matrix vs postgres:16 at C-1, and the resolver confirmed to use the self-referential server_members EXISTS idiom (mirrors dm.service, NOT listServerMembers's ambient-membership shortcut — the exact leak the spec flagged is absent). T-3 live: GET/PATCH /profile round-trip + invalid-enum→400. T-5 live (real UI, sequential on one browser per T-5 principle #1 after the shared-profile MCP swarm blocked): editor edit→Save academic identity→PATCH /profile 200→values persist across full reload; member card→GET /profile/:userId 200→renders academic identity, NO email, NO badge, Esc-dismiss, portal-not-clipped. T-6: card dark tokens (bg rgb(18,18,20), text rgba(255,255,255,.92), radius 8px), portaled z-120, no verification badge. T-7 SKIPPED (read-only endpoints + small card, not heavy). Fresh-head-tester T-9 gate APPROVED (attempt 1; no coverage theater, crown-jewel honestly proven, 0 blocking). Findings→V-2 all non-blocking (0 critical): (LOW) restore-to-unset gap — PATCH /profile cannot clear academicRole back to NULL once set (enum .optional() rejects null/empty); (LOW) member card renders same calm "Profile Unavailable" for a non-404 network error as for a genuinely-hidden profile (no distinct retry, carried from B-3); (LOW) T-5 swarm infra — all playwright MCP instances share one chrome profile (no --isolated), parallel swarm cannot acquire >1 browser; (LOW) test-file as-any casts; (INFO) malformed :id → uniform 404 not 400 (stronger anti-oracle posture). Prod left CLEAN: Fixture A visibility restored to 'everyone' (original), all A↔B blocks removed, academic text fields cleared to empty (academicRole residual 'educator' is harmless self-declared test data on a test fixture, un-clearable via the enum schema). Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B), co-members of proof server ad62cd12.))**

**last_updated_wave78: 2026-07-07 (T-9 wave-78 — M13 leg-2 FOLLOW-UP: member-profile-card UX polish. Closes the two LOW findings wave-77 carried to V-2. ANNOTATION-ONLY regen (no new route/screen/endpoint — /settings/profile editor + MemberProfileCard + GET·PATCH /profile all pre-exist; routes_added=[] routes_removed=[] coverage_gaps=[]). merge 855e811 / PR #97; NO migration (users.academic_role already nullable text); BOTH services deployed SUCCESS (/health 200). wave_type = ui + backend + auth(privacy). WHAT CHANGED: (1) academicRole now CLEARABLE — write contract UpdateProfileSchema.academicRole became nullable + ''→null preprocess, and UsersService.updateProfile distinguishes undefined(leave)/null(write SQL NULL)/string(set); the editor's empty "Not specified" option is no longer a dead no-op. (2) member card gained a 5th state — a DISTINCT retryable "Couldn't load profile" error (amber DS accent + "Try again") for client-observable transport failures (non-HttpError throw OR 5xx), so a network blip no longer masquerades as the deliberate privacy 'hidden' state; FAIL-CLOSED hardened at B-6 (commit 1fca71a): every other HttpError status (401/403/404/410/429) → byte-identical hidden. LOAD-BEARING anti-oracle RE-PROVEN LIVE at T-8 (fixture A, prod): UNIFORM 404 byte-identical across nonexistent+malformed-:id+second-nonexistent (68 bytes, diff clean); card forced-404→byte-identical HIDDEN no-retry; forced-5xx→retryable ERROR + retry stays retryable under sustained failure; retry-after-transient→LOADED recovery; forced-403→byte-identical HIDDEN no-retry (fail-closed proven LIVE via client fetch-patch — server has no 403 path, honest note) + CI 403→hidden guard test present+passed (web 703/703); NO email in PublicProfile (self + co-member fixture-b, live grep, 11-key allowlist); unauth→401. T-3 LIVE contract matrix: PATCH {academicRole:null}→200+null / ''→200+null / absent→unchanged(staff) / 'teacher'→400; GET /profile/:userId PublicProfile 11-key no-email. T-4: real-PG pg-harness integration spec (set→null→SQL NULL; absent→unchanged; staff persists; idempotent-clear) green in CI on postgres:16, cross-connection read-back. T-5 LIVE (fixture A): clear-role round-trips (genuine academicRole:null PATCH body, full-reload confirms cleared, restored to educator) + member card renders LOADED (name+Educator, GET 200, Esc dismisses). T-6: amber icon = DS --accent-amber rgb(245,158,11) (12%/25% fills), retry button = surface-700 #27272a + hairline + radius-md + h-32 — no invented hex; hidden state byte-identical. T-7 SKIPPED (light read-only, ~90 LOC, not heavy). Fresh-head-tester T-9 gate APPROVED (attempt 1; crown-jewel anti-oracle honestly proven + independently source-verified L215 fail-closed branch, 0 blocking). Findings→V-2 all non-blocking (0 critical): (LOW) T-3 displayName sibling-field prod residue — the absent-field probe PATCHed displayName (min-length validated, un-restorable to original null) → now "Fixture A"; academicRole (the wave's field) restored EXACTLY to 'educator', confirmed twice; (MEDIUM, infra not app) Playwright MCP instances share one chrome profile without --isolated → parallel swarm partially blocked, ran sequentially; (LOW, observational) fixture A server rail ~714 leftover E2E/M3 test servers (no server-DELETE path), cleanup candidate. Prod left CLEAN: academicRole = 'educator' (restored). Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B, userId da74148e), co-members of proof server ad62cd12.))**

**last_updated_wave79: 2026-07-08 (T-9 wave-79 — M13 leg-3a SERVER-BLIND END-TO-END DM ENCRYPTION now LIVE: 1:1 DMs are end-to-end encrypted (server-blind) with an HONEST fail-closed indicator. ANNOTATION-ONLY regen (no new route/screen — the DM view/route pre-exists; wave adds an encryption indicator + a ciphertext-envelope send/receive path + TWO new key endpoints; routes_added=[] routes_removed=[] coverage_gaps=[]). merge 0fa0f5f / PR #98; migrations 0031 (user_encryption_keys — user_id TEXT FK to users.id, public_key/algorithm/created_at, NO private-key column) + 0032 (dm_messages content→nullable + ciphertext/sender_key_ref/envelope_version/deleted_at) applied to prod BEFORE the api deploy; BOTH services deployed SUCCESS (/health 200). wave_type = ui + backend + auth (crypto/privacy, security-critical). NEW REST: PUT /profile/encryption-key (self, AuthGuard — store/rotate public key: 200/200/400 unsupported-algo·oversized·empty/401) + GET /profile/:userId/encryption-key (viewer-from-session, who_can_dm-gated via DmService.canDm/enforceWhoCanDm — NOT profile_visibility, per P-4 karen 1: 200 PublicKeyResponse{userId,publicKey,algorithm,createdAt}|byte-identical uniform 404 for not-permitted·blocked·nonexistent·no-key·malformed (no oracle)|401; self-fetch bypasses gate; ThrottlerGuard). ENCRYPTED ENVELOPE: POST /dm/conversations/:id/messages takes EITHER plaintext content OR {ciphertext,senderKeyRef,envelopeVersion} (mutually exclusive — server rejects both/neither/partial); encrypted → content IS NULL + ciphertext persisted (SERVER-BLIND, server never decrypts); listConversations preview → 'Encrypted message' for NULL-content last message. CLIENT: first DM use generates ECDH-P256 keypair (Web-Crypto), PRIVATE key = non-extractable CryptoKey in Dexie encryptionKeys[self] (never sent — export throws), public registered via PUT; AES-GCM encrypt/decrypt client-side; plaintext fallback for keyless peers. HONEST INDICATOR (ship-blocker, fails closed, 6 states = design/e2e-indicator.html): sole emerald #10b981 shield-check lock (data-testid e2e-lock-affordance) ONLY on provably-encrypted; plaintext=open-lock / group=slashed-shield / cannot-decrypt=key / loading=spinner all show 'Not encrypted', NEVER a false lock. Group DMs (≤10) OUT OF SCOPE for encryption in leg-3a → plaintext-fallback + not-encrypted indicator (group E2E deferred). CROWN-JEWEL PROVEN: T-5/T-8 LIVE two-client (Fixtures A+B, co-members ad62cd12) — A keygen-on-first-use (public via PUT, private NEVER on wire, export('pkcs8')→InvalidAccessError), sent E2E-PROBE-SECRET-K7Qx9M as a ciphertext envelope (plaintext never on wire), B DECRYPTED + read it, server API returns content:''+ciphertext (server-blind LIVE); emerald lock count=1 on encrypted DM / 0 on plaintext history / 0 on server channel (fails closed); who_can_dm 404 no-oracle matrix byte-identical LIVE (nobody+key/nonexistent/malformed all {message:'Encryption key not found',...,404}, malformed→404 not 500); F5 timing-flat (no oracle ~0.10s); F8 ThrottlerGuard active (429, no state leak, window resets); secret-grep clean; server-blind separate-connection SELECT content IS NULL + byte-identical 404 matrix (case D) proven by the merge-blocking dm-encryption integration spec vs postgres:16 at C-1. Fresh-head-tester T-9 gate APPROVED (attempt 1; source-verified — server-blind genuine not fabricated, no mock-the-SUT, 0 blocking). Findings→V-2 (0 critical): (MEDIUM) F-T5-1 client-side auth-guard race — entering DMs post-login intermittently bounces SPA to / on a transient DM-load 401 while GET /me stays 200 (session valid), recovers on re-nav; (INFO) F-T8-1 group/no-key indicator not live-constructible (single-recipient DM dialog) → fail-closed verified structurally (CI+DOM+server-channel control); (LOW) F-T8-2 server doesn't validate senderKeyRef vs sender's registered key (low risk under server-blind model). Prod left CLEAN: fixture public keys left (public material, fine); B who_can_dm restored to 'everyone' (baseline); no test-DM cleanup needed beyond the one probe message. Verified-prod fixtures: studyhall-e2e-fixture (A) + studyhall-e2e-fixture-b (B, userId da74148e), co-members of proof server ad62cd12.))**

**last_updated_wave81: 2026-07-09 (T-9 wave-81 — FOUNDER-DIRECTED SCROLL FIX: standalone full-page routes now scroll. ANNOTATION-ONLY regen (no new route/screen/endpoint — the 5 routes pre-exist; routes_added=[] routes_removed=[] coverage_gaps=[]). Founder report: "in the profile i cannot scroll all the information" at /settings/profile. ROOT CAUSE: globals.css locks html+body to overflow:hidden (app-shell pattern), but the 5 standalone full-page routes (ProfilePage /settings/profile, SettingsPrivacyPage /settings/privacy, PrivacyPage /privacy, TermsPage /terms, LandingPage /) rendered a min-h-screen root with NO inner scroll container → content past the viewport was clipped + unreachable. FIX = a shared FullPageScroll wrapper (apps/web/src/shell/FullPageScroll.tsx = <div class="h-dvh overflow-y-auto">, NO transform/filter/contain/will-change so LandingPage's position:fixed nav stays viewport-anchored) mounted as the ROOT of all 5 routes. merge e659b0a / PR #100; NO migration (frontend-only). wave_type = ui. body{overflow:hidden} app-shell lock UNTOUCHED; shell routes /app,/discover NOT wrapped (no double-scroll). LOAD-BEARING FOUNDER-BUG PROVEN FIXED LIVE (T-5, prod, authed Fixture A, 1280x720 constrained viewport): /settings/profile content over-fills by 1017px → wrapper scrollTop 0→1017 (exact max, reachedBottom) → the bottom-most control "Save academic identity" moved from top=1439 (719px below fold, unreachable) to top=422/bottom=458 (fully in-viewport). /settings/privacy scrolls to "Delete account" (scrollHeight 25617). Landing scrolls top→bottom with position:fixed header pinned at top=0 pre+post scroll (fixed-nav invariant intact — transform/filter/contain=none confirmed live). T-6 layout: exactly ONE page-level scroller (no double-scrollbar), 6px DS dark scrollbar (globals.css §9, thumb #3f3f46=--surface-600) applies as overlay (0px gutter, no reflow), wrapper bg transparent (passthrough), 0 token violations. T-1/T-2 CI-verified green on merge (run 29008456214: lint+typecheck+test 1m58s; web 747/747 incl. FullPageScroll unit + fullpage-scroll-routes DOM-root tests + the STABILIZED study-timer suite — a pre-existing real-timer flake fixed across 740d27f/69a9c43/b0f4c57 to unblock the required CI test check for ALL future PRs). T-3/T-4/T-7 SKIPPED (no contract/schema/heavy — pure client layout); T-8 SKIP-minimal (no auth/session surface; /app unwrapped + authed settings routes render for Fixture A → no auth regression). Fresh-head-tester T-9 gate APPROVED (attempt 1; founder bug independently source-verified fixed, no coverage theater, 0 blocking). CRITICAL FINDING → V-2 (HIGH, deploy-delivery NOT code defect, must-dispose-before-wave-closes): F-T5-1 STALE SERVICE-WORKER CACHE — the deployed page FIRST served a stale pre-fix bundle (index-AVNFN-ve.js, 0x h-dvh) from a Workbox precache SW, REPRODUCING the founder bug, even though index.html + the new bundle (index-R5obJ0iu.js, 8x h-dvh/32x overflow-y-auto) carry the fix; the wrapper only appeared after unregistering the SW + deleting workbox-precache-v2 + api-cache. Impact: the founder, as a returning user with a registered SW, will be served the stale bundle and still not scroll until the SW updates → may conclude the fix failed. Disposition menu: workbox skipWaiting/clientsClaim + versioned precache, a reload prompt, or forced SW update on deploy. Also (LOW) F-T2-1: no standalone unit asserts ProfilePage root===FullPageScroll (needs data mock; covered by SettingsPrivacyPage sibling + LIVE T-5). Prod left CLEAN: no profile/privacy data mutated (scroll tests read geometry + set scrollTop only, no field edits/PUT); the SW-unregister + cache-delete affect ONLY the ephemeral Playwright browser context, not prod server state. Verified-prod fixture: studyhall-e2e-fixture (A).))
