# .env.example — placeholders for project env vars.
# Populated at v6b (onboarding) from architecture/security.md + architecture/sdks.md.
# After onboarding, B-0 Action 3 appends new placeholders per wave.
#
# DO NOT commit .env (gitignored). Generate secrets per always-on rule #6:
#   openssl rand -base64 32

# Database
# DATABASE_URL=

# Auth
# (filled in at v6b based on chosen auth provider)

# Payments
# (filled in at v6b based on chosen payment provider)

# Storage
# (filled in at v6b based on chosen storage provider)

# Observability
# (filled in at v6b based on chosen error-tracking / metrics provider)
